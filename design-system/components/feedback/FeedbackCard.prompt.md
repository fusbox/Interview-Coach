Purple survey card — the product's user-feedback moment (purple = survey tone). 5-point emoji or numeric scale.

```jsx
<FeedbackCard title="How confident do you feel about this interview?" scaleType="numeric"
  lowLabel="Not confident" highLabel="Very confident" onRate={(v) => save(v)} />
```
