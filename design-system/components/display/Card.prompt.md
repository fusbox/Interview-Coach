shadcn-dialect card (12px radius, raised-1 shadow). `variant="glass"` gives the pale-blue glass gradient.

```jsx
<Card>
  <CardHeader><CardTitle>Sessions</CardTitle><CardDescription>Last 30 days</CardDescription></CardHeader>
  <CardContent>…</CardContent>
</Card>
```
