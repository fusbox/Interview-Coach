Tinted icon capsule — functional glyphs can sit in rounded tinted squares when they need more visual weight than inline icons.

```jsx
<IconBadge variant="primary" size="md"><Icon name="clock" /></IconBadge>
```

Note: unlike the source (which takes a lucide component), pass the icon as children.
