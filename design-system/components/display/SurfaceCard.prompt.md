Candidate section card — the default container on job-seeker pages (24px widget/card radius, `--candidate-shadow-card`).

```jsx
<SurfaceCard eyebrow="Coach Plan" title="Question plan" description="What the coach will cover and why.">
  …content…
</SurfaceCard>
```
