Labelled form field — micro uppercase bold label, 48px subtle-fill rounded-xl control, italic micro hint.

```jsx
<FormField label="Target role" hint="Shown to your coach" inputProps={{ placeholder: "Product Designer" }} />
<FormField label="Job description" kind="textarea" inputProps={{ placeholder: "Paste the JD..." }} />
```

Also exports `FieldGroup`, `FieldLabel`, `FieldHint` for custom compositions.
