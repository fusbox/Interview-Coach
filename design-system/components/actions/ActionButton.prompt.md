Candidate pill CTA — the job-seeker surfaces' primary action; hover lifts -2px, primary has a blue glow shadow.

```jsx
<ActionButton size="large">Start practice session</ActionButton>
<ActionButton secondary href="/dashboard">Back to Dashboard</ActionButton>
```
