Lucide stroke icon by name — use for every functional glyph; never hand-draw SVGs or use emoji as icons. Logos are brand assets, not icons.

```jsx
<Icon name="plus" size={18} strokeWidth={2.5} />
<Icon name="layout-dashboard" size={18} />
```

Available names: plus, x, check, search, chevron-right/left/down, chevrons-up-down, arrow-right, alert-circle, alert-triangle, check-circle, help-circle, info, clock, inbox, refresh-cw, layout-dashboard, sparkles, target, mic, keyboard, message-square, message-circle-question, file-text, briefcase, shield-check, list-checks, circle-user, trash, loader, circle. Icons inherit `currentColor`.
