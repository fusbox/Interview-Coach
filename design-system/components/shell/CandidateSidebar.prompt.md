Candidate desktop sidebar — fixed 16rem nav; rows scale 1.02 on hover, active row is a soft-blue pill.

```jsx
<CandidateSidebar activeLabel="Practice" logoSrc="assets/TA-logo.png" onNavigate={go}
  footer={<ActionButton size="large">New practice</ActionButton>} />
```
