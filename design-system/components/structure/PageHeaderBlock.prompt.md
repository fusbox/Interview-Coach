Page header block — large section header with a bottom hairline divider; used at the top of recruiter pages.

```jsx
<PageHeaderBlock title="Sessions" description="Manage practice invites." actions={<Button>New invite</Button>} />
```
