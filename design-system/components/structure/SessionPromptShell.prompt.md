Glass prompt panel — wraps the live question during a practice session (pale-blue glass, 32px radius, floating shadow).

```jsx
<SessionPromptShell><h2>Tell me about a time you resolved a conflict.</h2></SessionPromptShell>
```
