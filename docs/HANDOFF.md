# Interview Coach V2 Handoff

Status: Active resumption contract
Last updated: 2026-07-31
Current branch: `feature/candidate-v2-rebuild`

## Resume Here

V2 is a clean rebuild with locally accepted candidate-led, invited-candidate, and standalone recruiter journeys. Release scope now explicitly includes app-owned candidate registration/login alongside TalentArbor host launch, with full candidate feature parity and no host dependency on the app-account path. Slices 197-201 establish and locally gate the app-account foundation, lifecycle, protected product boundary, and recovery/operations contract. Slice 202 adds the target-aware deployment environment contract, and Slice 203 adds the locally proven least-privilege PostgreSQL execution boundary. Slice 205 lands the authored shared live-session composition and the durable candidate-only Hints/Strong response lifecycle. Slices 206-207 restore and refine the bounded post-submit coaching rhythm, immersive staged-feedback surface, single-owner session-entry transition, and live microphone-level visualization while advancing only acknowledgement tone to prompt bundle V15/adapter V16. Slices 208-209 repair both atomic boundaries required to launch canonical baseline questions that were not exposed in a smaller first round and tighten the text-composer action gap. The accepted Coach Desk home and opened surfaces, candidate entry refinement, and recruiter invitation workflow refinement are packaged on `feature/candidate-v2-rebuild`. Credentialed V15/V16 calibration remains the immediate core runway; physical-device microphone review and the Supabase staging role cutover remain explicit operator actions.

The 2026-07-31 demo recovery hotfix keeps both hero account-entry actions on a hard `/candidate/register` navigation, prevents the noninteractive intro reveal layer from intercepting them, and clears a failed finish message when the candidate deliberately starts a linked answer retry. Production metadata confirmed the supplied retry lineage, completion snapshot, accepted evaluator runs, and Coach Update were internally consistent; the observed finish/dashboard failures were `EMAXCONNSESSION` rejections from a Vercel deployment using the Supabase session pooler on port `5432`. The Vercel preflight now rejects that configuration and requires transaction mode on `6543`. This hotfix requires a Vercel `DATABASE_URL` correction and redeploy but no schema migration; the separate least-privilege runtime-role cutover remains an explicit release gate.

### Demo Deployment Packaging

The current application chain is packaged first on `feature/candidate-v2-rebuild`, excluding the separately staged discovery and validation-fixture lanes. Exact clean-snapshot verification passes TypeScript, zero-warning lint, documentation links, 99 candidate files / 697 tests, 11 recruiter-auth files / 60 tests, 56 recruiter/invited files / 196 tests, and the optimized production build. That source checkpoint is forward-integrated into the latest `feature/integration-TA`; the resulting integration head is then forward-integrated into the latest `release/njcareers`. Both shared branches retain their target-only history and use normal non-force pushes. `release/njcareers` is a demo deployment target in this pass, not evidence of production-release approval; the production gates below remain unchanged.

Read in this order:

1. [SPEC](./SPEC.md): product behavior and boundaries.
2. [DATA_CONTRACT](./DATA_CONTRACT.md): durable ownership, state, and lineage.
3. [Local Dev Bootstrap](./09-dev/local-dev-bootstrap.md): current environment, database, and verification commands.
4. The subsystem contract linked from the current-state table.
5. [Production UI Workstream](./03-design/production-ui-workstream.md) when changing tracked product UI.
6. [Autonomous Development Operating Model](./07-ops/autonomous-development-operating-model.md) before executing a multi-slice milestone.

Before a meaningful workflow slice, inspect matching V1 behavior from `feature/candidate-module` and classify it as `preserve`, `reinterpret`, `retire`, or `defer`. Use `.agents/skills/senior-slice-pass` for meaningful slices, `senior-milestone-pass` at integrated boundaries, and `senior-release-pass` before deployment, pilot, migration, or release decisions. Use `.agents/skills/autonomous-milestone-run` only after the milestone outcome, authority envelope, and acceptance evidence are explicit.

## Next Work

### Core Lane

195. Run the post-UI V15/V16 evaluator calibration gate. Start with the focused technical-boundary, answer-usability, criterion-availability, strong-answer, and acknowledgement-tone cases, then run the complete credentialed scenario set. Compare candidate-visible coaching, question-preparedness bands, Coach Update framing, and execution/assertion failures against the accepted fixture baseline. Do not promote a serving profile, alter evaluator meaning, or expand provider cost without separate review.

Alongside Slice 195, prioritize broader operation cleanup jobs, dependency disposition, telemetry/alert sinks, and release evidence. Real TA staging integration remains dependent on final host/network access and upstream acceptance details.

### Production UI Lane

The dedicated `feature/candidate-v2-production-ui` worktree remains the historical parallel lane, but concurrent UI work is currently inactive. For the bounded dashboard migration beginning on 2026-07-31, the repository owner explicitly transferred implementation ownership to `feature/candidate-v2-rebuild` in this worktree. Preserve the shared-file lock, consume current route/read/mutation contracts, preserve all failure and recovery states, and avoid duplicating domain logic. Do not resume parallel edits in the dedicated UI worktree until ownership is explicitly reassigned.

Dashboard composition is now governed by [Candidate Dashboard Composition Direction](./03-design/dashboard-composition-direction.md) and [Dashboard Progress Visualization](./03-design/dashboard-progress-visualization-contract.md). The visual references live under `.untracked/design-system-maturation`; `.untracked/ui-lab` is the disposable implementation/probe workspace and its pages are not mockups. The Phase 1 lab probe now covers the five priority states, one dominant first-viewport stage, a compact Strong-of-plan completion rail, two alternative opened-Plan structures, the production transcript canvas with annotated/plain/thin fixtures, light/dark themes, three review-only dashboard canvas studies, and mobile-to-desktop reflow. `Diagonal confluence`, `Cool horizon`, and `Peripheral mesh` reuse primary blue and the cooler `--chart-2` teal beneath the same content without assigning new semantic meaning. The ready Coach Update stage consumes the shared spotlight utility intact, including its gradient, panel radius, border, and elevation; exact production compass assets distinguish unseen, active-review, and neutral reviewed/detail states. Its earlier pill rim is removed in favor of a bounded top-edge accent and dark-legible radial light. The current white dark-mode action is deliberately softened through optical falloff and reduced shadow, while white-versus-dark remains a human-review decision. The unfinished-round probe no longer fabricates a saved-question secondary row already duplicated by its dominant resume stage. The latest density pass removes the ready-update answer-count row already stated by its acknowledgement, gives the neutral Practice Next row a lightning cue plus separated source/recommendation/question typography, and replaces the Plan rail's competing gauge and horizontal segments with an enlarged overall gauge beside a vertical shape-and-label question breakdown under `Coach plan progress`. The following material refinement keeps every prep-state badge circular, makes Strong the only filled/high-emphasis badge with a checkmark, lightens every other state through its preparedness role, turns the unchanged-height role switcher into a pill, and reverses the promoted Practice Next light so green enters from the upper-left through a gradient border. The current Plan-rail material pass keeps `View plan` in a compact `44px` header, gives the gauge and question breakdown equal centered cells, and places their lighter translucent pane inside darker smoky-glass shell gutters with `32px` side depth; the footer matches the header at `44px`. The opened-Plan refinement removes the redundant Plan-rail heading, replaces compact inherited text measures with available-width layout, moves `Why this plan` onto the sheet canvas, adds a rounded accessible Q1–Q5 tab switcher beneath it, and removes the duplicate bottom question-set control. Promoted Practice Next now uses the same lightning cue inside a white surface badge. Round-level Coach Update entry names the practiced question range, tactical guidance names its exact question, and Practice Next uses a neutral surface with viridian limited to its stroke, icon, tint, and bounded directional path. Categories, Skills, and Question Set remain possible content domains, not required faces. Opened Coach Update feedback-surface work was explicitly outside that lab pass. Its production-change gate is now closed by the accepted home migration recorded below. The former peer-card grid and standalone preparedness block were known prune debt, not a pixel-locked baseline.

The current opened-Plan probe makes the canonical question set the operational spine while adding an explicit `Questions` / `Categories` comparison tab pair. Questions remains the default: its tabs expose the same highest-earned prep states as the Plan rail, upcoming wording remains hidden until deliberate reveal, selected questions receive tailored answer-building guidance and a direct practice action, and practiced Q1 opens `Review your answer`. That disclosure now reuses the same answer-review component as opened Coach Update: a `Your answer` production transcript canvas plus separately owned whole-answer observation and `Try next` guidance when accepted projection data supplies them; plain fallback renders only the transcript. Categories reinterprets the useful live behavior through the original maturation plan's category-first/question-peek pattern: a category selector opens purpose, answer-shape guidance, one weak-pattern warning, constituent question states, and a `Strong X of Y` count derived from those same question states; selecting a question returns to its question detail. It is not a second practiced-count completion model, and the explicit category face remains a review probe rather than ratified production navigation. The full question-set presentation remains available as a comparison structure.

The latest surface-factoring pass supersedes the earlier two-color Plan-rail shell-and-pane experiment. Every non-spotlight dashboard card now consumes `--radius-card`; the ready Coach Update continues to consume `--radius-panel` through `.surface-spotlight`. Opened Coach Update and Coach Plan sheets consume `--radius-feedback-modal`, contained answer-review cards consume `--radius-card`, and the annotation popover consumes its existing popover role. Radius roles remain independent from elevation. Coach Plan progress now uses one translucent, blurred glass surface across header, gutters, progress field, and footer; its inner grid is layout-only with no second fill, radius, or shadow. Verification: lab lint/build, documentation links, computed token values across every dashboard lifecycle card and both opened surfaces, light/dark screenshots, 390px and 1440px overflow checks, and zero browser console errors.

The latest Diagonal-confluence calibration makes the complete light-theme canvas substantially lighter and more washed out. Both corner color fields and every blue-to-green base stop move to higher OKLCH lightness with roughly half the prior chroma, keeping the cool color direction visible while returning visual priority to the cards. Dark mode, comparison canvas studies, card materials, and elevation remain unchanged.

The latest Coach Plan progress readability pass removes the obsolete empty footer, increases the single-glass opacity to `0.68`, removes the visible border, and uses `--shadow-raised-2` plus a restrained inset top highlight. That token supplies a visible `0 8px 24px` light-theme shadow and its existing `0 8px 32px` dark-theme override without promoting the rail to panel or modal elevation. The progress field now uses a `96px` gauge with `X of Y` language, sentence-case `Overall` / `Questions` labels, a quieter track, a heavier blue progress stroke, a shorter low-contrast divider, and more evenly aligned question rows. `View plan` remains a stronger text action rather than another pill or nested surface. The resulting card is `236px` tall in the representative state, remains overflow-free at `320px`, `390px`, and `1440px`, and preserves the one-surface glass contract.

The following gauge-alignment pass top-anchors both Plan-progress cells so centered `Overall` and `Questions` labels share an exact baseline and the gauge starts directly beneath its header rather than centering against the full five-row list. The standard gauge grows to `108px`, with a responsive `92px` size below `23rem`; its progress stroke now consumes theme-aware `--prep-strong`, and `--shadow-raised-1` is applied as drop-shadow depth to the SVG rings beneath the card's stronger `--shadow-raised-2`. The gauge center remains transparent to the card glass instead of introducing a circular badge surface. The card eyebrow moves from primary blue to the intermediate neutral text role `--text-secondary`. Computed light/dark token values, exact header coordinates, the zero-Strong state, 320px fit, and browser diagnostics were verified.

The quiet unpromoted Practice Next material probe now uses negative space instead of another visible card plane. Its original background and one-pixel border declarations remain available through the shared quiet-row base, while the Practice Next modifier renders both transparent in light and dark modes. Its `36px` lightning badge replaces the opaque accent fill with low-opacity `--glass-tint`, backdrop blur, restrained inset light, and `--shadow-raised-1`; the badge midline aligns to the eyebrow text within `0.02px` rather than centering against the complete copy block. Both dashboard Coach Update appearances render their compass artwork at `36px` inside a `38px` one-pixel ring, while opened-feedback avatar sizing stays unchanged. The trailing action reads `Start` because the eyebrow already identifies Question 1, and the action shares that eyebrow row's midline with the badge. The three-level text hierarchy, primary action color, full-row hit target, and keyboard focus outline remain intact. The promoted Practice Next stage is unchanged. This is a reversible lab comparison rather than a production token decision.

The next dashboard-composition probe replaces the role-duplicating introduction with an accessible `Practice` / `Progress` tab group. Practice remains the default and owns the lifecycle stage plus its truthful quiet supporting action; Progress moves the existing Coach Plan rail into the primary position and may show the same already-supported quiet action beneath it. The Plan rail is rendered once per selected view rather than copied into both panels, and no category, score, or preparedness fact is fabricated to fill space. The switcher starts from the existing rounded tab interaction pattern, adds roving Arrow/Home/End keyboard focus and 44px targets, and tests a contoured glass material with restrained elevation; the material remains explicitly provisional. Dashboard-home text is concurrently normalized to shared global semantic typography roles instead of keeping a collection of one-off sizes and `750` weights. Opened Coach Update and Plan typography, category visualization, and wider surface maturation remain outside this slice.

The after-review material pass gives the state an explicit material ladder over the washed diagonal canvas. Promoted Practice Next is the one raised action slab: borderless translucent neutral glass on `--radius-card`, `--shadow-raised-2`, a restrained inset top highlight, and a localized upper-left viridian bloom instead of the prior gradient border. Its white lightning badge, question-owned content, primary action, and secondary customization path remain unchanged. The reviewed Coach Update row is now canvas-native and borderless at rest, with blue localized behind its production `surface` compass; hover and focus reveal only a low-opacity glass fill plus `--shadow-raised-1`. The row remains one full-width action in both Practice and Progress views. No lifecycle copy, opened surface, or production route changes in this slice.

The immediate material correction removes authored gradients and colored washes from both after-review surfaces. Promoted Practice Next now uses one neutral translucent fill; any green visible within it comes only from the shared canvas through the glass. Reviewed Coach Update now keeps a very-low-opacity neutral glass fill, `--shadow-raised-1`, and inset contour visible at rest, with only a modest opacity increase on hover/focus. Its former localized blue surface light is removed; the compass artwork continues to carry Coach Update identity. The promoted lightning badge face is increased from `28px` to `36px`, matching the compass artwork; the compass retains its `38px` one-pixel wrapper. The same pass keeps the recommendation heading on `display-sm`, tightens the instruction to `body-md`, lowers the secondary action to `label`, and replaces the Practice Next stage's uniform gap with grouped source, recommendation, and action spacing. It also removes the tab group's blue/green border gradient, mixed-value fill, and saturation boost: both the shell and selected tab now use single neutral translucent fills, so any color there is inherited from the canvas through glass. The shell uses the same `0.14` resting surface opacity as the reviewed Coach Update row in light and dark modes; the selected tab remains a stronger but reduced-opacity nested glass layer. Content and behavior remain unchanged.

The canvas-color correction supersedes the earlier blue/teal canvas study without changing its layout geometry. `Diagonal confluence`, `Cool horizon`, and `Peripheral mesh` now use only blue-family stops in both themes; teal and green are removed from the canvas layer. Semantic green remains available to component-level meaning such as Strong progress and the lightning accent, but neutral glass surfaces must reveal only the blue canvas beneath them.

The quiet Practice Next material correction retires the invisible-surface experiment. Its full-row action now shares the reviewed Coach Update row's `0.14` neutral glass fill, `--shadow-raised-1`, inset contour, blur, and neutral interaction-opacity ladder in both themes. The existing `36px` clear-glass lightning badge, typography, alignment, copy, and behavior are unchanged.

The unfinished-round and cold-start parity pass is the final lab gate before dashboard migration. Both state-owned stages now share the promoted Practice Next card's borderless raised neutral-glass foundation while retaining their distinct facts and actions. Their former `1px` primary border, opaque fill, flat shadow, `15px` inline state icon, and uniform component gap are retired. Each state now uses a `36px` circular glass icon badge, semantic dashboard typography, and grouped spacing from state metadata through question context and actions. Unfinished round keeps the answered/current progress strip and one Resume action; cold start keeps the plan-ready count, first-question action, and lower-emphasis Plan review path. Verification must cover both themes at `320px`, `390px`, and desktop width before the accepted lab composition moves into the rebuild branch.

The accepted dashboard-home and opened-surface migration is now live on `/candidate/dashboard` in the rebuild worktree. The route uses the tokenized blue canvas and transparent header, glass role/queue controls, accessible Practice/Progress tabs, five read-model-driven lifecycle states, quiet supporting actions, and the Strong-of-plan progress rail. Unfinished round and cold start passed the same semantic typography, `36px` icon, grouped-spacing, and truthful-content checks as the reviewed states; neither fabricates a secondary status row. Opened Coach Update now uses the extracted production dialog: question context stays outside a shared answer review, the nested canvas contains only `Your answer` and the immutable transcript, whole-answer observation and one warm-coral `Try next` remain separate, annotations reveal on hover/click/keyboard/tap, and noncurrent carousel slides are removed from focus and active height. Opened Coach Plan now defaults to Questions, offers Categories as a secondary context lens, derives all Strong counts from rated preparedness, reuses the same exact answer-review component when source-linked evidence exists, and preserves hidden upcoming wording. Both opened surfaces use `--radius-feedback-modal`, tokenized sheets, 44px effective controls, responsive top-sheet geometry, focus containment/restoration, scroll locking, Escape, and permitted clickaway. Five focused files pass 27 tests, the complete candidate suite passes 99 files / 697 tests, and build, lint, docs, diff, and authenticated 320px/1280px light/dark browser checks pass. The retained priority compatibility consumer now imports the same extracted Coach Update dialog as the live Coach Desk, so there is one authoritative opened-feedback implementation across both paths.

Only one lane may own a shared file at a time. Treat `src/index.css`, shared design-system components, route pages, `package.json`, `SPEC.md`, `DATA_CONTRACT.md`, and this handoff as explicit integration files. See [Production UI Workstream](./03-design/production-ui-workstream.md).

## Current State

| Domain | Landed contract | Main remaining work | References |
| --- | --- | --- | --- |
| Candidate identity, setup, and resume | App-owned account and host launch are independent entry adapters converging at opaque candidate-profile ownership. Shared password/session primitives, candidate role, one-to-one app profile binding, app-cookie precedence, symmetric host/app profile fences, transactional registration, mutable contact preferences, immutable consent receipts, explicit single-use email verification and recovery, candidate login/logout, all-app-session reset revocation, DB-backed metadata-only rate/audit controls, deterministic local accounts, public account UI, and a self-contained cross-browser account gate are landed. One shared access resolver guards `/candidate`, setup, dashboard, session, Coach Update, resume, and follow-up pages and mutations; recruiter and invited cookies grant no candidate-owned access, and a present invalid app cookie cannot fall through to host access. Phone is captured but remains unverified and is not an auth factor. The two-minute TA exchange, seven-day host session, setup, and resume lifecycle remain durable. RW remains disabled. | Real TA acceptance, deployed SMTP/grants/log-redaction evidence, and deployed resume controls. | [Authenticated access](./02-requirements/authenticated-candidate-access.md), [Account milestone](./05-quality/candidate-account-access-milestone.md), [Practice setup](./02-requirements/practice-setup-scope.md), [Resume ingestion](./04-architecture/storage-and-resume-ingestion.md) |
| Practice planning and wording | Stage owns one immutable 5/5/7/10/10 prep-context baseline and complete wording set. Round-one count selects from that baseline; follow-up rounds reuse stable question lineage; supplemental questions never expand the denominator. Production Gemini wording is configuration-fingerprinted and fail-closed. V2 wording now minimizes unjustified technical-reference dependency. | Credentialed multi-role V2 wording calibration, deployment approval, and future baseline evolution only when product requirements demand it. | [Practice Plan baseline](./04-architecture/practice-plan-baseline-and-round-selection.md), [Question wording](./05-quality/production-question-wording-integration-contract.md) |
| Live session and evaluation | Candidate-led and invited paths share the authored cutout-based production shell, typed/voice practice, immutable attempts, autosave and exact recovery, retries, question audio, transcript-first recording, evidence-first evaluation, immediate coaching, and truthful continue-without-coaching behavior. Candidate-only Hints generate automatically when a question becomes current; Strong response generates only after explicit request. Both use durable ownership-scoped artifacts and exact session/question lineage, replay across recovery and tabs, and never become evaluator evidence or recruiter-visible transcript content. Fixed three-step typed and four-step voice progress overlays cover the complete accepted-answer/analysis interval; quick voice submission enters that shared overlay without a duplicate transcript/save loader, while explicit Review retains transcript preparation. Coaching opens in a tall 32px-radius staged surface whose visible chrome is limited to coach identity and progress; stage labels remain assistive-only. Voice is the default absent an explicit persisted text choice, and a transient Web Audio analyser drives the live level display without persisting or transmitting amplitude samples. V15 appraises answer-demonstrated role capability without inferring qualification, keeps `not_assessed` technical accuracy neutral, derives one transparent question-preparedness result from the complete universal criterion set, and uses that result to move on, polish, or remediate. Category signals can be grounded by exact span, whole-answer meaning, or meaningful absence. | Credentialed V2 wording, assistance, and V15/V16 evaluator calibration; provider-side audio approval, physical-device microphone/visualizer evidence, and later audio hardening remain. Photo answers remain deferred. | [Question wording](./05-quality/production-question-wording-integration-contract.md), [Question preparedness](./04-architecture/question-preparedness-progress-contract.md), [Evaluator](./05-quality/production-evaluator-integration-contract.md), [Voice](./04-architecture/voice-answer-transcription-contract.md) |
| Dashboard, Coach Update, and follow-up | Opaque prep-context scoping, active-round recovery, immutable practiced-question Coach Update, provenance-bound transcript canvas, Coach Plan coverage, editable next-round queue, one-question/fixed-set actions, and idempotent follow-up launch are wired. Atomic queue-to-intent and intent-to-session launch now resolve never-exposed missing-evidence questions from the matching immutable prep-context baseline while keeping feedback-driven practice tied to persisted source-round answer and analysis evidence. The production dashboard consumes the canonical preparedness projection, keeps coverage separate from highest-earned achievement and latest feedback, and fails malformed historical analysis closed as evaluation unavailable. The accepted Coach Desk composition is now implemented on `/candidate/dashboard`: Practice/Progress views, five priority-driven lifecycle states, tokenized glass materials, Strong-of-plan progress, the question-owned opened Coach Update, and the Questions-first Coach Plan with a secondary category lens all consume the existing read and mutation contracts without fixture-only behavior. | Human review of the opened Plan category lens and answer-review density remains appropriate before release. A criterion aggregate projection is added only if a distinct candidate need survives review. Multi-round trends, regression policy, trusted coach bundles, resume-version staging, background generation, and release-environment proof remain later work. | [Composition](./03-design/dashboard-composition-direction.md), [Progress visualization](./03-design/dashboard-progress-visualization-contract.md), [Question preparedness](./04-architecture/question-preparedness-progress-contract.md), [Dashboard IA](./04-architecture/evidence-first-dashboard-information-architecture.md), [Transcript canvas](./03-design/coach-update-v2-card-spec.md) |
| Invited and recruiter V2 | Invites exchange bearer links into isolated sessions; initials are a non-gating match signal. Shared live practice, pause/recovery, summary, whole-round repeat, latest-answer-only recruiter transcript, app-owned recruiter auth/settings, fixed-slot invite creation, durable delivery ledger, dashboard, handoff, and guarded SMTP verification are locally accepted. | Deployed SMTP/network evidence, key rotation window, recruiter rate/cleanup controls, optional experience feedback, and future TA recruiter integration. | [Invited runtime](./04-architecture/invited-practice-live-runtime.md), [Recruiter delivery](./04-architecture/recruiter-v2-delivery-and-host-integration.md), [Recruiter milestone](./05-quality/recruiter-standalone-flow-milestone.md) |
| AI-eval operations | Migrations `037-041` provide an individually granted workbench, exact source review, findings, remediation/rechecks, versioned scenarios, deterministic and credentialed runs, operation checkpoints, cost/call preview, complete candidate-visible layer inspection, same-profile comparison, a long-running worker service contract, and guarded dry-run/apply retention with immutable receipts. V9 has an accepted 32-case V3 credentialed calibration; V15/V16 is locally accepted through focused fixtures and retains the V14 question-preparedness contract while tightening acknowledgement tone. The active Scenario picker exposes baseline V5 and later while retaining baseline V1-V4 only as restorable persistence and historical run evidence; current operator-authored work remains available as `Custom`. Browser routes never call providers. | Run focused and complete credentialed V15/V16 calibration; then deploy/supervise the worker, schedule retention through a dedicated maintenance role, and prove grants, masking, telemetry, and alerts. Alternate-profile A/B remains deferred. | [Workbench](./05-quality/ai-eval-operator-workbench.md), [Scenario lab](./05-quality/evidence-first-coaching-scenario-lab.md), [Worker/retention](./07-ops/ai-eval-worker-and-retention.md) |
| Production UI | The lean global system now drives a coherent candidate setup, initial/follow-up landing, live-session, complete Coach Desk dashboard composition, and public candidate-account entry. `/candidate/setup` uses the 56rem form-flow frame and one scroll-aware Role/Resume/Interview timeline. The shared pre-session landing presents the immutable question plan, bounded long-text expansion, audience-correct assurances, first-question audio prefetch, and candidate-led Coach Plan return. One destination-owned transition covers routed entry without a competing source overlay or generic skeleton. The shared candidate/invited live session follows the authored mobile hierarchy with compact sticky context, segmented progress, cutout question/composer surfaces, durable assistance controls, voice-default lifecycle composition, one focused desktop column, and an immersive staged-feedback surface with promoted content rather than visible scaffold titles. The dashboard now uses the tokenized blue canvas, glass controls and lifecycle stages, Practice/Progress switcher, plan-completion rail, opened evidence review, and question/category Plan reference. Purple remains session-specific to Strong response and voice Review. Registration, login, and explicit email verification use the shared candidate brand frame, tokenized responsive surfaces, accessible labeled inputs, truthful loading/error states, and compact policy access. | Human visual review of setup/account entry, opened dashboard detail, and physical-device session microphone/visualizer states, then production replacement of invited/recruiter/QA surfaces and remaining provisional/compatibility consumers. | [Design system](./03-design/design-system-foundation.md), [UI milestone](./05-quality/candidate-production-ui-milestone.md), [UI workstream](./03-design/production-ui-workstream.md) |
| Quality and operations | Candidate/recruiter suites, migration smokes, type/lint/build, deterministic browser journeys, WCAG automation, local production budgets, metadata-only telemetry contracts, and provider/mailbox gates are established. PostgreSQL now has a migration-owner/runtime-role split, no Data API role access, RLS-backed runtime containment, a reviewed direct-function allowlist, hardened security-definer resolution, and a fresh-install smoke. The app is locally demonstration-ready, not release-ready. | Cut the Supabase staging runtime over to `interview_coach_runtime`, disable its unused Data API, capture the hardening smoke, then complete real-host/provider evidence, alert delivery, staging performance, manual accessibility, dependency disposition, rollback ownership, and senior release pass. | [Database access hardening](./07-ops/database-access-hardening.md), [Production controls](./07-ops/production-hardening-and-deployment-controls.md), [Accessibility](./05-quality/accessibility-baseline.md), [Test strategy](./05-quality/test-strategy.md) |

## Phase Map

Percentages are directional implementation estimates, not schedule or release claims.

| Phase | Progress | Characterization |
| --- | ---: | --- |
| A. Candidate identity, host launch, setup | 96% | Host launch and setup are production-shaped, and app-owned registration, versioned consent, email verification, candidate login/logout, single-use recovery, all-app-session revocation, rate/audit controls, deterministic account seeds, and the shared protected candidate route boundary are durable. Browser milestone proof and real-host acceptance remain release-critical. |
| B. Planning, session, and coaching engine | 98% | Baseline planning, wording, shared text/voice runtime, evidence-first evaluation, question preparedness, Coach Update, and the candidate production shell are mature; credentialed calibration and deployment evidence remain. Optional trusted-reference retrieval is not required for the release. |
| C. Dashboard and follow-up practice | 99% | Core feedback/feedforward loop, stable prep-context scoping, queue, direct practice actions, baseline-only follow-up launch, highest-earned progress, and the Coach Desk home/opened-surface composition are durable; regression handling, later trends, and resume evolution are enhancements. |
| D. Invited and standalone recruiter | 98% | End-to-end local flow is accepted, including email, repeat practice, operational dashboard, and transcript boundary; deployed SMTP and future host integration remain. |
| E. QA/evaluation operations | 98% | V9/V3 credentialed calibration and production-shaped worker/retention contracts are accepted locally; V15/V16 retains the fixture-accepted question-preparedness policy and stages the bounded acknowledgement-tone update for post-UI credentialed calibration, followed by deployed supervision, scheduling, grants, masking, and alerts. |
| F. Production UI integration | 97% | Canonical setup, shared initial/follow-up/invited landing, the shared live session, post-submit coaching progress, modal staged feedback, Coach Desk home/opened detail, public candidate account entry, and recruiter invitation workflow refinement are translated into production code with behavior-preserving tests. Broader invited/recruiter/QA replacement, physical-device voice review, and remaining compatibility-surface replacement remain. |
| G. Production hardening and release | 89% | The least-privilege PostgreSQL runtime boundary and fresh-install hardening smoke are locally accepted. Supabase staging cutover, Data API disablement, host/network/provider evidence, alerts, dependencies, manual accessibility, organizational approvals, and release evidence remain. |
| H. Retirement and docs cleanup | 85% | Canonical docs are promoted to `docs/`, the authority index and autonomous operating model are active, superseded material is visibly archived, obsolete handoffs are retired, raw staging exports are local-only, and link integrity is automated. Compatibility code/fixtures, remaining active-doc semantic compaction, and Git-history disposition remain. |

## Release-Critical Risks

- **Host acceptance:** run the real TA signed-token/network matrix; confirm issuer/source values, mint-per-click behavior, least-privilege MSSQL access, secret rotation, and upstream token-query redaction. RW is not enabled.
- **Candidate account security:** registration, verification, and recovery are enumeration-safe; candidate return targets are bounded; reset changes revoke every app-owned session; rate/audit controls avoid account identifiers; and protected candidate pages/mutations share one fail-closed resolver. The least-privilege runtime grant is locally accepted; complete Supabase staging cutover evidence and end-to-end deployed ownership/isolation proof before enabling public account creation.
- **Provider and email operations:** obtain organizational approval for Gemini use/data handling; prove deployed audio, evaluator, wording, Coach Update, OCR, and SMTP behavior with telemetry and rollback ownership.
- **Operational containment:** wire alert sinks, parser/OCR resource containment, retention cleanup, deployed AI-eval worker recovery, key rotation, and end-to-end access-log redaction for `/candidate/launch?token=...`, `/s/[token]`, `/candidate/verify-email?token=...`, and `/candidate/reset-password?token=...`.
- **Dependency posture:** resolve or explicitly accept the current `@google/genai` cleanup-chain, Next/PostCSS, and Sharp/libvips advisories through tested upgrades; do not apply npm's regressive Next downgrade or untested overrides.
- **Security cleanup:** raw staging CSV discovery exports have been removed from the current documentation tree and are ignored under `docs/09-dev`; determine whether historical Git objects require separate remediation before release. Commit no new raw candidate/job exports.
- **Release evidence:** complete staging performance, manual accessibility, environment/rollback, post-deploy smoke, and a senior release pass. Local production-shell evidence is not a substitute.

## Bounded Debt And Deferred Product Work

- Compatibility redirects `/practice2`, `/session2/[sessionId]`, and `/dashboard2`, plus the nonproduction browser bridge, remain retirement candidates.
- Cleanup jobs are still needed for setup-start, direct-intent, resume-ingestion, and recruiter question-set rows. AI-eval scenario retention is locally implemented but still needs a deployed schedule and maintenance-role grant.
- Invitation token key rotation, recruiter action rate limits, recruiter-create draft recovery, and `outcome_unknown` operations policy remain before pilot.
- Pending Coach Update has explicit repair but no background polling/worker contract. Full-history dashboard reads may later justify a versioned projection.
- Attempt trends, engagement-event BI, resume replacement/question reconciliation, stage evolution, candidate photo answers, production reference-library implementation, and alternate-model A/B are deferred product work.
- First-release question progress is highest earned. A first regression remains stable; later regression behavior is explicitly deferred until live product evidence can shape its window, messaging, and recovery rules.
- V1 data compatibility is not required. V2 development facts must remain truthful rather than backfilled with invented provider/configuration history.
- Candidate session progress writes still use optimistic browser state and fire-and-forget durable `PUT` calls. Draft and answer persistence have stronger recovery contracts, but a failed progress-only write is not yet surfaced while landing copy says progress is saved. Bound this before release rather than weakening the recovery claim.

## Recent Milestones

- **NJ Career visual-brand alignment:** replaces the TalentArbor image mark with the exact `release/njcareers` NJ Career asset across all nine live public, candidate, invited-practice, recruiter, and QA shell placements. Accessible names now identify NJ Career, intrinsic image dimensions match the source asset, and the shared footer selector is brand-neutral. TalentArbor host-launch identities, domains, policy links, provider contracts, and source-system values remain unchanged.
- **Slice 209, baseline-only follow-up session launch completion:** repairs the second atomic boundary behind the reported `PRACTICE_INTENT_INVALID_SESSION` 503. The intent-to-session function now permits exact candidate-owned prep-baseline wording only for `practice_missing_evidence`; feedback-driven questions remain bound to persisted source-round wording, and ownership, prep-context, intent/session snapshot, optimistic-count, replay, and immutability checks remain intact. The rollback smoke now proves the full 5-question baseline, 3-question first round, 2-question queue, immutable intent, and created follow-up session path. Text-mode composer spacing also reduces the overcorrected 16px action reserve to the compact 8px token. Verification: 55 next-round tests, 19 focused launch tests, 17 shared-session shell tests, the end-to-end database smoke, and the database hardening smoke pass. Human browser retry of the existing ready intent and visual confirmation of the compact text gap remain acceptance checks.
- **Slice 208, baseline-aware Practice Next launch repair:** aligns the atomic PostgreSQL queue validator with the canonical prep-context baseline already used by the dashboard builder. A `practice_missing_evidence` item anchored to the original round may now launch when its stable question exists in the matching candidate-owned baseline even if that question was not among the smaller first-round snapshot. Feedback-driven practice remains restricted to persisted source-round wording plus matching latest submission/analysis evidence; unknown baseline keys, cross-owner pointers, malformed payloads, and stale versions still fail closed without clearing the draft. Migration `048` is wired into the normal chain and a rollback-only 5-question-baseline/3-question-round/2-question-follow-up smoke proves exact intent creation, queue clearing, version advancement, and invalid-key preservation. Verification: 53 next-round tests, 692 candidate tests, the transactional smoke, the full migration chain, zero-warning lint, docs links, and diff check pass. Human browser replay of the reported prep context remains the final acceptance check.
- **Slice 207, session feedback and voice refinement:** removes the duplicate quick-submit transcript/save presentation so voice answers enter the shared four-step progress overlay immediately; explicit Review alone retains transcript preparation. The feedback surface now uses a dedicated 32px radius and taller viewport-bounded composition, shows one theme-correct coach avatar, promotes all stage copy, and keeps scaffold stage labels assistive-only. Follow-up entry has one destination-owned transition across Next.js loading and mounted-session handoff, with no generic skeleton between states. Voice is the default absent an explicit persisted text choice, and a transient Web Audio analyser drives the live visualization without storing or transmitting level samples. Verification: 70 focused tests, the 692-test candidate suite, zero-warning lint, valid documentation links, and live browser confirmation of loader, feedback, avatar, transition, and default-mode behavior. Physical-microphone response remains the device gate.
- **Dashboard Phase 1 local design gate:** replaces the unrelated `.untracked/ui-lab` entry view with a mobile-first review probe while preserving the lab as non-authoritative workspace. Five lifecycle states keep one dominant stage above a quiet secondary and compact Plan rail; Strong-of-plan is explicitly progress toward completing all canonical Plan questions, with practiced coverage kept separate. The probe includes guided and question-first opened-Plan structures, the production `CandidateTranscriptCanvas` with annotated, plain-fallback, and thin-answer fixtures, light/dark review controls, bottom-sheet-to-dialog reflow, Escape/backdrop/close behavior, focus return, and 320px through wide-screen layouts. The latest refinement makes every actionable guidance block traceable to a question number, text, or practiced range; keeps the unopened Coach Update at round acknowledgement depth; returns Practice Next to a neutral surface with bounded viridian accents; and corrects dark raised-surface hierarchy plus light/dark transcript contrast. The following material pass removes the fabricated unfinished-round secondary row, uses the exact production `cta`, `calm`, and `surface` compass assets across Coach Update states, replaces the presence pill rim with a bounded top-edge gradient and dark-legible radial light, and keeps the dark spotlight action optically softened white as a review hypothesis. The density/simplification pass removes the duplicate ready-update range/count row, gives unpromoted Practice Next a lightning cue and clearer three-level type rhythm, and recasts Plan progress as one enlarged overall gauge beside a compact vertical question breakdown. The latest material refinement makes all question badges circular, reserves filled-plus-checked emphasis for Strong, lightens every other preparedness role, preserves the role switcher's 44px height while making it a pill, and reverses the promoted Practice Next gradient and border lighting toward the upper-left. The current rail pass places `View plan` in an exact `44px` header, equalizes and centers the two progress cells, keeps the matching footer at `44px`, and makes the `32px` side gutters visibly part of a darker smoky-glass shell around one lighter inset glass pane. The opened-Plan pass removes the redundant Plan-rail heading and bottom question-set entry, replaces the soft-blue `Why this plan` card with quiet canvas orientation, adds an accessible rounded Q1–Q5 switcher, removes compact inherited text measures from its orientation and selected-question copy, and moves the promoted lightning cue into a white badge. The opened-Coach-Update pass keeps question context outside a simplified `Your answer` transcript surface, moves answer-level notes and `Try next` into separately owned guidance, reduces fixture annotation overlap, introduces a quiet warm-coral next-move material, and rebalances type, spacing, actions, and wrapping from 320px through dialog widths. Its compact annotation variant now leads with `What I noticed`, removes the duplicated `Coach noticed` label, reduces header height, and adds mouse-hover disclosure without changing the default canvas presentation or its click, keyboard, and touch behavior. No production route, read model, mutation, persistence, evaluator, or provider contract changed. Verification: lab lint/build, 11 focused production transcript tests, production compass-asset tests, documentation links/diff check, interaction snapshots for all five states and both Plan structures, annotated/plain/thin transcript and popover review, measured light/dark contrast, 320px and 1440px overflow checks, and zero browser console errors. Human review of visual hierarchy, the white-versus-dark spotlight action, and representative evaluator evidence remains the gate before production dashboard edits.
- **Dashboard canvas review sub-pass:** adds switchable `Diagonal confluence`, `Cool horizon`, and `Peripheral mesh` layers to the Phase 1 lab frame. `Diagonal confluence` is the current leading hypothesis; its latest calibration replaces denser alpha-over-neutral shading with explicit pale, higher-lightness powder-blue and cool-green fields. The complete field—including its center bridge and edge colors—moves one additional step lighter and lower-chroma, keeping the green recognizable beyond seafoam but softer than saturated viridian. Dark mode receives the same whole-field adjustment as a lifted low-intensity blue-green dusk with comparable color presence rather than a heavier shade. Card elevation remains unchanged. The canvas continues through a transparent dashboard header, and the role switcher and practice-queue control alone use restrained glass material for legibility. The other two studies keep their existing formulas while sharing that header treatment, and all three remain review hypotheses rather than production direction. The switcher and canvas remain overflow-free at 320px.
- **Recruiter invitation-create production UI refinement:** revives the v0.5 three-node workflow orientation as the only page-level UI above the active form while preserving the V2 invitation state machine. The full stepper and phase surfaces share one centered 56rem desktop reading column; the phase surface uses the calm 24px card contract, interview-stage choices use base surfaces at 16px, and text-entry areas use base surfaces at 12px. The retired page introduction and `Interview context` eyebrow no longer compete with the task. Current, completed, and upcoming progress are semantic as well as visual, and the narrow layout remains overflow-free. Focused component tests, docs links, zero-warning lint, browser geometry/computed-style review, and browser diagnostics pass.
- **Slice 206, post-answer coaching UI and acknowledgement tone:** restores the v0.5-shaped submit-to-feedback rhythm over the shared V2 runtime. Typed answers use the original three-step 3,000ms cadence and voice answers use the original four-step 2,500ms cadence; the loader follows only real submit/analyze state and never branches on markers or claims provider-internal stages. Coaching-ready work opens in one elevated, focus-contained modal surface with the existing acknowledgement-first stages and persisted retry/continue/finish actions. Prompt bundle V15/adapter V16 changes only acknowledgement presentation: one conversational, candidate-directed, evidence-grounded sentence that follows the code-owned posture without inventing praise or adding another finding. Ninety-one focused tests and zero-warning lint pass. Credentialed tone calibration and human mobile/desktop review remain.
- **Slice 205, shared live-session production UI:** translates `Candidate Session Mobile A.dc.html` into the rebuild branch without splitting candidate-led and invited runtime behavior. The compact sticky header, segmented progress, cutout question/composer surfaces, question audio, typed/voice lifecycle, immediate re-record discard, audience-safe exit, and exact recovery remain contract-driven. Flex-column geometry places the 14.25rem resting composer at the safe viewport bottom while retaining layout space; routine draft mutation stays silent, and voice elapsed time has no countdown or automatic three-minute stop. Typed drafts and voice transcripts are now mode-owned submission sources: switching modes cannot submit or display the inactive mode's content, and accepting either mode removes the obsolete typed draft locally and atomically from durable session state. Question wording remains wording-only. Candidate-only Hints generate automatically for the current question, while Strong response generates only after explicit request; durable claim/replay artifacts bind each result to the owned session/question/setup fingerprint and truthful loading, unavailable, and retry states. Thirty-six-pixel visible notch controls retain 44px effective targets and stay centered inside their 44px cutouts. Assistance uses semantic green/purple controls and one question-anchored elevated drawer above a fixed page interaction shield; persistent toggles close the active view or directionally switch content, and open/close is a reduced-motion-safe vertical crossfade. Purple `--accent-alt` remains local to Strong response and voice Review. Verification includes the dedicated assistance lifecycle suite, 87 wording/provider/setup tests, 65 focused session/repository tests including both cross-mode submission directions and pending-draft ordering, the voice and question-audio suites, docs links, lint, real local claim/replay/cascade evidence, and mobile composition measurements. Credentialed wording/assistance calibration and physical-device voice review remain.
- **Candidate pre-session production landing refinement:** replaces the provisional initial, follow-up, and invited landing presentation with one shared production composition while preserving route ownership, first-question audio prefetch, transition behavior, and audience boundaries. The spotlight carries round context and a stable fact rail; the immutable staged questions appear as the Question plan with measured `Show more` / `Show less`; candidate-led initial and follow-up rounds both return to the role-scoped Coach Plan, while invited practice remains isolated. Mobile keeps one stacked reading path and full-width primary-first actions. Desktop keeps the centered one-column task flow, uses the available spotlight text measure, and closes with an intrinsic right-aligned secondary-then-primary action cluster. Verification: 44 focused tests, the 683-test candidate suite, zero-warning lint, documentation links, clean layout detection, diff check, and live 390px/1440px overflow and geometry review.
- **Slice 203, PostgreSQL access hardening:** separates the migration owner from `interview_coach_runtime`, removes `PUBLIC` and Supabase Data API role access, enables RLS as a service-role containment boundary, grants table DML plus a reviewed direct-function allowlist, hardens every security-definer search path, and makes future objects private by default. A dedicated provisioning command rotates the runtime credential without printing it; deployment preflight rejects remote owner/admin connections. The full migration chain and representative candidate, recruiter, account, and AI-eval workflows pass against a fresh restricted-role database. Supabase staging cutover and dashboard-level Data API disablement remain explicit operator actions.
- **Slice 202, deployment environment preflight:** adds a secret-safe, target-aware validator for the web app, host launch, AI-eval worker, and retention process. The active `.env.example` is now intentionally narrower: it is the Azure staging handoff for candidate, invited-candidate, recruiter, and TalentArbor host-launch runtime only. `env:check:azure-staging` validates both active boundaries from one ignored operator snapshot without printing values; migration-owner, AI-eval, and local-only credentials remain in their separate operational contracts. A scoped manifest-completeness test prevents required Azure staging variables from silently drifting out of the example.
- **Slice 201, app-owned candidate access milestone:** adds a self-contained Playwright gate that always targets the named local smoke database and removes its synthetic account and product history at teardown. Four serial browser journeys prove fresh registration, explicit inert-GET verification, pre-verification denial, two-session password-reset revocation, old-password denial, new-password login, reset replay denial, new-browser durable session recovery, cross-candidate read/mutation denial, simultaneous candidate/recruiter sessions, audience-specific logout, app-cookie precedence, invalid-app-cookie fail-closed behavior, and intact host access after app logout. The integrated pass also preserves the immutable consent-receipt invariant during fixture cleanup and prevents exported `DATABASE_URL` from redirecting the destructive test boundary. Verification: 4 browser journeys, migrations/smokes `042-045`, 55 shared-auth tests, 72 lifecycle tests, 683 candidate tests, 195 recruiter/invited tests, typecheck, zero-warning lint, docs links, diff check, and optimized build. Verdict: `ready` locally; real SMTP, production DB grants, deployed token-URL redaction, and TA acceptance remain release gates. See [candidate account milestone](./05-quality/candidate-account-access-milestone.md).
- **Slice 200, candidate recovery and account operations:** adds 30-minute configurable single-use forgot/reset password links, enumeration-safe request responses, explicit expired/invalid handling, hashed reset storage, delivery invalidation, and one atomic reset transaction that changes the password, clears login lock state, consumes every outstanding reset token, and revokes all app-owned sessions without touching host launch. Candidate registration, login, verification request/consume, and reset request/consume now use DB-backed fail-closed rate buckets keyed by one-way request-origin digests; denial and delivery audit evidence is metadata-only. Candidate-only sign-out controls appear on setup/dashboard for app-account access and expose a retry state on failure. Two verified deterministic local accounts and a rollback-only recovery smoke make ownership and new-device browser proof repeatable. Verification: 70 lifecycle tests, 55 shared-auth/host-isolation tests, 681 candidate regressions, transactional Postgres recovery smoke, typecheck, lint, docs links, diff check, and optimized build. Integrated browser proof was accepted in Slice 201.
- **Slice 199, protected candidate product boundary:** `/candidate` now resolves an authenticated candidate to setup or dashboard from owned prep-context state. Setup, dashboard, candidate session, Coach Update repair, resume ingestion/selection, direct and fixed follow-up practice, next-round queue, question audio, voice transcription, answer drafts/submission/analysis, feedback actions, progress, and completion all derive identity from the shared candidate access resolver before repository ownership checks. A present invalid app-session cookie fails closed without host fallback; recruiter and invited cookies grant no candidate-owned access; invited routes remain isolated. Login returns preserve bounded candidate query state such as dashboard prep selection and session entry transitions. Route-local dev-cookie decoders and anonymous setup-start browser fallback are retired; explicit dev host fixtures still enter through the shared resolver. Verification: 55 app-auth foundation tests, 48 account-lifecycle tests, 215 protected page/mutation tests, 13 Coach Update/next-round tests, the 681-test candidate suite, typecheck, lint, diff check, and optimized build. Deterministic cross-browser proof was accepted in Slice 201.
- **Slice 198, app-owned candidate account lifecycle:** migrations `043`-`044` atomically create the shared credential, candidate role/profile binding, TA-aligned phone/ZIP profile extension, three mutable contact preferences, five immutable versioned policy/contact receipts, one hashed email-verification token, and metadata-only audit evidence. The required platform acknowledgement links and records the TalentArbor Terms, Privacy Policy, and Cookie Policy without treating policy acknowledgement as optional-cookie consent; Responsible AI and contact authorization remain separate decisions. Verification links require an explicit same-origin POST, tokens are single-use with replacement/cooldown behavior, and provider failure invalidates unusable tokens without changing enumeration-safe registration/resend responses. Candidate login requires active candidate role, verified email, and the exact app-owned profile; logout revokes and clears only `ic_candidate_app_session`. `/candidate/register`, `/candidate/login`, and `/candidate/verify-email` use the shared candidate brand/design tokens. Verification: focused lifecycle and migration checks, identity-foundation checks, recruiter-auth regression checks, transactional Postgres smoke, typecheck, lint, docs links, and optimized production build.
- **Slice 197, app-owned candidate identity foundation:** extracts generic password/session/store/runtime code from its recruiter-owned implementation into shared app auth while retaining thin compatibility exports for existing recruiter consumers. Migration `042` adds the candidate role, one-to-one `candidate_profiles.app_user_id` binding, and the `interview_coach` workspace invariant. The candidate app-session resolver requires active session/user/role/profile, gives that cookie fail-closed precedence, preserves explicit dev host fixtures, and resolves production host sessions only to unbound host profiles. Host profile lookup, refresh, and session insertion now reject app-bound profiles. Focused app/recruiter tests, typecheck, and transactional Postgres smoke pass.
- **Slice 196, canonical candidate setup UI:** translates `Candidate Setup Mobile A.dc.html` into the live route without replacing its mature state machine. The screen now uses the official calm compass avatar, shared Dashboard/New role navigation, one spotlight, compact Role/Resume/Interview details hierarchy, 3/5/7/10 count choices, a centered three-fact round summary, and in-flow start action with a same-tree desktop derivation. Paste, upload, and photo share one control primitive, present one truthful selected mode, and restore that draft-local choice without retaining raw sources. The photo workspace is unframed; the mobile dock yields on downward reading and reveals on upward/near-top movement or focus. The latest human-directed refinement replaces the provisional 70rem main-plus-rail desktop composition with the 56rem form-flow frame and a reusable scroll-aware vertical timeline. Explicit Role/JD and optional-resume continuation actions join the preserved stage-to-count and count-to-start motion chain; downstream sections remain in one semantic form, failure/review-required states do not advance, and only explicit completion gets a completed node. Resume paste now begins with one `Upload` action, exposes contact-detail removal only during processing, and then prompts explicit review/edit before acceptance. Section labels use the shared blue eyebrow pattern; selected controls use opaque primary-soft plus a tight contour; resume status and count guidance share the 2px-blue, white, elevated coach-voice surface on alternate parent panels; the neutral summary carries slight elevation; and the final action right-aligns. PDF/DOCX/photo/paste processing, PII review, draft recovery, trusted-host read-only context, invalid-focus behavior, duplicate-context choice, idempotent start, and setup-to-session behavior remain intact. Verification evidence is maintained in the [candidate production UI milestone](./05-quality/candidate-production-ui-milestone.md).
  The latest control-state pass makes selected borders transparent while preserving their one-pixel geometry, leaving the soft-primary fill and contour to carry selection. Primary actions now use the shared short neutral-first, faint-blue CTA drop shadow instead of the diffuse blue glow.
  The closing shape/surface pass establishes the global 12/16/24/32px non-pill ramp with no 28px tier, brings all three setup sections into alternate-parent/base-input parity, assigns 24px to panels and coach voice and 16px to setup selectors/summary, unifies inactive border-only hover, promotes stage option titles, moves the summary to base, and adds a primary-blue-needle surface compass variant for light/dark coach-voice contexts.
  The setup close now uses a second 32px spotlight stacked below Interview details. A `Your practice round` eyebrow introduces a four-fact glass summary ordered Resume, Stage, Recommended, and Selected; its accepted-resume label matches the pre-session landing contract. The inner desktop layout compresses the summary into the left column and places the reusable opaque-white, brand-blue `.on-color-action` Start practice control in the right, while mobile stacks both regions.
  Editing accepted processed resume text returns the coach-voice rail to explicit review, removes resume inclusion from the round summary, and keeps practice blocked. `Use this resume` creates a new immutable artifact version and advances the active setup selection; edits changed by current privacy processing require another review, and unconfirmed text is not stored in the browser draft.
- **Slice 194, autonomous candidate production UI:** integrates the lean global design foundation across candidate setup, initial/follow-up landing, live practice, and dashboard; adds shared candidate branding, route loading/error boundaries, deterministic required-field and question-change focus, mutation locks, responsive composition, and the canonical question-preparedness surface. Malformed session analysis projections are dropped before dashboard derivation; a separately validated accepted evaluator run may still provide preparedness evidence, otherwise the question remains evaluation-unavailable. Verification: 676 candidate tests, deterministic three-journey browser smoke, typecheck, lint, optimized build, documentation links, desktop/mobile screenshots, axe checks, and overflow/focus evidence. Verdict: local milestone pass; see [candidate production UI milestone](./05-quality/candidate-production-ui-milestone.md).
- **Documentation and autonomous-operations gate before Slice 194:** promotes the canonical V2 stack to `docs/`, establishes an explicit authority index and historical archive boundary, retires duplicate pre-cadence handoffs, removes raw staging exports from the tracked current tree, adds documentation link verification, and defines a lead-agent multi-slice milestone contract with escalation and evidence gates. This changes development operations, not product behavior.
- **Slice 193, global production design foundation:** installs the maturation package's lean token contract as the one app-wide system. Active `--candidate-*` tokens and the `.candidate-design-system` wrapper are removed; runtime token colors and Tailwind consumers use RGB triplets; the canonical reference load order now includes roles and spacing; and provisional-only roles are isolated in `legacy-compat.css` with a consumer-driven removal rule. The design reference is canonical over the package gap log. Production surface composition remains Slice 194.
- **Slice 192, question preparedness and stable progress:** V14/adapter V15 replaces weakest-criterion intervention with a deterministic question result derived from the complete universal criterion set. It keeps technical `not_assessed` neutral, applies availability and trusted-contradiction caps, supports span/whole-answer/absence category evidence, and preserves highest-earned canonical-question progress while retaining latest-attempt state. Verification: 153 evaluator-configuration tests, 675 candidate tests, 103 AI-eval workbench tests, typecheck, lint, production build, and the 40-case non-provider database scenario/retention smoke. Credentialed validation was intentionally deferred until production UI and live candidate runthroughs.
- **Slice 191, completion-policy diagnostic:** V13/V7 made intervention code-owned and proved that strong/clear/emerging posture could be enforced across feedback and Coach Update. Subsequent product review found weakest-criterion selection too prescriptive; Slice 192 supersedes that completion rule while preserving its generated-language safety and Coach Update framing protections.
- **Slice 190, technical-language repair proof:** the V12/V6 focused credentialed run completed 13/13 expanded cases and 24/24 live provider operations on their first attempt. It proved the bounded generated-language repair and technical-boundary availability work, then surfaced the remaining model-owned intervention drift that Slice 191 addresses. See [scenario evidence](./05-quality/ai-eval-scenario-lab-milestone.md).
- **Slice 189, technical-framing release boundary:** V2 question wording now prefers practical role capability, application, judgment, verification, and learning over unsupported factual recall. V10 separates answer-demonstrated `role_skill_signal` from optional reference-grounded `technicalAccuracy`; the 40-case V4 scenario baseline adds an eight-role no-reference matrix while preserving supported/contradicted controls. Focused suites, the 666-test candidate suite, typecheck, lint, production build, and the 40-case database scenario smoke pass. Credentialed V2/V10 calibration remains Slice 190.
- **Slice 188, worker and retention operations:** the browser-never-provider boundary now has a long-running worker service with bounded policy, health/readiness, graceful shutdown, and metadata-only events. Migration `041` adds idempotent dry-run/apply cleanup for expired terminal scenario artifacts, function-owned cascade authorization, active-run protection, and immutable count receipts. Local tests and database smokes are accepted; no deployed supervisor, schedule, role grant, alert sink, or retention-period approval is claimed. See [worker/retention operations](./07-ops/ai-eval-worker-and-retention.md).
- **Slice 187, accepted V9 calibration:** a 13-case targeted run and fresh 32-case baseline completed without provider/runtime failure. The full run exposed one contradicted-technical-evidence appraisal defect; the deterministic fix, focused regression, and credentialed case confirmation are accepted. See [scenario evidence](./05-quality/ai-eval-scenario-lab-milestone.md).
- **Slices 179-186, AI-eval operations:** individually granted exact-output review, findings/remediation/rechecks, 32-case scenario lab, gated worker, recovery/cost controls, and accepted representative live calibration. Verdict: `conditional`; see [operator evidence](./05-quality/ai-eval-operator-milestone.md) and [scenario evidence](./05-quality/ai-eval-scenario-lab-milestone.md).
- **Slices 165-178, voice and resume:** shared transcript-first voice answers plus bounded pasted/trusted-host/PDF/DOCX/photo resume processing, review, recovery, PII scrubbing, and multi-instance operations controls. See [voice evidence](./05-quality/transcript-first-voice-answer-milestone.md) and [resume evidence](./05-quality/resume-ingestion-milestone.md).
- **Slices 147-164, standalone recruiter and invited practice:** app-owned recruiter auth/create/delivery/dashboard/settings, clean invite entry, shared runtime, completion/repeat, recruiter transcript boundary, and credentialed SMTP gate. See [recruiter evidence](./05-quality/recruiter-standalone-flow-milestone.md).
- **Slices 140-146, candidate production demonstration:** production-shaped session/dashboard/Coach Update surfaces over the evidence-first engine plus local hardening and browser journeys. See [candidate milestone](./05-quality/candidate-led-production-demonstration-milestone.md).
- **Slices 125-139, host/question/follow-up hardening:** one-time TA launch, direct MSSQL context, trusted setup staging, production question wording/Coach Update, exact idempotency, baseline plan, and immutable follow-up intent/session launch.
- **Slices 1-124:** cleanroom foundation through the first production evaluator and evidence-first dashboard. Use the dated archive snapshots for numbered detail.

## Operating Rules And Archive

- V1 and the original refactor pack are behavior/architecture references, not default code donors. Name conflicts with current V2 before implementation.
- Product intent, user safety, durable invariants, and current repository evidence outrank a stale slice plan.
- Use [Documentation Authority](./README.md) to choose active sources. Do not broad-scan the historical archive for implementation direction.
- Before removing a path, search callers and classify it as `keep as transition`, `remove now`, or `mark for retirement`.
- Update this handoff only with changed current truth, phase movement, next work, risks, and a short milestone range. Put detailed evidence in the governing contract or dated archive.
- Full pre-compression state through Slice 186: [2026-07-23 snapshot](./reference-archive/candidate-interim/handoff-pre-compaction-2026-07-23.md).
- Earlier snapshots: [2026-07-18](./reference-archive/candidate-interim/handoff-pre-compaction-2026-07-18.md) and [2026-07-12](./reference-archive/candidate-interim/handoff-pre-compaction-2026-07-12.md).
- Historical V1/interim contracts and SQL: [Reference Archive](./reference-archive/README.md).
