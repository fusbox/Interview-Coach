# Postgres Migration Integration Checklist

## Purpose

Use this checklist to confirm what **"migrate from Supabase to company Postgres"** means operationally, what evidence proves the migrated app works, and which gaps must be resolved before the app can be called ready.

The **"Current app"** column models the kind of answer needed from the integration team. It preserves prior-state context where useful, but the current migration branch runtime is Postgres-backed and no longer depends on Supabase for app execution.

## How To Use

- Fill the **"Integration team response"** column with answers from the integration, infra, and auth owners.
- Treat unanswered auth, deployment, and DB privilege items as blockers for company-environment deployment, not local proof of Supabase removal.
- Keep database migration and auth migration separate in estimates, even if the final deployment must ship with both complete.
- Do not switch the current dirty AI-quality worktree to the Postgres branch. Use a separate worktree before implementation starts.

## Current Phase-1 Assumption

As currently understood, phase 1 is still a standalone app hosted at `interviewcoach.talentarbor.com`, but internal users are expected to launch it from the company's ATS via a button. The ATS or an enterprise identity service such as Okta is expected to pass authenticated internal-user identity into Interview Coach. That identity handoff is outside this migration branch's implementation scope, but it changes the target UX: internal users should land on the create-invite screen, and standalone login/create-account/settings-profile/logout UI should become transitional or removable once the company identity handoff exists.

The app-owned Postgres auth implementation in this branch remains useful as a local/UAT validation bridge and as proof that Supabase Auth can be removed. It should not be treated as the final enterprise identity design unless the integration team explicitly chooses it.

## Working Decisions And Assumptions

These are the current working answers as of May 4, 2026. They are safe to use for planning and implementation unless contradicted by the integration team.

| Topic | Working answer | Implementation impact |
| --- | --- | --- |
| Supabase scope | Treat phase 1 as full Supabase replacement, not database-only replacement. | Replace Supabase data access and Supabase Auth/session behavior. Do not plan to keep Supabase Auth as the steady-state phase-1 answer. |
| Recruiter/internal auth | Target deployment should use ATS-launched enterprise identity handoff, potentially Okta or an equivalent internal service. | App-owned Postgres auth remains a local/UAT bridge and Supabase-removal proof. Final production should not depend on recruiter self-signup, password reset, or profile-managed identity fields unless the integration team chooses that fallback. |
| Candidate access | Keep candidate access token-link based for phase 1. | Preserve `/s/[token]` style entry and `x-candidate-token` API protection. Port token storage/validation to Postgres. |
| Historical data | Fresh target DB is acceptable. Existing real sessions remain in the current Supabase project if needed later. | No bulk historical data migration is required for phase 1. Focus on schema creation and new-record validation. |
| SQL functions/procedures | Proceed assuming target Postgres allows SQL functions/stored-procedure-style logic. | Port existing RPC-style behavior into SQL functions where that remains the cleanest implementation. This is now a working assumption based on user confirmation that stored procedures are used in the company DB environment. |
| DB env format | `DATABASE_URL` vs `POSTGRES_*` remains open. | Implement config that accepts both and normalizes internally, with `DATABASE_URL` preferred when present. |
| App DB user | Final user/privilege model remains open. | Code can proceed against env-provided credentials, but production should use a least-privilege app user rather than the generic `postgres` user. |
| Target host | Working host is `interviewcoach.talentarbor.com`. | Use that as the production-origin assumption. Expect staging branch, staging URL, or UAT domain to be confirmed separately. |
| Logs/DB inspection | Still open. | Not a coding blocker, but it is a deployment-readiness blocker. Someone must be able to inspect logs and confirm DB writes. |
| Acceptance ownership | After migration/auth code is ready, integration team should validate in the company deployment environment, then QA should run product validation. | We should provide automated tests plus a smoke/acceptance runbook; integration and QA own environment-level signoff. |

## Progress Notes

| Date | Status | Note |
| --- | --- | --- |
| 2026-05-06 | Done | Removed the active Supabase runtime fallback. Runtime code no longer imports Supabase helpers/packages, repository and operational-store factories are Postgres-only, current-user/middleware auth uses the app-session bridge, Supabase `User` type imports were replaced with app-owned user types, Supabase env vars were removed from `.env.example`, and `@supabase/ssr` / `@supabase/supabase-js` were removed from package dependencies. Verification: runtime Supabase scan over `src`, package files, and `.env.example` returned no active references; `npx tsc --noEmit` passed; focused auth/repository/operational-store/API tests passed. |
| 2026-05-06 | Done | Added repeatable local Postgres smoke utilities and runbook. `npm run postgres:smoke:start` creates/starts a disposable known-credential Docker Postgres container, `npm run db:apply-schema` applies the neutral schema, and `npm run db:smoke-schema` runs the rollback schema smoke. The smoke DB started successfully, schema apply passed, rollback smoke passed, and `fu@rangam.com` was provisioned into the smoke DB with recruiter/admin/QA roles. A local HTTP smoke on port `3100` also passed: `/api/auth/login`, `/recruiter`, `/qa/ai-quality`, and `/admin/feedback` all returned `200` using the Postgres app-auth session. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Added `npm run postgres:smoke:product` as a repeatable Level 2 product-flow smoke. Against the disposable Postgres DB and a local Next server on `3100`, it logged in as `fu@rangam.com`, created an invite batch, opened `/s/[token]`, fetched the candidate session, submitted initials, started practice, saved a draft, submitted one answer, ran mock-backed answer analysis, and verified Postgres rows for invite tracking, sessions, questions, answers, eval results, candidate tokens, idempotency, rate limits, metrics, and `ai_generations`. Email-send validation remains separate because SMTP provider acceptance is required before `/api/invite/send` marks invitations sent. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Added `npm run postgres:smoke:ai` as a repeatable Level 3 AI/QA smoke. Against the disposable Postgres DB and a local Next server on `3100` with a real `GEMINI_API_KEY`, it generated recruiter questions, created a Postgres-backed invite, opened the candidate link, generated hints and a strong response, submitted/analyzed one answer, completed the session to generate a debrief, verified successful `gemini` / `gemini-2.5-flash` `ai_generations` rows for all five AI surfaces, and confirmed `/qa/ai-quality` plus JSON export returned `200`. This also removed direct `SupabaseSessionRepository` use from the tips and strong-response routes. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Added and ran `npm run postgres:smoke:email` as a repeatable Level 4 real SMTP smoke. Against the disposable Postgres DB and local Next server on `3100`, Office365 SMTP accepted the initial invite email and session-complete debrief email to the intentional smoke recipient. The script verified the provider message id, `sessions.invitation_sent_at`, `summary_narrative`, `intake_json.summary_expires_at`, and successful invite/session-completion metrics in Postgres. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Ran Level 5 browser-visible recruiter smoke. Login, protected recruiter shell, manual create-invite steps, preview modal, visible send/delivered state, dashboard visibility, and DB verification all passed against the disposable Postgres DB. The pass found stale Resend-vendor footer copy in the preview modal and two small browser warnings; vendor copy, password autocomplete metadata, and logo aspect-ratio styles were cleaned up. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Ran Level 6 browser-visible candidate practice smoke. Candidate invite link, initials gate, welcome/readiness, hints, strong example response, text-mode answers, answer feedback for three questions, final debrief summary, and DB verification all passed against the disposable Postgres DB with a real `GEMINI_API_KEY`. The pass found that `/api/tts` requires `GEMINI_API_KEY`, that automated browsers should use text mode when microphone permission is blocked, and that the no-SMTP summary screen should not claim a debrief email was sent. Summary copy and candidate logo dimensions were cleaned up. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Fixed and validated chained practice-again attempts. Session start now persists each newly issued candidate token back onto the session as encrypted invite-token metadata, so attempt 2 can create attempt 3 and debrief email links can point at the current attempt token. Added `npm run postgres:smoke:practice-again`; it created a Postgres-backed invite, created attempt 2 from attempt 1, created attempt 3 from attempt 2, fetched attempts 2 and 3 with their own tokens, and verified active candidate-token rows plus repeat-attempt invite-token metadata in Postgres. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Added and ran `npm run postgres:smoke:profile-settings` as a repeatable Level 8 profile/settings smoke. Against the disposable Postgres DB and local Next server on `3100`, it logged in with app-owned auth, loaded `/recruiter/settings`, fetched `/api/recruiter/profile`, saved a profile update, refetched it, verified the `recruiter_profiles` row in Postgres, and restored the original smoke-user profile. Added focused tests for the Postgres profile helper and invalid profile API payloads. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Added and ran `npm run postgres:smoke:resend-retry` as a repeatable Level 9 invite resend/retry smoke. Against the disposable Postgres DB and local Next server on `3100`, it logged in with app-owned auth, created a Postgres-backed invite, resent the invite through Office365 SMTP, verified `invitation_sent_at` and resend metrics, seeded a failed retryable batch, retried it through the route stack, and verified parent/child batch state, child session creation, and completed retry idempotency rows in Postgres. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Added and ran `npm run postgres:smoke:recruiter-review` as a repeatable Level 10 recruiter review smoke. Against the disposable Postgres DB and local Next server on `3100`, it logged in with app-owned auth, created a Postgres-backed invite/session, submitted a candidate answer, opened `/recruiter/sessions/[id]` as the owning recruiter, verified the visible review contract, and confirmed the page does not expose candidate AI feedback pulse labels. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Added and ran `npm run postgres:smoke:negative-permissions` as a repeatable Level 11 negative-permissions smoke. Against the disposable Postgres DB and local Next server on `3100`, it provisioned a recruiter-only second user, verified missing/mismatched candidate tokens return `401`/`403`, verified cross-recruiter review/resend denial, verified admin/QA page redirects and QA export `403`, and confirmed the smoke DB ownership/role state. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | User completed comprehensive manual validation of the Postgres-migrated local app: recruiter create/send invite, resend, dashboard/session details review; admin user-feedback review; QA AI-quality review; candidate session completion; and two additional practice-again attempts. Result reported: everything worked perfectly. See [local_postgres_smoke.md](./local_postgres_smoke.md). |
| 2026-05-06 | Done | Added operator/developer app-user provisioning for the Postgres auth model. `npm run auth:provision-user` creates or updates `app_users`, scrypt password credentials, DB roles, recruiter profile data, and a provisioning audit event in one transaction. Documented the runbook at [app_user_provisioning.md](./app_user_provisioning.md). Syntax/help checks, typecheck, and lint passed. |
| 2026-05-06 | Done | Added app-auth middleware protected-page handling for `APP_AUTH_BACKEND=postgres`. Recruiter, admin, and QA pages now redirect unauthenticated browser requests to `/login?next=...` when no app session cookie is present, while public candidate pages remain open and E2E auth cookies still work. Also split edge-safe auth/session config out of heavier app-auth modules so middleware does not import Postgres/password/session internals. Focused middleware/auth tests, typecheck, and lint passed; `next build` compiled but stopped later because local `GEMINI_API_KEY` is not set. |
| 2026-05-05 | Done | Removed the direct server-page `recruiter_profiles` Supabase reads from the recruiter dashboard and admin feedback page. Both pages now resolve profile/timezone data through `getRecruiterProfileRecord()`, which reads Postgres in app-auth mode and keeps Supabase only as migration fallback. Typecheck and lint passed. |
| 2026-05-05 | Done | Added `getAuthenticatedRouteUser()` as the shared server route auth seam and moved the direct API-route Supabase auth checks for question generation, invite create/send/resend/retry, ops metrics, and recruiter profile onto it. These routes now authenticate through `getCachedUser()`, which can resolve app sessions when `APP_AUTH_BACKEND=postgres` and Supabase users during fallback. Typecheck, lint, and focused route/helper tests passed. |
| 2026-05-05 | Done | Removed the remaining browser-side Supabase profile/current-user calls from recruiter settings, recruiter invite creation, and ProfileGuard. Added `/api/recruiter/profile` as the app-auth-compatible profile contract, backed by Postgres when `APP_AUTH_BACKEND=postgres` and Supabase during migration fallback. ProfileGuard now runs under app auth instead of being skipped. Typecheck and focused auth/profile tests passed. |
| 2026-05-05 | Done | Wired first app-owned auth runtime path. Added `/api/auth/login` and `/api/auth/logout`, set/clear the HTTP-only app session cookie, taught `getCachedUser()` and middleware to use app sessions when `APP_AUTH_BACKEND=postgres`, switched login plus desktop/mobile logout away from browser Supabase auth, and added a server profile loader that reads Postgres in app-auth mode. Focused auth route tests, auth/RBAC tests, and typecheck passed. |
| 2026-05-05 | Done | Started app-owned recruiter auth foundation. Added app-user type, scrypt password hashing/verification, opaque hashed app-session tokens, Postgres app-auth store primitives, password-login orchestration, session lookup/revocation helpers, audit-event writes, and DB-role-aware RBAC. Focused auth/RBAC tests and typecheck passed. Later progress notes cover login/logout UI wiring and middleware completion for the local/UAT bridge. |
| 2026-05-05 | Done | Ported the AI-quality generation capture workstream into `feature/postgres-integration`. `AI_GENERATION_REPOSITORY_BACKEND=postgres` now supports capture writes plus `/qa/ai-quality` reads, filtered pagination, aggregate summary widgets, and JSON/CSV export through Postgres-backed repositories. Focused AI-quality tests, answer submit/analysis route tests, typecheck, and disposable Docker schema smoke passed. |
| 2026-05-05 | Done | Added Postgres feedback repository and provider seam. `FEEDBACK_REPOSITORY_BACKEND=postgres` now captures candidate/recruiter feedback, preserves session/type update behavior when session context exists, supports recruiter-only feedback inserts, and returns the admin feedback view with session role/intake context. Typecheck, focused factory/UI tests, and disposable Docker Postgres integration tests passed. |
| 2026-05-05 | Done | Added Postgres template repository and provider seam. `TEMPLATE_REPOSITORY_BACKEND=postgres` now preserves recruiter-owned templates, shared-template visibility, template create/update/delete, and explicit admin manage-all behavior; template server actions now use the repository factory. Typecheck, focused factory/UI tests, and disposable Docker Postgres integration tests passed. |
| 2026-05-05 | Done | Added Postgres session repository and repository factory. `SESSION_REPOSITORY_BACKEND=postgres` now supports session create/read/update/delete, dashboard summaries, draft saves, answer/eval writes, analysis deletion, summary expiry, invitation sent timestamps, and atomic engagement increments through the neutral Postgres function. Shared candidate/recruiter/session call sites now go through the factory; focused unit tests, route/application tests, typecheck, and disposable Docker Postgres integration tests passed. |
| 2026-05-05 | Done | Added Postgres candidate-token backend. `CANDIDATE_TOKEN_BACKEND=postgres` now issues hashed candidate tokens and validates `x-candidate-token` against active session-bound rows; focused unit tests and disposable Docker Postgres integration tests passed, including expired/revoked rejection. |
| 2026-05-05 | Done | Added Postgres durable metrics backend. `METRICS_BACKEND=postgres` now writes counter/timing rollups and reads operational snapshots/SLO summaries through the neutral metrics SQL functions; focused unit tests and disposable Docker Postgres integration tests passed. |
| 2026-05-05 | Done | Added Postgres durable rate-limit backend. `RATE_LIMIT_BACKEND=postgres` now uses the neutral `consume_rate_limit_bucket()` SQL function; focused unit tests and disposable Docker Postgres integration tests passed, including concurrent consumption. |
| 2026-05-05 | Done | Added idempotency backend seam and Postgres implementation. `beginIdempotentRequest`, `completeIdempotentRequest`, and `releaseIdempotentRequest` use the Postgres implementation in the migrated runtime; focused unit tests and disposable Docker Postgres integration tests passed. |
| 2026-05-05 | Done | Added invite repository factory/provider seam. Invite create, retry, and candidate token lookup now use `createInviteRepository()` and the migrated runtime uses the Postgres implementation. |
| 2026-05-05 | Done | Added first Postgres invite repository implementation and optional Docker-backed integration test for tracked invite creation, token lookup, failure tracking, and retry marking. Later smokes validated the invite route stack with `INVITE_REPOSITORY_BACKEND=postgres`. |
| 2026-05-05 | Done | Validated `db/migrations/001_initial_schema.sql` against disposable Docker Postgres `interviewcoach-postgres-test` (`ankane/pgvector:latest`, PostgreSQL 15.4). Migration applied successfully, reran idempotently, and rollback-only smoke validation passed. |
| 2026-05-05 | Done | Investigated ChatArbor local Postgres setup as a validation option. Recommendation: reuse the `ankane/pgvector` image pattern in a new disposable Interview Coach container/volume, not the existing `chatarbor-postgres` container with ChatArbor data. |
| 2026-05-05 | Done | Drafted neutral executable initial schema at `db/migrations/001_initial_schema.sql`. It intentionally replaces Supabase Auth/RLS with app-owned auth tables and server-side authorization assumptions. |
| 2026-05-05 | Done | Added target schema reconciliation plan at [target_schema_reconciliation.md](./target_schema_reconciliation.md), using [db_schema.md](./db_schema.md), repo schema/migrations, and touchpoint inventory as source inputs. |
| 2026-05-05 | Done | Began Phase 1 implementation with a server-only Postgres foundation: env/config parsing, pooled `pg` client, and health-check helper. Later route-stack and browser-visible smokes validated repository adoption under Postgres selectors. |
| 2026-05-04 | Done | Created a clean separate worktree at `C:\tmp\Interview-Coach-Recruiter-postgres` on `feature/postgres-integration`, tracking `azure/feature/postgres-integration`. |
| 2026-05-04 | Done | Confirmed the original `C:\dev\Interview-Coach-Recruiter` worktree remains reserved for AI-quality staged work and session-recovery unstaged work. |
| 2026-05-04 | Done | Copied `integration_checklist.md` and `previous_chat.md` into this Postgres migration branch under `docs/04-architecture/postgres-migration/`. |
| 2026-05-04 | Done | Set phase-1 working scope to full Supabase replacement, fresh target DB, candidate token-link access preserved, SQL functions/procedures allowed, and app-owned email/password auth backed by Postgres. |
| 2026-05-04 | Done | Created the Supabase touchpoint inventory at [supabase_touchpoint_inventory.md](./supabase_touchpoint_inventory.md). |
| 2026-05-04 | In Progress | Created target runtime facts artifact at [target_runtime_facts.md](./target_runtime_facts.md). Several deployment/integration facts remain open. |
| 2026-05-04 | Open | Deployment/log access, DB inspection path, staging/UAT URL, final Postgres env contract, and final production DB user remain integration-team confirmations. |

## Scope And Ownership

| Status | To confirm | Current app | Post-migration clarification | Fu's notes | Integration team response |
| --- | --- | --- | --- | --- | --- |
| [ ] | Is this database-only replacement, or full backend replacement? | Uses Supabase for Postgres data, auth, SSR cookies, browser login/logout, recruiter identity, service-role/admin access, RLS-oriented policies, and RPC functions. A DB connection string alone does not replace all of that. | Working assumption: full Supabase replacement for phase 1, including data access and auth/session replacement. | Since we don't use Supabase, will proceed as if we need full Supabase replacement | [Your response here]
| [ ] | Is historical Supabase data being migrated, or is this a fresh environment? | Current app stores real app records in Supabase tables. | Working assumption: fresh target DB, no bulk historical migration. Current Supabase records remain available in Fu's project if needed later. | May be out of int. team's purview. Current records will be retained/migrated as needed. | [Your response here]
| [ ] | Who owns the target database? | Supabase project ownership. App code assumes tables, functions, and policies already exist. | DBA/infra owner named, with who applies DDL, who grants privileges, and who confirms production readiness. Not a blocker for local code planning; blocker for deployment/cutover. | Is this a blocker if not provided? | [Your response here]
| [ ] | Who owns auth replacement? | Supabase email/password and Supabase SSR session cookies. Candidate flow is invite-token based. | Updated target: integration/identity owners should provide the ATS/enterprise identity handoff for internal users. This branch's Postgres app-auth path is a local/UAT bridge and fallback, not the preferred final auth policy. Candidate access remains token-link based. | Internal users will launch from ATS; identity/auth passed through by service such as Okta. | [Your response here]
| [ ] | Who owns deployment and runtime logs? | Vercel/Supabase visibility in the existing setup. I don't have access to the company deployment/backend. | Deployment owner, log location, escalation path, and whether user can access logs directly. Not a blocker for coding; blocker for environment validation. | Is this a blocker if not provided? | [Your response here]
| [ ] | Which branch is the migration branch? | Azure remote branch `feature/postgres-integration` exists, but it should be inspected in a separate worktree because the current worktree has staged AI-quality work and unstaged session-recovery work. | Confirm branch name and whether work should begin from that branch, current `dev-Fu`, or a new feature branch. | I'll proceed as if this is true. | [Your response here]

## Deployment And Runtime Environment

| Status | To confirm | Current app | Post-migration clarification | Fu's notes | Integration team response |
| --- | --- | --- | --- | --- | --- |
| [ ] | Target deployment URL where migrated app will run | Current local dev runs through Next. Current known deployed app has been Vercel-hosted. Public URL generation uses `NEXT_PUBLIC_APP_URL` with `NEXT_PUBLIC_BASE_URL` fallback. | Current phase-1 assumption: `https://interviewcoach.talentarbor.com`. Confirm whether there is also a staging URL and whether the TBD candidate-led app shares this host. | Will proceed assuming candidate-led app will share this host. | [Your response here]
| [ ] | Canonical public origin for invite links and email links | Current code requires explicit public origin in production for invite/debrief links. | Confirm value for `NEXT_PUBLIC_APP_URL` or successor env name. | Will proceed using current invite link shape: https://[hostname]/s/[token]. | [Your response here]
| [ ] | Deployment platform | Next.js deployed on Vercel. | Confirm whether target is Azure App Service, container, internal hosting, or other. Include build/start command and Node version. | npm | [Your response here]
| [ ] | Runtime environment variables and secret store | Current app needs Supabase env vars, `GEMINI_API_KEY`, SMTP vars, metrics/rate-limit backend config, and public URL config. | Confirm where secrets are stored, who can update them, and how values are promoted between dev/stage/prod. | See .env.example | [Your response here]
| [ ] | Postgres connection format | `pg` is installed and the server Postgres config/pool now supports `DATABASE_URL` or individual `POSTGRES_*` values. | Working approach: support both formats and normalize internally, preferring `DATABASE_URL` when present. | Proceed with flexible config until final env contract is answered. | [Your response here]
| [ ] | Network access from app runtime to DB | Supabase access is outbound HTTPS through Supabase clients. Plain Postgres requires TCP access from runtime to DB. | Confirm firewall, VPC/VNet, IP allowlist, SSL/TLS requirement, and whether local developer access is allowed. Not a blocker for local code; blocker for target deployment validation. | Is this a blocker if not provided? | [Your response here]
| [ ] | Logs for app/API/backend failures | Validation can use local terminal, Vercel logs, and Supabase table inspection. | Provide exact log viewer, required access, correlation/trace lookup method, and who can retrieve logs if user lacks access. Not a blocker for local code; blocker for target deployment validation. | Is this a blocker if not provided? | [Your response here]
| [ ] | Rollback plan | Current production rollback is tied to existing deployed app and Supabase-backed runtime. | Define rollback trigger, rollback owner, prior deployment target, DB rollback approach, and data-loss expectations. Not a blocker for local code; blocker before production release. | Is this a blocker if not provided? | [Your response here]

## Database Schema, Migrations, And Privileges

| Status | To confirm | Current app | Post-migration clarification | Fu's notes | Integration team response |
| --- | --- | --- | --- | --- | --- |
| [ ] | Can target DB accept the app schema? | Schema starts in `supabase/schema.sql` and continues through migrations in `supabase/migrations`. | Confirm DDL can be applied, including enums, tables, indexes, constraints, triggers, and SQL functions. Not a blocker for drafting DDL; blocker for applying to target DB. | Is this a blocker if not provided? | [Your response here]
| [x] | Are SQL functions/procedures allowed? | Calls RPC/functions for invite batch creation, engagement increment, rate limits, metrics rollups, SLO summaries, and AI-generation summary. | Working assumption confirmed by user: the company DB can accept/run queries/functions and stored procedures are used. Validate exact permissions in target DB later. | Proceeding with SQL functions/stored-procedure-style logic. | [Your response here]
| [ ] | Which DB user should the app use? | Supabase service-role currently acts as privileged server access. Provided `POSTGRES_USER=postgres` may be placeholder or admin-level. | Working approach: code accepts env credentials; production should use a least-privilege app user. `postgres` can be treated as temporary/dev unless integration team confirms otherwise. | Proceed with flexible config until final DB user contract is answered. | [Your response here]
| [ ] | Does target DB need RLS? | Supabase schema enables RLS and policies in several tables, but server code often uses service-role/admin access. | Working approach: move authorization into server application code and do not depend on Supabase RLS semantics. Preserve DB constraints; recreate RLS only if company DB policy requires it. | | [Your response here]
| [ ] | Tables to recreate | Current tables include `sessions`, `questions`, `answers`, `eval_results`, `candidate_tokens`, `events`, `projection_session_now`, `recruiter_profiles`, `recruiter_templates`, metrics rollups, rate-limit buckets, invite batch tables, idempotency keys, and `ai_generations`. | DDL applied and verified with table/index/function inventory. | | [Your response here]
| [ ] | Required migrations | Migrations include recruiter profiles/templates, session lineage/readiness, paused status, idempotency, engagement increment, metrics, rate limits, invite batches, and AI-quality generation capture. | List which migrations apply to target DB and which are superseded by cleaned-up schema. | | [Your response here]
| [ ] | Seed/test data | Creates data through recruiter invite creation and candidate practice flows. App-auth users can now be provisioned with `npm run auth:provision-user`; see [app_user_provisioning.md](./app_user_provisioning.md). | Confirm whether broader product seed data will be provided or whether validation should create records through the app after provisioning test users. | | [Your response here]
| [ ] | DB inspection path | I can inspect Supabase tables directly. Target DB access is not available yet. | Provide SQL console, read-only DB credentials, approved query runner, or named person who confirms writes. | | [Your response here]
| [ ] | Backup and restore | Supabase provides managed backup/restore behavior outside the app code. | Confirm backup cadence, point-in-time recovery, restore owner, and whether staging/prod backups can be tested. | | [Your response here]

## Data Access Replacement

| Status | To confirm | Current app | Post-migration clarification | Fu's notes | Integration team response |
| --- | --- | --- | --- | --- | --- |
| [x] | Server Postgres client layer | Migrated runtime uses a shared server-only `pg` layer for app persistence. | Includes pooled DB access, query helpers, timeout support, and `DATABASE_URL`/split `POSTGRES_*` config. | Implemented and validated locally; target DB network/SSL facts remain open. | [Your response here]
| [x] | Session repository | Prior `SupabaseSessionRepository` handled session reads/writes, answers, eval results, question updates, debrief state, and engagement increment RPC. | Runtime now uses the Postgres session repository and neutral SQL functions. | Validated through focused tests, route-stack smokes, browser smokes, and manual validation. | [Your response here]
| [x] | Invite repository | Prior `SupabaseInviteRepository` created sessions/questions/tokens, invite batches, candidate tracking, retries, and batch lookup. | Runtime now uses the Postgres invite repository, including transactional batch creation/send tracking. | Validated through invite create/send/resend/retry smokes and manual validation. | [Your response here]
| [x] | Template repository | Prior template repository relied on Supabase current user, RLS, and optional service-role/admin access. | Runtime now uses explicit Postgres scope: `userId` plus `canManageAllTemplates`. | Implemented and validated with focused tests plus disposable Docker Postgres integration test. | [Your response here]
| [x] | Feedback repository | Prior feedback repository wrote through Supabase and exposed admin feedback by joining sessions. | Runtime now uses Postgres feedback repository and Postgres profile/timezone helpers. | Implemented and validated with focused tests, admin smoke, and manual validation. | [Your response here]
| [x] | AI-quality repository | AI-quality capture writes and reads `ai_generations`. | Runtime now uses Postgres-backed write/read/export/summary queries for `/qa/ai-quality`. | Focused AI-quality tests passed; Level 3 AI smoke verified all five surfaces and QA export. | [Your response here]
| [x] | Metrics backend | Durable metrics backend is Postgres in production; local/test can use memory. | Postgres backend preserves counter/timing rollup writes, operational snapshot reads, and SLO summary reads through neutral SQL functions. | Implemented and validated with focused tests plus disposable Docker Postgres integration test. | [Your response here]
| [x] | Rate-limit backend | Production rate limiting expects a durable backend. | Postgres backend preserves atomic `consume_rate_limit_bucket()` behavior; memory is rejected in production. | Implemented and validated with focused tests plus disposable Docker Postgres integration test, including concurrent requests. | [Your response here]
| [x] | Idempotency store | Prior idempotency path used Supabase service-role access. | Runtime now uses Postgres `api_idempotency_keys` behavior for reserve, pending duplicate, conflict, complete/replay, and pending release. | Implemented and validated with focused tests plus route-stack smokes. | [Your response here]
| [x] | Candidate token store | Candidate links use app-issued tokens. | Runtime now issues raw random tokens, stores hashes at rest, validates session binding, and rejects revoked/expired rows in Postgres. | Implemented and validated with focused tests, candidate smokes, and negative-permission smoke. | [Your response here]

## Authentication, Authorization, And Identity

| Status | To confirm | Current app | Post-migration clarification | Fu's notes | Integration team response |
| --- | --- | --- | --- | --- | --- |
| [x] | Recruiter login method | `/login` posts email/password credentials to `/api/auth/login` for the app-owned local/UAT bridge. | Target deployment should bypass standalone login for internal users: ATS button launches Interview Coach with a trusted identity handoff and lands users on `/recruiter/create`. | Internal users launch from ATS, not login/create account. | [Your response here]
| [x] | Auth callback/session exchange | Supabase auth-code exchange has been removed. App-auth login creates an app session directly after credential verification. | Final target needs an enterprise identity handoff endpoint/session exchange contract instead of password reset/email verification expansion. | | [Your response here]
| [x] | Middleware/session refresh | `src/middleware.ts` delegates to app-auth middleware, which performs lightweight protected-page cookie checks and redirects missing-session recruiter/admin/QA page requests to login. | Authoritative DB-backed auth remains in layouts/route handlers; middleware is a lightweight browser guard. | | [Your response here]
| [x] | Current-user helper | `getCachedUser()` returns an app-session user or an E2E test user. API routes that need a required user go through `getAuthenticatedRouteUser()` for shared auth-denial metrics. | Future ATS/Okta handoff can establish the same app-session shape or replace this helper with an enterprise identity adapter. | | [Your response here]
| [x] | Browser-side Supabase usage | Login, desktop logout, mobile dock logout, settings, recruiter create, and ProfileGuard now use app API routes instead of browser Supabase clients. | Remaining Supabase auth work is server-side route/helper migration, not browser-client cleanup. | | [Your response here]
| [x] | Recruiter profile ownership | Current migrated runtime maps `recruiter_profiles.recruiter_id` to the app user ID. | For target enterprise launch, map it to the stable internal identity subject supplied by ATS/Okta, or deprecate user-editable profile fields if identity data is authoritative upstream. | Name/email/phone UI can eventually be removed from settings/home/login/create-account surfaces. | [Your response here]
| [ ] | Admin access | Admin check is hardcoded email allowlist in `src/lib/auth/rbac.ts`; Postgres app roles now work in local smoke. | For target enterprise launch, map admin access from ATS/Okta claims/groups or an integration-owned role mapping. Temporary allowlist/DB roles can remain only as a bridge. | | [Your response here]
| [ ] | QA evaluator access | QA access accepts admin users, metadata roles, hardcoded email allowlist, and app-owned roles in local smoke. | For target enterprise launch, map QA access from ATS/Okta claims/groups or an integration-owned role mapping. | | [Your response here]
| [ ] | Candidate authentication | Candidate practice flow is token-link based, not Supabase login. Candidate API calls use `x-candidate-token`. | Working assumption: candidate remains token-only for phase 1. Later ATS integration can issue, broker, or decorate these links without forcing candidate accounts now. | | [Your response here]
| [x] | Service-role replacement | Trusted server operations now use the server-side Postgres pool plus explicit app authorization checks. | No Supabase service-role key is required by the migrated runtime. | | [Your response here]

## Product Flow Acceptance Tests

| Status | To confirm | Current app | Post-migration clarification | Fu's notes | Integration team response |
| --- | --- | --- | --- | --- | --- |
| [x] | Recruiter auth smoke | Local Postgres smoke user `fu@rangam.com` can sign in through `/api/auth/login` and reach `/recruiter` with a Postgres app-auth session. | Done locally through HTTP auth/protected-page smoke, browser-visible recruiter smoke, and manual validation. Target production should replace or bypass this login with ATS/Okta launch. | Local/UAT bridge validated; target identity handoff remains integration-owned. | [Your response here] |
| [x] | Recruiter profile/settings | Current settings/profile guard now go through `/api/recruiter/profile`, which uses the authenticated app user id and writes `recruiter_profiles` through the app-auth-compatible server helper. | Done locally through `npm run postgres:smoke:profile-settings` and manual validation. Target deployment may deprecate editable profile identity fields once ATS/Okta is authoritative. | Name/email/phone UI can eventually be removed from settings/home/login/create-account surfaces. | [Your response here] |
| [x] | Invite creation | Current flow creates sessions, questions, candidate tokens, and invite batch records. | Done locally through product-flow smoke, browser-visible recruiter smoke, resend/retry smoke, and manual validation with Postgres selectors. Target DB validation remains open. | Validate generated links use the target public origin in company env. | [Your response here] |
| [x] | Email send | Current email uses SMTP/nodemailer env vars. The target Microsoft/Office365 deployment must set `SMTP_HOST`, `SMTP_PORT`, `SMTP_USERNAME`, `SMTP_PASSWORD`, and `SMTP_FROM_EMAIL` explicitly. | Done locally with real Office365 SMTP for invite and debrief email. Target deployment still needs final SMTP policy/config and public-origin validation. | Local recipient/recruiter received invite/debrief during validation. | [Your response here] |
| [x] | Candidate starts practice | Current candidate opens token link and starts/resumes session without login. | Done locally through product-flow smoke, browser-visible candidate smoke, practice-again smoke, and manual validation. Target host validation remains open. | Candidate access remains token-link based. | [Your response here] |
| [x] | Candidate submits answers | Current answer submission writes answers, eval results, session status, and recovery state. | Done locally for normal text-mode submission and feedback flow through product/browser/manual validation. Voice-mode and the separate feedback-failure recovery hardening patch should be validated before final production cutover if included. | Positive path validated; recovery patch is separate workstream. | [Your response here] |
| [x] | AI surfaces | Current app calls Gemini for question generation, answer feedback, hints, strong response, and session debrief; AI-quality instrumentation records `ai_generations`. | Done locally: route-stack smoke triggers all five surfaces with Gemini and confirms Postgres `ai_generations` plus QA read/export; browser-visible candidate smoke also exercised hints, strong responses, answer feedback, and debrief. | QA AI-quality review validated manually. | [Your response here] |
| [x] | Session debrief/email | Current completion can generate debrief and send email. | Done locally: email smoke and manual validation confirm debrief generation, DB persistence, and debrief email delivery through SMTP. Target deployment still needs public-origin and SMTP validation. | Recipient received the debrief email. | [Your response here] |
| [x] | Recruiter session review | Current recruiter review shows session metadata, questions, submitted state, and transcript, not candidate AI feedback. | Done locally for current product contract: owning recruiter can open `/recruiter/sessions/[id]`, and cross-recruiter denial is covered by Level 11 negative-permissions smoke. | Recruiters do not see candidate AI coaching feedback. | [Your response here] |
| [x] | Admin/QA pages | Admin feedback and QA AI-quality are protected internal surfaces. | Done locally: positive admin/QA access covered by local/manual smokes; recruiter-only user redirects away from `/admin/feedback` and `/qa/ai-quality`, and `/qa/ai-quality/export` returns `403`. | QA access for Kushal should map to target identity/roles. | [Your response here] |
| [x] | Invite send/resend/retry flows | Current flow marks invitation sent and tracks invite batch retry state. | Done locally: invite send/debrief send passed Level 4 email smoke, and resend plus failed-batch retry passed Level 9 resend/retry smoke. Target-environment validation still needs the final public host, SMTP policy, and company DB. | Local resend manual validation passed. | [Your response here] |
| [ ] | Recovery path | Current app has an unstaged session-recovery patch workstream for answer submitted but feedback failed. | Decide whether this patch must merge before migration validation, then test recovery behavior on the Postgres target stack. | Separate from the completed Postgres migration positive-path validation. | [Your response here] |

## Observability And Operations

| Status | To confirm | Current app | Post-migration clarification | Fu's notes | Integration team response |
| --- | --- | --- | --- | --- | --- |
| [ ] | Application logs | Current Logger emits structured server logs with redaction. | Logs visible in target platform with request IDs/correlation IDs and no secrets/PII leakage. |
| [ ] | Metrics/SLO dashboard | Current metrics can be memory or Supabase durable backend; production contract expects durable backend. | Confirm durable metrics backend in target stack and validate `/api/recruiter/ops/metrics` if retained. |
| [ ] | AI quality records | Current `/qa/ai-quality` reads all `ai_generations` through service-role server access. | Confirm QA users can inspect target AI records and export filtered JSON/CSV. |
| [ ] | Error alerting | Current docs include ops alert policy, but app-level alert routing depends on deployment config. | Confirm where production errors, AI failures, auth failures, and rate-limit spikes alert. |
| [ ] | PII/redaction | Current app has log redaction and AI-quality redaction, but DB records still include candidate/recruiter domain data. | Confirm PII handling expectations, DB access restrictions, and data-retention owner. |
| [ ] | Performance/load | Current app relies on Supabase managed connection behavior. Plain Postgres introduces pooling and connection limits. | Confirm DB connection limit, pool size, timeout, slow query logging, and expected concurrent usage. |

## Communication Questions To Send

Use these to disambiguate scope with the integration team:

1. We are proceeding as if phase 1 is full Supabase replacement, not database-only replacement. Please correct this if that is not the intended scope.
2. We now understand internal users should launch Interview Coach from the ATS, with identity/auth passed through by an enterprise service such as Okta. Please confirm the handoff contract, trusted claims, role mapping, session behavior, and target landing route.
3. We plan to keep candidate practice token-link based with no candidate login for phase 1. Are there security or ATS-integration requirements that would block this?
4. We are proceeding as if the target Postgres DB is fresh, with no bulk historical Supabase data migration. Please correct this if existing records must be migrated.
5. We are proceeding as if Postgres SQL functions/procedures are allowed. Please confirm if there are restrictions.
6. We can support both `DATABASE_URL` and individual `POSTGRES_*` values, preferring `DATABASE_URL` when present. Please confirm the production env contract when known.
7. We can develop against the provided DB credentials, but production should use a least-privilege app user. Who will provide the final app DB user?
8. We are assuming production host `https://interviewcoach.talentarbor.com`. What staging, UAT, or branch deployment URL should we validate against during integration?
9. Who can view app logs and confirm target DB writes during integration validation?
10. What are the final Microsoft/Office365 SMTP host, port, sender format, and SMTP-auth policy for `interviews@coach.rangam.com`?
11. After migration/auth code is ready, should the integration team validate unit/integration behavior in the company deployment environment before QA performs product testing?

## Migration Roadmap

This roadmap is intentionally broader than the items we are fully confident about today. It captures the work needed to fully unwire Supabase while preserving current app functionality.

### Phase 0 - Workstream Isolation And Discovery

| Status | Work item | Notes / done when |
| --- | --- | --- |
| [x] | Create or use a separate worktree for `feature/postgres-integration`. | Done: `C:\tmp\Interview-Coach-Recruiter-postgres` created and tracks `azure/feature/postgres-integration`. Original dirty worktree was not switched. |
| [x] | Inspect the Azure `feature/postgres-integration` branch. | Done: active branch is `feature/postgres-integration`; latest reviewed/pushed head before this doc handoff slice is `4cc0c63 docs update target identity handoff`. |
| [x] | Inventory every Supabase touchpoint. | Done: implementation inventory created at [supabase_touchpoint_inventory.md](./supabase_touchpoint_inventory.md) and refreshed through browser-client cleanup, API-route auth seam work, server-page profile lookup cleanup, all local Postgres smoke levels, and manual full-flow validation. |
| [x] | Confirm full Supabase replacement as phase-1 scope. | Working decision captured: phase 1 should fully replace Supabase, including data access and auth/session behavior. Treat any Supabase runtime dependency as temporary debt. |
| [ ] | Confirm target runtime facts. | In progress: current facts and open confirmations are documented in [target_runtime_facts.md](./target_runtime_facts.md). Still needs integration-team answers for staging/UAT URL, deployment platform, secret store, logs, DB inspection path, final Postgres env contract, and production DB user. |
| [x] | Define acceptance ownership. | Working model captured: local automated tests first, integration-team validation in company deployment environment second, QA product testing third. |

### Phase 1 - Postgres Foundation

| Status | Work item | Notes / done when |
| --- | --- | --- |
| [x] | Add server-only Postgres client module. | Done locally: `pg` config/client helpers support `DATABASE_URL` preference, individual `POSTGRES_*` fallback, SSL mode support, pooling, query timeout, idle-client logging, and config tests. Route/repository adoption has been validated through Postgres smoke levels with selectors pinned to `postgres`. |
| [ ] | Add DB health/diagnostic helper for integration validation. | In progress: added a health helper that confirms connectivity and current database name without exposing secrets or PII. Disposable Docker validation also confirmed direct DB connectivity. Still needs a protected route or CLI wrapper once auth/ops access shape is decided. |
| [ ] | Define migration application strategy. | Decide whether migrations are applied manually by DBA, via script, or through deployment pipeline. First draft now lives under neutral `db/migrations/`; local smoke commands can apply it to a disposable DB. Execution ownership for company DB remains open. |
| [x] | Consolidate target schema. | Done locally: reconciliation plan created at [target_schema_reconciliation.md](./target_schema_reconciliation.md), executable neutral schema added at `db/migrations/001_initial_schema.sql`, and disposable Postgres validation passed through `npm run db:apply-schema`, `npm run db:smoke-schema`, repository tests, route-stack smokes, browser-visible smokes, and manual full-flow validation. Company DB validation remains open. |
| [x] | Add app auth/session tables. | Done for the local/UAT bridge: schema includes `app_users`, `app_user_credentials`, `app_sessions`, `app_user_roles`, password reset tokens, email verification tokens, and auth audit events. These support Supabase-free validation; production may use only the user/session/role mapping subset after ATS/Okta identity handoff. |
| [x] | Add role/permission model. | Done locally: schema includes `app_user_roles` with recruiter/admin/QA roles, RBAC accepts app-owned DB roles, and Level 11 negative-permissions smoke validates owner/role behavior. Target production still needs ATS/Okta group or claim mapping. |
| [x] | Preserve product data tables. | Done locally: neutral schema includes sessions, questions, answers, eval_results, projection_session_now, candidate_tokens, events, recruiter_profiles, recruiter_templates, invite_batches, invite_batch_candidates, user feedback, idempotency keys, metrics rollups, rate-limit buckets, and ai_generations. Product smokes and manual validation exercised the primary runtime tables with Postgres selectors. |
| [x] | Port SQL functions/procedures. | Done locally: neutral schema ports invite batch creation, engagement increment, rate-limit consumption, metrics counter/timing rollups, SLO summary functions, and AI-generation summary. Functions/procedures still need company DB permission validation during deployment. |
| [x] | Decide RLS posture. | Done as a working architecture decision: the branch uses server-side authorization plus DB constraints rather than Supabase RLS semantics. Revisit only if company DB policy requires RLS. |
| [ ] | Define production DB user privileges. | Code can run with provided creds while developing; production should use a least-privilege app user rather than generic `postgres`. |

### Phase 2 - Data Access Replacement

| Status | Work item | Notes / done when |
| --- | --- | --- |
| [x] | Implement Postgres session repository. | Done: the migrated runtime uses `PostgresSessionRepository` and preserves session create/read/update/delete, dashboard summaries, answer draft/final text, eval result persistence, analysis deletion, debrief summary expiry, invitation sent timestamp, and engagement increment behavior. Shared route/application/page call sites use the repository factory. |
| [x] | Implement Postgres invite repository. | Done locally: `PostgresInviteRepository` preserves session/question/token creation, candidate token lookup, invite batch tracking, failure tracking, and retry marking. Disposable Docker integration tests and route-stack smokes validate invite create, invite resend, failed-batch retry, candidate token lookup, batch state transitions, and idempotency with all relevant backend selectors pinned to `postgres`. Target DB validation remains open. |
| [x] | Implement Postgres template repository. | Done: `PostgresTemplateRepository` preserves recruiter-owned template create/update/delete, shared-template list visibility, and explicit admin manage-all behavior. Template server actions use the repository factory. |
| [x] | Implement Postgres feedback repository. | Done: `PostgresFeedbackRepository` preserves app feedback capture and admin feedback read shape. |
| [x] | Implement Postgres AI-quality repositories. | Done: Postgres AI-quality repositories preserve capture writes, read filters, pagination counts, grouped views, summary widgets, selected-record reads, and JSON/CSV export. |
| [x] | Port candidate token issue/validate. | Done: Postgres candidate tokens issue random raw tokens, store only token hashes, validate by `x-candidate-token`, enforce session binding, and reject expired/revoked rows. |
| [x] | Port idempotency store. | Done: Postgres idempotency uses the migrated table/constraints and preserves reserve, pending duplicate, conflict, complete/replay, and pending release behavior. |
| [x] | Port rate-limit backend. | Done: `RATE_LIMIT_BACKEND=postgres` uses the neutral Postgres function and preserves window reset, over-limit denial, and concurrent atomic consumption. Local/test memory remains available; production defaults to Postgres and rejects memory. |
| [x] | Port metrics backend. | Done: `METRICS_BACKEND=postgres` uses the neutral Postgres metrics functions and preserves counter/timing writes, rollup reads, and operational SLO summaries. Local/test memory remains available; production defaults to Postgres and rejects memory. |
| [x] | Replace direct Supabase query calls in routes/pages/actions. | Done for migrated runtime: API-route auth checks go through `getAuthenticatedRouteUser()`, server pages/actions use app-session user helpers plus Postgres repositories/profile helpers, app-auth middleware handles missing-session redirects, and Supabase fallback repositories/helpers/packages/env were removed. |

### Phase 3 - Internal User Identity

| Status | Work item | Notes / done when |
| --- | --- | --- |
| [ ] | Define enterprise identity handoff contract. | New target: internal users launch from ATS, likely through Okta or equivalent. Need integration-owned contract for identity payload, signature/token validation, role/group mapping, session establishment, expiration, replay protection, and landing route. |
| [x] | Keep app-owned auth as local/UAT bridge. | Done locally: public self-signup remains paused, operator/developer provisioning seeds local/UAT app users directly in Postgres, and local route-stack plus browser smokes validate login/session behavior. Do not expand password reset/self-signup unless app-owned auth becomes the final production choice. |
| [x] | Implement password hashing. | Done for local/UAT bridge: app auth foundation uses Node `crypto.scrypt` with per-password random salt and stores only encoded hashes. |
| [x] | Implement login route/server action. | Done for the local/UAT bridge: `/api/auth/login` verifies credentials, creates a hashed server-side session, records auth audit events, and sets the HTTP-only app session cookie. Target production should replace or bypass this with ATS/Okta session exchange. |
| [x] | Implement logout. | Done for local/UAT bridge: `/api/auth/logout` revokes the server-side session by hashed token and clears the cookie. Target production UX expects no visible logout button if users launch from ATS. |
| [x] | Implement session validation middleware. | Done for the local/UAT bridge: app-auth mode redirects missing-cookie recruiter/admin/QA page requests to `/login?next=...`, allows public candidate pages, and preserves E2E cookies. Middleware intentionally does not hit Postgres; layouts/route handlers remain the authoritative auth layer. Final work is retiring Supabase fallback middleware when cutover is complete. |
| [x] | Replace `getCachedUser()`. | Done for migrated runtime: `getCachedUser()` resolves the app session cookie or E2E cookie and no longer delegates to Supabase. Future ATS/Okta handoff can establish the same app-session shape or replace this helper with an enterprise identity adapter. |
| [x] | Replace RBAC helpers. | Done for migrated runtime: helpers use the app-owned `AppUser` shape with DB-backed `roles`. Target production should map roles from ATS/Okta groups/claims or integration-owned role mapping. |
| [ ] | Implement password reset. | Defer unless app-owned auth becomes the final production choice. Enterprise identity should own password/MFA lifecycle. |
| [ ] | Implement email verification if self-sign-up remains. | Defer unless app-owned auth becomes the final production choice. |
| [x] | Replace browser Supabase auth clients. | Done for current runtime browser clients: login posts to `/api/auth/login`, desktop/mobile logout post to `/api/auth/logout`, and settings/create/ProfileGuard read/write profile state through `/api/recruiter/profile`. These surfaces become candidates for removal or simplification once ATS/Okta launch is implemented. |
| [x] | Preserve E2E test auth seam. | Done: E2E cookie mode now returns an app-owned user shape without Supabase types. |
| [x] | Add auth tests. | Done for the local/UAT bridge: focused tests cover scrypt password hashing/verification, successful/failed password login orchestration, hashed session lookup/revocation, audit writes, DB-role RBAC, login cookie setting, logout cookie clearing, and app-auth middleware redirects/allows. Route-stack and browser-visible smokes cover successful Postgres login. Password reset/email verification remain deferred because enterprise identity should own that lifecycle unless app-owned auth becomes the final fallback. |

### Phase 4 - Candidate Flow Preservation

| Status | Work item | Notes / done when |
| --- | --- | --- |
| [x] | Preserve `/s/[token]` invite entry. | Done locally: Level 2 product-flow smoke, Level 6 browser-visible candidate smoke, and manual validation opened Postgres-backed candidate links and fetched sessions with `x-candidate-token`. Level 11 negative-permissions smoke validates missing/mismatched token denial. |
| [x] | Preserve initials/intake/resume behavior. | Done locally: Level 2 product-flow smoke verifies initials submission and resume text persistence through invite-created session intake, and Level 6 browser-visible candidate smoke validated the visible initials/readiness flow. |
| [x] | Preserve answer submission behavior. | Done locally: product and browser-visible smokes verify draft save, answer submit, answer feedback persistence, and session progress with Postgres-backed session/eval stores. The separate session-recovery hardening patch for feedback-failure dead ends remains a non-migration workstream decision. |
| [x] | Preserve TTS/candidate API authorization. | Done locally for candidate-token protection: candidate routes continue using `x-candidate-token`/session-bound checks, and Level 11 negative-permissions smoke validates missing/mismatched token denial. Browser-visible smoke also confirmed candidate AI/TTS-adjacent paths require `GEMINI_API_KEY` in realistic candidate UX. |
| [x] | Preserve practice-again links. | Done locally: chained repeat attempts now persist each issued candidate token as encrypted session invite-token metadata. `npm run postgres:smoke:practice-again` validates attempt 1 -> attempt 2 -> attempt 3 through the route stack with Postgres-backed session, invite, and candidate-token stores. Target-environment validation still needs the final public host. |

### Phase 5 - Supabase Unwiring And Runtime Cleanup

| Status | Work item | Notes / done when |
| --- | --- | --- |
| [x] | Remove `src/lib/supabase/*` runtime dependency. | Done: Supabase server/middleware helpers were removed and `src/middleware.ts` delegates to the app-auth middleware. |
| [x] | Remove Supabase packages. | Done: `@supabase/ssr` and `@supabase/supabase-js` were removed from `package.json` and `package-lock.json`. |
| [x] | Replace Supabase `User` types. | Done: components, RBAC, tests, and E2E helpers use app-owned user types. |
| [x] | Remove Supabase env requirements. | Done for migrated runtime: `.env.example` no longer lists `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, or `SUPABASE_SERVICE_ROLE_KEY`; production contract tests now use Postgres-oriented runtime values. |
| [x] | Rename backend configuration values. | Done pragmatically: backend selector env names remain stable, but supported runtime values are now Postgres-only for migrated stores, with `memory` retained only for local/test rate-limit and metrics paths. |
| [x] | Update docs that name Supabase as current auth/data provider. | Done for the core migration handoff docs. Broader historical docs may still mention Supabase as prior-state context. |
| [ ] | Retire or convert `supabase/` migrations. | Neutral target SQL already exists at `db/migrations/001_initial_schema.sql`. Decide whether to keep `supabase/` as historical source material, archive it, or remove it after reviewers no longer need original schema provenance. |

### Phase 6 - Product And Operational Validation

| Status | Work item | Notes / done when |
| --- | --- | --- |
| [x] | Run unit and integration tests locally. | Current Supabase-sunsetting pass: `npx tsc --noEmit` passed and the focused auth/repository/operational-store/API test set passed. Earlier smoke notes cover broader Postgres route-stack, browser-visible, email, AI, permissions, and manual validation. |
| [x] | Run full product smoke locally. | Done for the currently identified local smoke scope: local Postgres smoke DB, schema, rollback smoke, app-user provisioning, HTTP auth/protected-page smoke, repeatable Level 2 product smoke, repeatable Level 3 AI/QA smoke, repeatable Level 4 real SMTP invite/debrief smoke, Level 5 browser-visible recruiter invite smoke, Level 6 browser-visible candidate practice smoke, Level 7 practice-again chain smoke, Level 8 profile/settings smoke, Level 9 invite resend/retry smoke, Level 10 recruiter review smoke, and Level 11 negative-permissions smoke passed. User also completed manual validation across recruiter create/send/resend/dashboard/session review, admin feedback, QA AI-quality, candidate session, and two additional practice-again attempts. |
| [ ] | Validate deployment env. | Confirm DB connection, SMTP, Gemini, public origin, auth secrets, cookie security, logs, and target host. Local disposable Docker schema validation passed; company deployment environment validation remains open. |
| [ ] | Validate in staging/UAT. | Integration team should confirm app writes to company Postgres and logs are available in the target environment. |
| [x] | Validate permissions. | Done locally in Level 11 negative-permissions smoke: recruiter-only cross-session review returned `404`, cross-recruiter resend returned `403`, admin/QA pages redirected to `/recruiter`, QA export returned `403`, missing candidate token returned `401`, and mismatched candidate token returned `403`. Target-environment validation still remains. |
| [ ] | Validate operational controls. | Backups, rollback plan, secret rotation path, DB connection limits, slow query visibility, alerting, and PII handling are confirmed. |
| [ ] | QA product validation. | QA runs full product regression after integration validates environment behavior. |

## Work That Can Continue Now

These items can proceed without waiting for every target-environment answer:

- Code review this branch against the consolidated reviewer/deployment handoff in [postgres_migration_handoff.md](./postgres_migration_handoff.md) and the smoke evidence in [local_postgres_smoke.md](./local_postgres_smoke.md).
- Apply `db/migrations/001_initial_schema.sql` to a company dev/UAT Postgres database once the integration team provides a target DB and migration execution path.
- Configure a migrated environment with all runtime backend selectors pinned to `postgres`, plus `DATABASE_URL` or `POSTGRES_*`, `NEXT_PUBLIC_APP_URL`, `ENCRYPTION_SECRET`, `GEMINI_API_KEY`, and Microsoft/Office365 `SMTP_*` values.
- Implement or adapt the internal-user identity handoff so ATS/Okta launch creates an authenticated app session, maps recruiter/admin/QA roles, and lands users on `/recruiter/create`.
- Hide or remove transitional standalone internal-user UI after identity integration is ready: login/create-account, logout controls, settings identity fields, and home-page identity prompts.
- Update production contract/env tests and deployment runbooks to reflect Postgres/app-auth-or-ATS-auth/SMTP expectations.
- Decide whether to archive or remove the historical `supabase/` schema/migration directory after reviewers no longer need it for provenance.
- Decide whether the separate session-recovery hardening patch should merge before final production cutover, then validate the feedback-failure recovery path on the migrated stack.

## Coalesced Open Questions

These are the remaining questions that materially affect deployment readiness:

1. Hosting/runtime: What platform will host the app, what staging/UAT URL should be used, and is Node 22 approved there?
2. Secrets/env: Where are runtime secrets managed, and is the final DB env contract `DATABASE_URL`, split `POSTGRES_*`, or both?
3. Network/DB access: Can the app runtime reach Postgres over TCP, what SSL mode/cert is required, and who provides the least-privilege app DB user?
4. Migration execution: Who applies DDL/functions/triggers/indexes to the company DB, who owns rollback, and what backup/restore policy applies?
5. Internal identity handoff: What ATS button/deep-link contract, Okta or identity-provider token, trusted claims, role/group mapping, session lifetime, and replay protection will the integration team provide?
6. Target UI posture: Should login/create-account/logout/settings identity fields be disabled by deployment config, removed immediately after identity handoff, or left available for UAT fallback only?
7. Email: What are the final Microsoft/Office365 SMTP host, port, sender format, and SMTP-auth requirements for `interviews@coach.rangam.com`?
8. Observability: Who can view app logs, query target DB records for validation, inspect failed requests by correlation ID, and receive production alerts?
9. Acceptance: Which checks belong to local development, integration-team environment validation, and QA product regression before production cutover?
10. Recovery patch timing: Should the answer-submitted/feedback-failed recovery hardening patch be merged into this branch before target-environment validation, or released separately?

## Senior Readout

With confidence, a senior engineer can say:

- The Supabase-backed app was already a working product before this migration branch.
- This branch now has a validated Postgres-backed runtime path that can run independent of Supabase when backend selectors are pinned to `postgres`.
- Supabase runtime fallback has been removed from the codebase. Remaining Supabase references are historical schema/docs context for reviewers, not active deployment dependencies.
- The preferred target internal-user flow is ATS-launched enterprise identity handoff, not standalone app-owned email/password. The app-owned Postgres auth built here is a local/UAT bridge and proof that Supabase Auth can be removed.
- Candidate token-link access has been preserved and validated locally; candidates still do not need accounts for phase 1.
- Remaining blockers are target-environment facts and ownership: company DB provisioning/migration execution, least-privilege DB user, hosting/runtime, secrets, logs/DB inspection, SMTP policy, ATS/Okta identity contract, and QA/integration acceptance.
- A realistic signoff path is local automated/manual validation already completed, integration-team validation in the company deployment environment, then QA product regression before production cutover.
