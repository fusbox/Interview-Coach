# Candidate App Handoff

Status: Historical reference

> [!WARNING]
> This file is archived historical context and does not govern current V2 implementation.

Original status: Active execution state
Last updated: 2026-07-18

## Agent Bootstrap

- Active rebuild branch: `feature/candidate-v2-rebuild`.
- Current route target: canonical candidate routes under `/candidate/*`; `/practice2`, `/session2/[sessionId]`, and `/dashboard2` are compatibility redirects only.
- Before a meaningful candidate workflow slice, inspect the matching V1 behavior from `feature/candidate-module` and record what V2 preserves, reinterprets, retires, or defers.
- Run `.agents/skills/senior-slice-pass` before and after meaningful slices. Use `.agents/skills/senior-milestone-pass` before multi-slice commit/phase boundaries and `.agents/skills/senior-release-pass` before deployment, pilot, migration, or release decisions.
- Use [SPEC](./SPEC.md) for product behavior, [DATA_CONTRACT](./DATA_CONTRACT.md) for durable shapes, [Local Dev Bootstrap](./09-dev/local-dev-bootstrap.md) for local DB/runtime commands, and [V1 SWOT And Rebuild Runway](./04-architecture/v1-swot-and-rebuild-runway.md) for the V1 reference rule.
- Default verification for candidate code changes: focused tests for touched modules, `npm run test:candidate`, `npm run typecheck`, `npm run build`, and `git diff --check`. Add DB smoke validation when migrations, seeds, or repository contracts change.
- Keep this file narrow. It should tell the next coding agent what is true now, what to do next, what is risky, and where to reconstruct older history.

## Current State

- The rebuild is intentionally cleanroom. Bring old app code back only when a numbered slice needs it, and favor V2 contracts over V1 code unless V1 is clearly more fit for the job.
- Public root, `/candidate/launch`, dev launch, `/candidate/setup`, `/candidate/practice/ready`, `/candidate/practice/ready/[intentId]`, `/candidate/session/[sessionId]`, and `/candidate/dashboard` exist.
- Host launch has dev-mode token minting, a `jose`-backed production HS256 verifier, standard numeric `iat`/`exp` validation, a two-minute launch-token ceiling, optional issuer-scoped `jti`, SHA-256 token-fingerprint replay protection, independently governed seven-day app sessions, candidate launch-session storage, server-selected entry routing, and fail-closed orchestration. Raw launch tokens are not persisted. The first production host-data implementation is a TA-only server-side MSSQL adapter: it resolves `CandidateMaster`, requires exact `CandidateJobCollectionTxn` ownership for job-aware launch, reads canonical `JobCollection` context, uses bounded pooling/timeouts and typed parameters, and emits metadata-only diagnostics. Job-aware launch atomically stages immutable canonical role/JD with the launch session; identity-only candidates enter setup when they have no prep contexts and otherwise enter the dashboard. It does not infer a job, query resume/consent domains, trust `CreatedBy` as user identity, or enable RW. Existing launch identities refresh canonical TA profile attributes and `last_seen_at` without permitting candidate relinking or disabled-profile revival. Production launch responses now carry a random request id that correlates browser-safe outcomes with allowlisted assembly, verification, and exchange diagnostics; observability delivery cannot alter launch correctness. A stdin-only live acceptance probe validates clean redirects, cookie attributes, destination reachability, replay, and generic rejection without retaining credentials or candidate/job content. Real TA traffic remains configuration- and network-gated pending host secret operations, least-privilege credentials, and execution of the signed staging matrix.
- `/candidate/setup` is the generic new-prep-context setup surface. It preserves unsubmitted candidate-scoped browser drafts and validates the typed setup contract before submit. Identity-only production launch may create a manual candidate-owned path without host source identity. Job-aware production launch resolves unexpired server staging from the launch cookie, renders canonical role/JD read-only, and rejects browser mutation or stale-tab reuse. Manual setup creates path one when no exact candidate + normalized role/JD match exists, repair-reuses only a matching profile with no sessions, and returns a candidate-owned choice dialog instead of silently reusing any matching profile with activity. Host-backed paths use candidate + source platform + job collection id and retain optional requirement/launch lineage. The dialog can canonicalize to the selected dashboard context or explicitly create a new independent `role_profile_id`; closing or a failed separate-path write preserves the draft. Staging is consumed atomically with first-session creation or explicit existing-path selection, and the UI clears the setup draft only after either succeeds. The created session stores non-null `role_profile_id`. The provisional browser bridge remains a nonproduction-only exception when durable identity/storage is unavailable. Follow-up practice setup must not be rendered through this generic setup surface.
- `candidate_practice_sessions` is the main V2 durable session boundary. It stores candidate ownership, setup snapshots, question-plan snapshots, optional wording state, progress state, answer drafts, pending answer submissions, accepted analysis snapshots, feedback action events, candidate-led completion snapshots, and follow-up attempt lineage. `planned` means no answer has been accepted; the first accepted answer promotes the session to `in_progress`, where it remains until explicit completion. Completed or abandoned sessions reject new accepted-answer writes. Migration 012 repairs historical answered rows that were left `planned`. In explicit local dev host-launch mode, routes resolve deterministic `dev-host-launch-*` cookies to fixture candidate profiles without treating those cookie values as DB launch-session ids.
- The candidate session now enters a shared one-question live-practice shell driven by neutral session runtime facts. It provides role/question progress, candidate-ready typed-answer capture, debounced and serialized durable draft saves, exact-question and draft recovery, distinct submit-versus-analysis recovery, candidate-safe coaching, and a persistent candidate exit to `/candidate/dashboard` that does not reset progress. Candidate and invited adapters share the shell while retaining distinct mutation, completion, and return behavior. Final completion mounts one stable `Preparing your Coach Plan` overlay before the completion request and keeps it mounted through dashboard navigation so feedback/button state cannot flash during the handoff. Legacy `question_preview` progress is migrated to the equivalent live question; preview navigation and Back to plan are no longer candidate product UI.
- Initial and follow-up candidate-led rounds render through one production-shaped pre-session landing composition. Initial rounds transition from the durable session record. Every new follow-up round assembled from an existing prep context, whether one question or many, must route through `/candidate/practice/ready/[intentId]`; direct intent creation may bypass the editable builder but not the landing. Only an already-started or consumed round may resume directly. Both entry types use candidate-led preparation/no-hiring-decision copy, pause-and-return reassurance, one primary action, and the 1.25-second "Entering practice space" overlay. The overlay fades in over the landing, holds while the live shell mounts underneath, then fades away; reduced-motion users receive the same orienting state without animation. A shared invited-practice module provides the future initials-confirmation -> invited landing variant with recruiter-visibility and original-invitation-return copy, but no invite route or persistence mutation is wired yet.
- The shared question-audio lifecycle defines landing Q1 prefetch, current/next prefetch, user-gesture unlock, and browser-session play-once memory. No production TTS transport is wired: V1's TTS route is coupled to legacy authorization and must not be copied. Production question wording, media capture, and invited route convergence remain pending.
- `/candidate/practice/ready` is the follow-up practice bridge. Query-pointer actions are validated against candidate-owned facts, durable `candidate_practice_intents` support one or many selected questions, `/candidate/practice/ready/[intentId]` renders staging, and `/candidate/practice/ready/[intentId]/start` creates follow-up `candidate_practice_sessions` with source intent, source question, session attempt, and question attempt lineage. Repeated practice is intentionally unlimited.
- `/candidate/dashboard` consumes typed V2 read models derived from candidate-owned `candidate_practice_sessions` plus every lifecycle attempt of versioned Coach Update artifacts. Its stable shell uses profile-backed candidate identity, an opaque prep-context switcher, a modest Coach Plan introduction, and state-priority active-round, Coach Update, Practice Next, and Coach Plan modules. The newest artifact generation attempt for the latest selected-context round projects explicitly as awaiting practice, pending, ready, or unavailable; pending/failed work never falls back to older prose or exposes operational error detail. A ready sparse entry opens the exact immutable artifact in an accessible practiced-question carousel with clickaway/Escape/close exits and focused-practice actions. Its `New` treatment stores only an opaque browser-local presentation key, performs no server write, and hands primary emphasis to Practice Next after review when no active round exists. The Coach Plan entry opens an accessible Categories/Question Set reference derived from the earliest original session under the selected opaque prep context. Cross-role inventory and profile-backed selected-context reads use complete candidate-owned session history so older prep contexts and their baselines cannot fall out of a recent-activity window. Coverage counts each baseline root question once after any submitted answer, including durable answers in unfinished rounds; drafts and repeated attempts do not inflate it, and malformed lineage fails closed. Category teaching is shared domain content. Upcoming question wording is hidden until deliberately revealed. Scores, mastery claims, Skills aggregation, transcripts, and per-question queue mutation inside Coach Update or Coach Plan detail are absent. The provisional metric strip, duplicate always-open latest-round transcript, and unanswered questions inside Coach Update are retired. Headline and per-context answered/coached evidence still includes active and completed rounds, counting each submitted question occurrence once and counting coaching only when the latest submission has matching accepted analysis; retry attempts remain separate lineage. Canonical navigation is `/candidate/dashboard?prep=<roleProfileId>`; profile selection is ownership-scoped, same-title profiles remain separate, and session exit/completion plus follow-up staging return to the session or intent's profile. Bare, malformed, stale, or unauthorized profile selectors choose the normal candidate-owned fallback once and canonicalize. `targetRole` remains a bounded compatibility selector only for historical records whose `role_profile_id` is null and cannot select profile-backed work. Plan Progress creates one durable multi-question intent for unanswered planned coverage, Practice from Feedback resolves the coached-question intent, and the persistent selected-context control opens the durable practice builder with its authoritative count and eligible Coach Plan choices. No follow-up path uses generic setup. Attempt lineage is data infrastructure for later trend, recruiter, and BI uses, not current candidate UI.
- QA/evaluation has a typed V2 export contract for answer-quality review and model A/B comparison. It derives redacted fixed cases, model/prompt/evaluator run snapshots, and same-input comparison snapshots. It intentionally does not include a candidate-app versus recruiter-app source axis because V2 AI calls should be evaluated as one interview-coach evaluator job.
- Normalized `candidate_answer_attempts` and `candidate_answer_evaluation_runs` preserve immutable answer and evaluator lineage independently of the slot-keyed latest-result JSON. The answer-submit route appends or replays attempt one, authorizes feedback retry only from the exact latest analyzed attempt and saved retry action, appends the retry under a slot lock, and projects stable attempt identity into `answer_submissions_json`. Evaluator runs have sequential generations within one answer attempt/purpose, an exact 60-second claim lease over the 45-second runtime budget, ownership-scoped advisory-lock claims, immutable terminal history, and run-id completion fencing. Stale requested work is failed before a new generation is allocated; a fresh matching claim replays without a second provider call; terminal retry may retain its logical request key; and candidate coaching permits at most one fresh requested and one accepted completed result per answer attempt/input fingerprint. The claim boundary blocks a new generation after a nonretryable terminal result and caps candidate coaching at three generations per answer attempt in ten minutes; QA-comparison variants remain independently claimable. The accepted internal run stores parsed stage artifacts and bounded stage metadata; the separate session compatibility projection stores only candidate-facing coaching and its minimal interaction directive. Hidden extraction, criteria, pattern gap, verifier, and feedback-plan facts do not enter browser/session state.
- The ratified [evidence-first evaluator contract](./05-quality/evidence-first-evaluator-contract.md) fixes one immutable answer attempt as the evaluation unit; validates exact answer spans and category signals; separates applicability from observed qualitative bands; requires versioned references for technical correctness; conditionally verifies risky claims; validates candidate coaching against accepted evidence; and defines provider metadata, timeout/retry, redaction, telemetry, QA, and candidate-safe projection policy. Its provider-neutral runtime defines one-attempt extractor, verifier, and composer ports; owns the 45-second aggregate and 12-second stage limits; permits only policy-allowlisted retries/re-extraction; and rejects malformed stored-run timelines or stage sequences. The local deterministic fixture traverses that exact runtime. An explicit nonproduction server-environment harness proves provider, malformed-output, fingerprint/span, unsafe-inference, verifier, concurrency, stale/late completion, projection-repair, refresh/new-tab, and new-generation recovery behavior without changing answer durability. The ratified [production evaluator integration contract](./05-quality/production-evaluator-integration-contract.md) approves the Google Gen AI SDK and Gemini Developer API seam, pinned `gemini-2.5-flash` serving baseline, QA-only model promotion, stage profiles, prompt/schema and retry ownership, immutable configuration capture, fail-closed technical correctness, and continue-without-coaching behavior. Every evaluator claim persists a typed resolved configuration manifest and canonical SHA-256 fingerprint; claim replay and terminal immutability include both. The conformed Google adapter implements all three stage profiles, code-owned system prompts, untrusted user-data envelopes, provider-compatible schemas, numeric thinking budgets, one-call transport, and safe provider/safety/schema error normalization. It now deterministically removes unsupported technical verdicts when no trusted reference was supplied and bounds generated prose before strict validation. The answer-analysis route selects it only from the exact server provider/profile/key contract and only invokes it after candidate ownership plus a durable evaluator-run claim. Candidate recovery now distinguishes pending, accepted-result repair, retryable failure, and unavailable terminal/capped states; accepted answers stay locked, accepted internal coaching can repair its projection without a currently available provider, and continue/finish without coaching cannot create coached-answer truth. Completed-round repair reuses that exact ownership, claim, runtime, and projection boundary for at most two eligible latest immutable attempts per request; malformed lineage and nonretryable/capped work fail closed, and Coach Update synthesis remains forbidden until every answered occurrence has accepted evaluator evidence. Completion performs repair best-effort, while an explicit dashboard action exposes truthful retry failure without making dashboard reads mutate state. The explicit live-validation gate uses seven synthetic fixed cases, the exact pinned Google profile, six independent opt-in controls, safe terminal capture, immutable configuration verification, derived gate counts, and ignored redacted artifacts. Final artifacts `live_eval_196fb6fd160ace41` and `live_eval_79d9b3263cd85691` each passed 7 of 7 under fingerprint `466f1d3d7f5395346ba8172b952c35c5a5f1a9f182f4ce2014495c208f26ae93`; comparison `live_compare_7a8db71ebade7af2` found all seven comparable, and Slice 124 ratified this profile as the first candidate-serving baseline. A disposable-DB candidate-route submission then proved one immutable attempt, one completed live generation, accepted internal stage artifacts, a matching candidate-safe projection, visible staged coaching, and refresh recovery. Earlier live failures were treated as contract findings: the provider now supplies exact quotes and bounded semantics while code owns immutable identity, offsets, marker/missing-evidence derivation, technical-reference gating, prose bounds, and sensitive intervention anchors. Earlier V2 development rows remain honestly `pre_manifest_v2`, and V1-created app data has no migration or runtime compatibility requirement. Technical-reference integration beyond supplied QA references, full production observability, and organizational deployment readiness remain pending.
- Candidate feedback now renders one stage at a time from candidate-safe evidence-first facts: acknowledgement, focused content coaching, optional delivery coaching, then next step. Every selected transition is persisted before it executes and is recovered only for the exact analyzed attempt. Continue/finish and skip retain candidate control. Retry is offered only when immutable attempt identity exists; choosing it reopens the preserved answer and a successful submit appends a linked attempt rather than overwriting history. Provider/analysis retry remains a new evaluator run on the same answer attempt.
- The ratified [evidence-first dashboard information architecture](./04-architecture/evidence-first-dashboard-information-architecture.md) makes opaque candidate-owned prep-context identity authoritative; distinguishes immediate in-session feedback from a versioned post-session Coach Update synthesis; limits Coach Update detail to practiced questions; keeps unanswered plan coverage in Coach Plan/Practice Next; and separates a durable editable next-round draft from immutable launch intents. Title-keyed scoping is now isolated to historical null-profile records; duplicate review surfaces and dashboard-local queue language remain compatibility scaffolds, not the final contract.
- Durable `candidate_next_round_drafts` provide one candidate-owned editable queue per opaque prep context, with normalized ordered source-question items, provenance, a 20-item capacity, and optimistic versioning. The selected-context dashboard now exposes its truthful count through a responsive practice builder and one shared queue affordance across Practice Next, Coach Update, and Coach Plan. Candidate-owned Coach Plan choices can be added, removed, reordered through accessible controls, or cleared with confirmation; every mutation replaces client state with the authoritative server draft and stale versions recover without silent overwrite. `public.snapshot_candidate_next_round_draft_to_intent(...)` revalidates evidence and exact item assembly, creates one immutable `practice_builder` intent, clears the draft items, and advances its version atomically. Duplicate launch recovers the same ready intent or consumed session, and successful launch stops at the existing `/candidate/practice/ready/[intentId]` landing. Queue-created session snapshots retain draft/version/item/provenance lineage, and follow-up question lineage resolves a canonical root across repeated follow-ups so attempt numbering does not fork or reset. One-question, unanswered-plan fixed-set, and fixed coach-bundle contracts create immutable intents without mutating the editable draft and still stop at the ready landing.
- Current persistence posture: dashboard meaning remains a read-time derivation until multi-round aggregation, query pressure, or analytics needs justify projections. Exact post-session Coach Update wording is the bounded exception: it is now a versioned source-linked operational artifact for stable replay and QA, not persisted preparedness truth.
- Coach Update synthesis now has a production-shaped provider-neutral runtime and a credentialed, reconciled Google serving profile over that artifact lifecycle. `google_gemini_2_5_flash_coach_update_v1` uses the existing server-only `GEMINI_API_KEY`, pinned `gemini-2.5-flash`, a code-owned system instruction, an explicitly untrusted JSON envelope, provider-compatible structured output, and one 12-second SDK transport call. The provider request contains bounded practiced-question, response-mode, and accepted candidate-safe coaching facts but excludes current/prior raw answers, candidate/launch identity, ownership ids, raw evaluator output, hidden plans, total unanswered coverage, JD/resume content, and assembled-prompt persistence. Google may write only title, summary, primary focus, and per-practiced-question comparison language; code attaches status/fingerprint and reattaches immutable answer/source identity, accepted coaching, question order, and comparison counts after strict schema, mapping, and prohibited-language validation. One artifact claim owns one transport attempt with no hidden application retry; replay identity includes immutable provider/model/prompt/evaluator plus exact profile/configuration fingerprint, and migration 019 leaves earlier V2 development rows null rather than inventing history. Terminal or abandoned claims recover through a later generation attempt against unchanged source facts, while completion and dashboard return stay best-effort. Runtime telemetry is metadata-only and production-wired; sink failure cannot alter synthesis. Server-environment fault modes remain explicit-local-only, and the deterministic fixture remains independent from answer-analysis selection. Missing or mismatched Google configuration leaves synthesis unavailable without falling back to fixture output. The synthetic credentialed gate and disposable-DB completion/replay/dashboard reconciliation pass; organizational deployment approval remains separate.

## Recommended Next Slices

The host-launch and candidate-coaching production-integration milestone is implemented and conditionally cleared for continued rebuild work. The next critical functional gap is production question wording; keep it behind the same exact-profile, privacy, failure, and live-validation discipline as answer evaluation and Coach Update.

134. Review V1 question generation, the original refactor prompt/context boundary, the current deterministic plan/wording contract, and the production evaluator/Coach Update integration patterns. Ratify and implement the first production question-wording provider profile with bounded role, JD, optional resume, stage, and plan-slot context; strict slot/order/schema validation; exact immutable configuration identity; fail-closed retry and recovery behavior; metadata-only telemetry; and an explicit credentialed synthetic gate. Preserve the production-shaped ready landing and do not begin final session/dashboard UI redesign, invited-flow wiring, or technical-reference retrieval.

## Open Decisions And Risks

- Production `/candidate/launch` now has a TA-only launch-context adapter, privacy-safe request-correlated diagnostics, and a credential-nonretaining acceptance probe. It remains fail-closed unless verifier, Postgres, and complete bounded TA MSSQL configuration are valid. Production acceptance still requires deployment network reachability, least-privilege credentials, shared-secret operations, and execution of the real TA-signed staging matrix. RW remains intentionally disabled until its candidate namespace is ratified.
- Setup staging is inaccessible after launch-session expiry and is deleted on successful session creation or existing-path selection, but physical cleanup of expired launch sessions remains an operational retention task. Live acceptance must prove refresh/new-tab persistence, stale-tab conflict after consume, and host-job snapshot stability.
- Host-launch replay, launch-token lifetime, and app-session lifetime boundaries are now implemented on the IC side: every raw token is fingerprinted, a fingerprint or issuer-scoped `jti` may create at most one launch session, accepted tokens require numeric `iat`/`exp` within the configured two-minute ceiling, and the IC cookie/session uses an independently configured maximum seven-day lifetime. The host must still confirm mint-per-click behavior, exact issuer/source-portal values, shared-secret exchange and rotation, and whether `product` remains the custom audience discriminator.
- Because the agreed launch shape carries the signed credential in a query parameter, host/CDN/proxy access logs must redact its value before production. The IC exchange response is `no-store` and `no-referrer`, but application code cannot retroactively sanitize upstream request logs.
- Historical tracked CSVs under `docs/candidate-app/09-dev` predate this milestone and include staging discovery rows. The current milestone adds no new raw export and ignores the new diagnostic CSV names, but repository-history review plus quarantine/removal of unnecessary candidate/job records is required before broader repository sharing or release.
- Identity-backed setup draft persistence remains pending; local browser setup preservation does not satisfy the production new-device requirement.
- Resume ingestion still needs product and technical decisions for upload, OCR/photo capture, PDF extraction, PII handling, storage, and when extracted text becomes the setup payload. Later resume revision under the same prep context must stage a versioned resume label/snapshot, preserve historical session inputs, and reconcile existing plan questions through candidate-approved slot-preserving replacements rather than silently rewriting them.
- Production question wording still needs its provider, prompt, credentials, retry/error behavior, observability, redaction, and QA policy. Answer analysis has a ratified first serving profile, credentialed artifacts, a disposable-DB candidate-route reconciliation, and bounded completed-round repair, but production observability and deployment readiness remain open.
- Coach Update has exact mocked conformance, a passing credentialed synthetic gate, and disposable-DB completion/replay/dashboard reconciliation under the first serving profile. Migration 019 makes profile/configuration identity part of immutable claim and replay matching. Deployment still requires organizational approval of account/data handling plus environment/secret/telemetry operations; any prompt, schema, model, timeout, or generation-setting correction must advance immutable profile/configuration identity and rerun the gate.
- The evaluator contract, provider-neutral runtime, durable fencing, immutable configuration identity, local route/browser/DB fault matrix, mocked Google adapter conformance, exact route selection, and bounded candidate recovery are landed. The ratified integration contract selects the Google Gen AI SDK through the Gemini Developer API seam, pinned `gemini-2.5-flash`, stage-specific profiles, immutable safe configuration capture, fail-closed technical correctness, and nonblocking unavailable behavior. Organizational approval of the production account, region, service terms, and candidate-data handling remains required. Provider work must satisfy the [development fault-injection and browser/DB failure matrix](./05-quality/evidence-first-evaluator-contract.md#production-provider-failure-validation-gate); mocked conformance and the deterministic fixture cannot prove live-provider behavior.
- Slot-keyed answer/analysis JSON remains a latest-result compatibility boundary. Fixture analysis records the accepted internal evaluator run against a fixed answer attempt, then projects only candidate-safe coaching into that JSON. Production provider wiring must preserve the same claim, validation, internal persistence, safe projection, and terminal lifecycle rather than bypassing it.
- The shared audio lifecycle has no production TTS adapter or authorization contract. Preserve the prefetch/play-once boundary while replacing V1's legacy candidate-token route with a V2 identity-owned transport.
- The final feedback stage executes the fixed candidate completion mutation and selected-context dashboard return behind one persistent completion overlay. Further visual polish can wait for the productized session pass; do not reintroduce intermediate feedback/button flashes while navigation is pending.
- Follow-up practice intent creation plus session creation works, but production transaction hardening around intent consume/session create remains pending.
- Queue launch requires one opaque prep context and one coherent staged setup snapshot. The product does not intentionally assemble a round across role profiles, stages, or resume postures. The current matching guard is therefore an integrity boundary, not an unresolved mixed-context product option. Future resume revisions must define which version is staged on the ready landing before queued questions are launched.
- Historical sessions and intents whose `role_profile_id` is null remain readable through isolated title-keyed compatibility behavior. New profile-backed work must never be grouped or selected by role title. Migration 010 backfills intent identity only when it can be traced to a candidate-owned source session; malformed legacy multi-context intents remain fail-closed at follow-up session creation.
- Historical null-profile Coach Plan references now remain discoverable through the complete candidate-owned dashboard history read, but their title-keyed grouping is still compatibility behavior rather than authoritative identity. Repairing or retiring null-profile dashboard contexts remains later migration work.
- Manual prep-context resolution detects candidate + normalized role + normalized JD hash matches but no longer silently reuses profiles with activity. `practice_path_number` permits explicit independent duplicate UUIDs without becoming user-facing identity or inherited lineage. Host-backed paths now use candidate + source platform + job collection id; optional requirement identity remains stored but is not yet part of TA uniqueness. Do not extend the manual key as a substitute for trusted platform identity.
- Candidate-initiated interview-stage changes remain unimplemented. The ratified direction is a new linked `role_profile_id` with blank evidence; it must not mutate the existing path or port practice history.
- Prep-context resolution and setup-session insertion are currently two durable statements rather than one transaction. A failed session insert can leave a candidate-owned profile with no practice activity; retry safely reuses it, and future selectors must not imply evidence from profile existence. Make the combined write transactional before production setup launch if empty profiles become user-visible or operational cleanup becomes necessary.
- Setup-session creation still lacks a request-level idempotency key. The UI suppresses ordinary duplicate submit, and prep-profile creation is concurrency-safe, but two accepted setup requests can create two sessions. Add setup-start idempotency before production network-retry and multi-tab acceptance testing.
- Direct one-question and fixed-set practice-intent creation still lacks a request-level idempotency key. Immediate client claims suppress rapid duplicate activation, and builder launch is atomically replay-safe, but a retried direct creation request can leave more than one equivalent ready intent. Add an explicit per-user-action idempotency contract before production network-retry and multi-instance acceptance testing without deduplicating later intentional repractice of the same question content.
- Dashboard correctness currently derives cross-role inventory and selected-context meaning from complete candidate-owned session history. This is deliberate while the product contract is still changing, but query volume and hydration cost must trigger a separately versioned summary/projection design before high-volume production use; do not reintroduce a silent recent-session cap.
- The stable dashboard shell, Coach Update debrief, Coach Plan reference, durable builder, and shared Practice Next action language are landed. The `coach_bundle` persistence/action boundary is executable, but candidate-facing bundle recommendations remain intentionally unbuilt until a trustworthy producer contract exists. Production synthesis/provider wiring, staged resume selection, coach-bundle generation, trend views, and multi-round aggregation remain unbuilt.
- Pending Coach Update entries are truthful but do not yet poll. Completed unavailable work has an explicit ownership-scoped retry action, but asynchronous refresh, bounded waiting, and safe background retry behavior must be designed before pending generation is accepted as production UX.
- A completed round with any answered question lacking an accepted evaluator run cannot assemble a Coach Update, by design; partial synthesis remains forbidden. Bounded completed-round repair now reuses the exact evaluator claim/runtime/projection boundary and can recover eligible work explicitly from the dashboard. Malformed lineage plus nonretryable or capped failures remain truthfully unavailable and require operational diagnosis rather than fabricated coaching.
- Coach Update claim recovery uses a two-minute lease around a synchronous 12-second serving budget. This is validated for the current profile; background or longer-running generation must ratify a new lease/heartbeat contract before it is enabled.
- The milestone dependency audit removed the Google SDK transitive critical/high findings and upgraded Next.js to `15.5.20`. A high `nodemailer` advisory requiring a major-version upgrade and a moderate PostCSS advisory bundled by the current Next.js line remain app-wide release-hardening gates. Reassess and resolve or formally accept them before pilot/release; do not treat this milestone commit as deployment approval.
- Candidate-visible attempt counts are deliberately deferred. Keep attempt lineage and rollups available in data/read models, but do not spend current UI scope on count chips, labels, or charts before the core session and dashboard surfaces are productized.
- `feedback_actions_json` preserves only the latest recovery action per question. Decide before analytics/BI work whether full feedback-stage engagement needs an append-only event boundary; immutable answer attempts already preserve actual retry behavior.
- Sensitive data can still be too visible in AI-quality/debug surfaces until masking/redaction work lands.
- Historical candidate implementation tests are not fully restored on the cleanroom branch; keep `npm run test:candidate` scoped to deliberately restored surfaces.

## Major Rebuild Phase Map

Phase A: Host launch, identity, and setup foundation. Status: the IC-side TA launch, setup-entry, and first-session contracts are implemented but still require live deployment acceptance. Landed: standard numeric-date HS256 verification through `jose`, issuer/product/source-portal checks, two-minute launch-token ceiling, fingerprint/`jti` replay rejection, identity-only and job-aware context shapes, independent seven-day app-session policy, launch route shell, dev host launch, launch-session schema/repository, exact `CandidateMaster` identity resolution, `CandidateJobCollectionTxn` ownership proof plus canonical `JobCollection` reads, bounded direct-MSSQL runtime configuration, server-selected setup/dashboard entry, immutable job-aware role/JD staging, identity-only manual first setup, separate host/manual prep identity, stale-tab mutation protection, atomic staging consume, setup contract/UI, ownership-scoped prep-context resolver, intentional independent same-role/JD profiles, activity-summary choice dialog, setup-session `role_profile_id` propagation, candidate-scoped browser draft preservation, and intentional setup reset behavior. Remaining: live TA signed-token/network validation, RW identity adapter, ratification of RequirementID in prep identity, shared-secret rotation operations, durable cross-device setup drafts, resume/OCR ingestion and revision reconciliation, interview-stage lineage, expired-launch retention operations, and production access/session details.

Phase B: Planned session and question generation foundation. Status: scaffolded with deterministic planning and fixture wording. Landed: setup-to-session handoff, carried question-plan snapshots, provider-free wording contract, strict slot-mapped wording validation, fixture wording, production-shaped pre-session landing, and migration from obsolete preview progress into live practice. Remaining: production question-wording provider, prompt/context policy, retry/error states, and observability.

Phase C: Live session runtime and answer-analysis foundation. Status: candidate-led session arc browser-validated through fixture and live Gemini coaching, interruption, recovery, immutable retry, follow-up practice, skip, completion, and role-scoped dashboard return. Landed: shared runtime vocabulary and live-practice component, shared answer-mutation presentation states, candidate/invited completion adapters, dashboard exit without progress reset, exact-question recovery, serialized durable answer autosave, browser-bridge draft preservation, typed answer lifecycle, idempotent submit/analysis boundaries with failed-work cleanup and stale-pending recovery, analysis-only retry after answer acceptance, normalized immutable answer attempts, generation- and lease-fenced evaluator-run lifecycle persistence, provider-neutral extractor/verifier/composer runtime with bounded retries, internal accepted-run persistence and provider-independent repair, pending/retryable/recoverable/unavailable candidate states, bounded continue/finish without coaching, explicit local failure/recovery orchestration, persisted/recovered staged feedback actions, feedback-triggered immutable attempt append, immutable first-write candidate-led completion snapshots, ownership-scoped bounded completed-round evaluator repair, the shared audio lifecycle seam, two credentialed synthetic live gates, one durable live-provider route reconciliation, and ratification of the first serving profile. Remaining: technical-reference source, production TTS transport, media analysis, post-round presentation refinement, and invited route wiring.

Phase D: Candidate dashboard, Coach Update, and follow-up practice. Status: the profile-scoped dashboard/follow-up milestone passed its senior audit, and the first Coach Update serving profile passed credentialed synthetic plus durable route/DB/dashboard reconciliation. Landed: dashboard read model, complete candidate-owned context inventory, profile-backed candidate/context header, active-round priority, Plan Progress versus Practice from Feedback split, one artifact-backed Coach Update entry/detail, focused-practice intent pointers, plan-aware multi-question intent creation, durable one-or-many practice intents, follow-up staging/session creation, attempt lineage rollups, evidence-first dashboard/queue decision, candidate-owned `role_profile_id` propagation through intents and follow-up sessions, canonical opaque dashboard navigation, accepted-evidence synthesis inputs, deterministic local Coach Update fixture, production-shaped provider-neutral synthesis runtime, exact Google provider/profile/key selection, code-owned prompt and untrusted-data separation, provider-compatible structured output, strict code-owned artifact hydration, configuration-aware immutable artifact claims/replay, production-wired metadata-only telemetry, development fault injection, stale-input rejection, candidate-safe same-question comparison context, practiced-only artifact-backed dashboard reads, normalized candidate/prep-context queue drafts, optimistic add/remove/reorder/clear mutations, responsive builder interactions, cross-surface queue truth, one-question/fixed-set/builder action separation, stale-version recovery with candidate notice, exact source-session resolution outside recency windows, and atomic idempotent queue-to-intent launch through the ready landing. Remaining: direct fixed-intent request idempotency, staged resume-version selection and later question reconciliation, trusted coach-bundle generation, multi-round progress/coverage, later trend presentation, justified projections, and deployment operations/approval.

Phase E: QA/evaluation and model comparison. Status: the typed evaluator, persistence, pinned Google adapter, two credentialed live/golden artifacts, same-profile comparison, and durable candidate-route reconciliation are landed. Landed: normalized fenced evaluator-run persistence, ratified evidence-first and production-integration policy, provider-neutral stage runtime, accepted local fixture capture, local failure/recovery matrix, immutable configuration manifests/fingerprints, mocked Google conformance, exact route selection, bounded candidate recovery, seven synthetic edge cases, exact live opt-in guards, safe failure capture, configuration/artifact integrity validation, redacted ignored run artifacts, two passing credentialed runs, same-profile repeatability comparison, and a future different-profile A/B shape with no automatic judgment. Remaining: human case review and serving decision, approved QA-only comparison profiles, reviewer annotation workflow/UI, technical-reference policy/source, any justified raw-payload retention exception, operational masking, and explicit production promotion/rollback evidence.

Phase F: Shared invited/candidate runtime convergence. Status: first shared runtime boundary landed. The live-practice shell and completion/return adapters are audience-neutral; invited initials, landing copy, route/auth, permissions, employer visibility, and recruiter-created session constraints remain separate and largely unwired.

Phase G: Recruiter/admin route rebuild or preservation decision. Status: deferred. Future work should decide which recruiter surfaces remain from V1, which move under `/recruiter/*`, and which need V2 shared read models rather than direct legacy mutation paths.

Phase H: Production hardening, privacy, accessibility, and observability. Status: ongoing risk stream. Future work includes host-launch security, audit/event strategy, sensitive-data masking, provider failure handling, accessibility/performance passes, route-level error recovery, deployment env docs, and release validation.

Phase I: Retirement and cleanup. Status: ongoing discipline. Future work includes removing temporary redirects, stale V1 routes/helpers, provisional browser bridges that have durable replacements, fixture-only paths, obsolete tests/docs, and historical scripts once replacement behavior is verified.

## Recent Completed Slices

133. Audit the Slices 125-132 production-integration milestone. Reconstructed the boundary from commit `a8dd8f0`, reviewed the integrated host-launch/setup/session/Coach Update journey, token and app-session trust, identity/job ownership, replay, secret/log/privacy posture, exact evaluator and synthesis configuration identity, migration order, failure recovery, live evidence, runbooks, and release gates. The audit found and fixed one real clean-install defect: the Coach Update configuration-identity smoke depended on pre-existing development data, so validation 020 now creates and rolls back its own exact-profile fixture. A true empty-database readiness run and the existing upgrade database both pass the complete 001-019 migration/smoke/seed chain. Supply-chain review upgraded Next.js to `15.5.20` and patched the newly introduced Google SDK transitive critical/high findings; the inherited `nodemailer` high and Next-bundled PostCSS moderate advisories remain named release gates. Staging-derived CSV exports are ignored and remain local-only. Verification passed 543 candidate tests, 40 host-launch tests, 58 host-setup tests, 126 evaluator-configuration tests, 126 Coach Update tests, 49 next-round tests, typecheck, scoped lint with zero warnings, production compilation, both database readiness paths, and `git diff --check`. Full repository lint remains blocked only by unrelated errors in untracked `src/app/candidate/dashboard-demo/page.tsx`. Verdict: conditional for deployment, ready for the next rebuild slice and a clean code commit; live TA staging acceptance, least-privilege/network/secret operations, organizational Coach Update deployment approval, and the named dependency gates remain external release conditions.

132. Execute and reconcile the first credentialed Coach Update gate. Added a six-control opt-in synthetic harness for `google_gemini_2_5_flash_coach_update_v1`, exact request/configuration inspection, privacy-key scanning, safe accepted/failure artifacts, and a credential-nonretaining CLI/runbook. Final artifact `live_coach_update_f18cc81d46d244d5` made one accepted Gemini call in 3.5 seconds under configuration fingerprint `c98abf8bce3ce0f407b1db0e6359a0b85775adb789a2aaae44fce2dd679e6859`; automated checks and human review accepted its grounded first-practice/repeat-practice language with no score, ranking, or hiring claim. Disposable-DB reconciliation proved best-effort completion, accepted-evidence gating, one exact-profile provider call, immutable artifact persistence, replay without another generation or telemetry event, unavailable behavior for superseded evaluator evidence, and two refreshed dashboard reads recovering live content. The senior pass found and fixed two configuration-identity gaps: profile/configuration had existed only in terminal validation, and the manifest labeled but did not hash the actual system instruction. Migration 019 now persists exact profile/configuration fields in new claims, enforces them on future inserts while preserving honest null history, uses them in replay matching, and freezes them at terminal transition; the manifest fingerprints the instruction, response schema, and generation settings. The final durable run created generation 4 with matching physical/validation fingerprints; identical replay stayed at generation 4 with one current-profile runtime event. Verification passed the focused Coach Update suite, all 543 candidate tests, typecheck, focused lint, the complete DB migration/readiness chain, production compilation, and the live/durable protocols. The build's final lint gate remains blocked only by unrelated errors in untracked `src/app/candidate/dashboard-demo/page.tsx`. No second profile, technical-reference retrieval, background job, TA traffic, or final UI redesign was introduced.

131. Wire the first production Coach Update synthesis adapter. Preserved V1's best-effort completion side effect and useful session-level synthesis intent; reinterpreted it as one structured language pass over accepted candidate-safe evaluator facts; retired raw transcript/JD injection, hidden-score patterning, Markdown output, provider-owned identity/evidence, stock fallback success prose, and prompt/raw-output retention; and deferred credentialed live reconciliation, technical-reference retrieval, background execution, and final dashboard visual work. The exact `google_gemini_2_5_flash_coach_update_v1` profile selects only through `CANDIDATE_COACH_UPDATE_PROVIDER=google_genai`, the matching profile id, and server-only `GEMINI_API_KEY`. It pins `gemini-2.5-flash`, the code-owned prompt/schema/settings, and evaluator contract metadata; sends one explicitly untrusted JSON envelope containing no current/prior raw answer, candidate/session/prep identity, credential, JD/resume, raw evaluator output, or hidden plan; and permits Google to write only bounded title, summary, primary-focus, and same-order question-comparison language. Code attaches status/fingerprint and rehydrates immutable lineage after strict schema, mapping, and prohibited-language validation. One artifact claim still owns one bounded 12-second transport call; replay/concurrent claims make zero provider calls, stale source is rejected, partial evaluator evidence makes zero provider calls, and timeout/rate-limit/4xx/5xx/safety/malformed responses receive safe terminal classification. Production metadata-only telemetry is wired and diagnostic failure cannot control synthesis or completion. Missing or mismatched production configuration leaves synthesis unavailable and cannot fall back to fixture behavior; fixture/fault selection remains explicit-local-only. The senior pass corrected an initial configuration-assembly hazard that could have turned a missing production key into a completion-route 500. Verification passed 115 focused Coach Update tests, all 543 candidate regression tests, typecheck, focused lint with zero warnings, production compilation, and `git diff --check`. Full repository lint and the build's final lint gate remain blocked only by pre-existing errors in untracked `src/app/candidate/dashboard-demo/page.tsx`; no migration or DB smoke was required, and no credentialed Coach Update call was made.

130. Land ownership-scoped completed-round evaluator repair. Preserved V1's ownership-gated mutation, deterministic replay, and useful missing-summary regeneration intent; reinterpreted repair as exact latest immutable answer-attempt recovery through V2's existing evaluator claim/runtime boundary; retired mutable answer overwrite and whole-session regeneration; and deferred technical-reference retrieval, production Coach Update provider wiring, background jobs, and final dashboard styling. Candidate-led completion now repairs at most two eligible latest attempts per request before Coach Update synthesis, restores a missing candidate-safe projection from an accepted internal run without a provider, retries only policy-recoverable work under the current serving configuration, and keeps malformed lineage plus nonretryable/capped results unavailable. Completion remains durable and best-effort when repair or synthesis fails. An ownership-scoped completed-session POST action gives the unavailable Coach Update card an explicit retry without introducing dashboard-GET side effects; it returns a truthful failure when accepted evaluator evidence still cannot produce an artifact. Full-set gating prevents partial artifacts, existing run claims make refresh/concurrent requests converge, and metadata-only request-correlated diagnostics cannot alter behavior. The senior pass found no unresolved in-scope correctness issue. Verification passed all 543 candidate tests, including 102 focused Coach Update/completion/repair tests, plus typecheck, focused lint with zero warnings, and production compilation. Full repository lint and the build's final lint gate remain blocked only by pre-existing errors in untracked `src/app/candidate/dashboard-demo/page.tsx`; no migration or DB smoke was required because the slice changes no schema.

129. Build the deployment-run TA host-launch acceptance protocol and metadata-only probe. Added a canonical staging matrix for new and returning identity-only launch, owned job-aware launch, replay, expiry, wrong product/issuer/source portal, unowned job, refresh/new-tab staging, atomic consume, stale-tab rejection, and database reconciliation. The `qa:candidate:host-launch` CLI accepts only an enumerated case plus flags, reads one host-minted URL through hidden standard input, rejects unsigned query additions and non-HTTPS deployment targets, performs bounded manual-redirect requests, never reads response bodies, never emits or writes token/cookie/candidate/job content, and reports only status, canonical route category, cookie-attribute booleans, safe failure codes, and random request ids. Production launch now emits request-correlated allowlisted assembly, exact verifier, and final exchange diagnostics while keeping browser rejection generic; diagnostic sink failure cannot alter verification or cookie issuance. The probe treats destination 4xx/5xx as failure, verifies clean privacy headers and HTTPS cookie posture, and reuses a valid token only inside explicit replay cases. Preserved V2's fail-closed one-time exchange and server-owned routing; V1 had no production launch acceptance behavior to preserve. Deferred actual TA staging execution, upstream query-log redaction proof, shared-secret operations, deployment network/least-privilege credential proof, RW, and authoritative host resume. Verification passed 40 focused host-launch tests, all 528 existing candidate tests, CLI help and safe invalid-input execution, focused lint with zero warnings, typecheck, production compilation, and `git diff --check`. The build's final lint gate remains blocked only by pre-existing errors in untracked `src/app/candidate/dashboard-demo/page.tsx`; no real host token was available, so the live matrix remains an external release gate rather than claimed acceptance.

128. Carry host context into the candidate journey. Closed the integration contract assembled by Slices 125-127 without adding duplicate runtime paths: an identity-only launch opens setup for a candidate with no prep contexts and the dashboard for a returning candidate; an owned job-aware launch opens setup with canonical role and JD staged server-side and rendered read-only while stage, question count, and optional candidate-supplied resume remain editable; and first-session creation preserves host-backed prep identity through source platform, job collection id, optional requirement id, and source launch-session lineage. Candidate identity lineage remains separately traceable through provider, issuer, subject, workspace/source portal, and platform candidate identifiers plus launch-session source metadata. Authoritative host resume selection remains deliberately deferred; candidate-entered resume context is not mislabeled as host-provided. Existing focused resolver, repository, setup-entry, page, and start-route tests prove the journey branches, ownership, mutation rejection, refresh/new-tab staging, and atomic consume behavior. V1 had no verified host-launch journey to preserve; its unauthenticated/manual entry was retained only in explicit development behavior and reinterpreted behind verified candidate ownership for production. The senior completion pass found and corrected a zero-argument Vitest mock inference that made the integrated MSSQL test fail TypeScript despite passing at runtime. Verification passed 59 focused journey tests, all 526 candidate tests, the corrected 4-test MSSQL runtime file, focused lint with zero warnings, typecheck, and `git diff --check`. No database migration rerun was required because this slice changes no schema; live host/network acceptance remains Slice 129.

127. Implement setup entry policy after a successful TA launch. Preserved authenticated manual setup and candidate-owned duplicate choice; reinterpreted signed TA job context as immutable transient setup input rather than automatic prep/session creation; retired the production trust guard that stranded first-time identity-only candidates; and deferred resume retrieval, RW, cross-device drafts, RequirementID uniqueness, and live TA exchange validation. Production entry routing is server-owned: job-aware launch enters setup, identity-only launch enters setup only when no prep contexts exist, and otherwise enters the dashboard. Job-aware launch stages bounded canonical role/JD atomically with its launch session, outside URL/cookie/compact context JSON. Setup renders those fields read-only, re-resolves them server-side, rejects mutation and stale-tab reuse, and consumes staging only with first-session creation or explicit existing-path selection. Host paths use candidate + source platform + job collection id while manual paths retain normalized role/JD identity; browser draft keys use the same host context boundary so resume/stage/count from another role cannot bleed into the launch. The senior pass added database-enforced staging ownership, activity-backed existing-path consume, and corrected fresh-install migration order. Verification passed all 526 candidate tests, 58 focused host-setup tests, typecheck, focused lint, migration 018 apply/smoke on the working database, a complete migration chain plus transactional smoke on a fresh disposable database, production compilation, and `git diff --check`. Full repository lint and the build's final lint gate remain blocked only by pre-existing errors in untracked `src/app/candidate/dashboard-demo/page.tsx`; no live TA DB or signed-token browser validation was possible from this machine.

126. Implement the TA-first server-only MSSQL launch-context adapter. Preserved the injected lookup seam, identity-only dashboard launch, compact Postgres session snapshot, and fail-closed orchestration; reinterpreted direct host access as one narrow TA server adapter; retired the discovery draft's requirement/channel/resume/consent joins from the launch-critical path; and deferred RW, resume selection, trusted job-to-setup staging, deployment network validation, and live signed-token exchange. The adapter validates positive SQL `Int` identifiers before connection, requires exactly one `CandidateMaster` row, proves job ownership only through `CandidateJobCollectionTxn`, reads canonical context only from TA `JobCollection`, treats active/expired state as context rather than ownership, and never downgrades an invalid or unowned requested job to identity-only success. The runtime uses parameterized queries, one bounded shared MSSQL pool, strict connection/request timeouts, complete fail-closed environment assembly, and diagnostics limited to operation plus bounded reason. `CandidateMaster.CreatedBy` is deliberately not treated as host user identity. Production assembly remains TA-only and now reuses one bounded Postgres pool. The senior pass also made returning launches refresh canonical profile attributes and identity `last_seen_at`, reject subject-to-candidate relinking, keep disabled profiles closed, and avoid partial profile creation when durable mappings disagree. Resume and consent absence is no longer fabricated as false state. Verification passed 513 candidate tests, 34 focused adapter/runtime/orchestration tests, 19 focused identity/session tests, typecheck, focused host-launch lint, production compilation, and `git diff --check`. Full-repository lint and the build's final lint gate remain blocked only by pre-existing errors in the untracked `src/app/candidate/dashboard-demo/page.tsx`; no live TA DB or signed-token test was possible from this machine.

125. Harden the host-launch token exchange and app-session boundary. Replaced the custom production JWT parser with `jose` HS256 verification; required standard numeric `iat` and `exp`; enforced exact issuer/product, optional source-portal consistency, a default two-minute token lifetime ceiling, and bounded future-issued tolerance; carried optional `jti`; and retained telemetry-safe failure reasons without logging token or claim content. Reinterpreted host launch as two valid entry shapes: candidate identity alone for a dashboard quick-link, or candidate plus optional job context for job-aware entry. The launch token is now a one-time exchange credential: code hashes the normalized raw token with SHA-256, persistence stores only that fingerprint plus optional issuer-scoped `jti` and token expiry, unique constraints reject replay, and resolver/route contracts propagate replay without setting a cookie. Interview Coach session expiry is independently calculated from a configurable policy with a seven-day maximum instead of inheriting the remaining token lifetime. The exchange redirect is explicitly non-cacheable and suppresses referrer propagation; upstream access-log query redaction remains a deployment obligation. Migration 017 makes `job_collection_id` nullable, adds replay metadata, and preserves earlier rows without invented fingerprints. Preserved dev launch and clean token-stripping redirects; deferred TA/RW MSSQL lookup, resume selection, host minting/rotation, and live signed-token validation. Verification passed 61 focused completion tests, all 500 candidate tests, typecheck, migration 017 apply, rollback-only replay/database smoke, and `git diff --check`. The production bundle compiled successfully, but repository lint and the build's final lint gate are currently blocked by pre-existing errors in the untracked `src/app/candidate/dashboard-demo/page.tsx`; that separate prototype was not modified as part of this security slice.

124. Ratify the first candidate-serving evaluator baseline. Human review accepted the current `google_gemini_2_5_flash_v1` profile and live candidate-facing coaching as the first V2 serving baseline after both final seven-case artifacts passed; this is a profile decision, not automatic deployment approval. The immutable profile/configuration fingerprint and existing validation gates remain the rollback boundary, and any prompt or adapter revision must advance immutable configuration identity and rerun the same gates. The completed-round evaluator-repair path is retained as a required pre-production recovery item because partial Coach Update artifacts remain forbidden. Technical-reference retrieval, Coach Update provider wiring, organizational account/data-handling approval, and a different-profile A/B experiment remain separate work.

123. Execute and reconcile the credentialed live evaluator gate. Ran the exact `google_gemini_2_5_flash_v1` profile against all seven synthetic golden cases, reconciled a disposable-DB route submission, and then used broader manual browser protocols to probe candidate-serving behavior. Browser and repeated credentialed findings exposed contradictory intervention composition, a grounded school-grade outcome mistaken for coach scoring, evidence ids attached to an unobserved signal, a correctly contradicted technical claim misread as an unsupported extractor conclusion, opaque schema diagnostics, a technical verdict invented without a supplied reference, and provider prose exceeding strict application bounds. Those failures remain ignored local evidence and drove durable boundary corrections: code now normalizes usable affirm-plus-upgrade responses, clears invalid citations, distinguishes candidate outcomes from coach scoring, emits content-free schema issue paths, separates verifier triggers from unsupported conclusions, forces no-reference technical accuracy to `not_assessed`, and sentence/word-clips generated prose before strict validation. The current validated contract is evaluator `candidate_evidence_first_v2`, prompt bundle `candidate_evidence_first_prompts_v6`, Google adapter `google_genai_evidence_first_adapter_v10`, and fingerprint `466f1d3d7f5395346ba8172b952c35c5a5f1a9f182f4ce2014495c208f26ae93`. Final artifacts `live_eval_196fb6fd160ace41` and `live_eval_79d9b3263cd85691` each accepted and passed 7 of 7; comparison `live_compare_7a8db71ebade7af2` found all seven comparable and left only the intentionally unresolved human-review flag. Earlier fingerprints and failed artifacts are superseded for serving-profile review. The route still persists one immutable attempt, durable generation lineage, exact manifest/fingerprint, accepted internal artifacts, a candidate-safe projection, staged coaching, and exact refresh recovery without keys, prompts, raw provider output, or hidden evaluator facts in session state. Manual completion also exposed and fixed an unrelated local-runtime coupling: explicit local development now selects the deterministic Coach Update fixture independently from the answer-analysis provider, and replaying the completed session produced the missing accepted artifact. The broader network-free suite, evaluator and Coach Update suites, typecheck, lint, DB readiness chain, and diff check pass. A production build was not rerun while the active dev server owned `.next`; it remains required before merge or deployment review. No human serving-profile promotion, second profile, technical-reference service, Coach Update provider, or deployment approval is implied.

122. Add the explicit opt-in live evaluator and first redacted golden/QA comparison path. Preserved the refactor plan's minimum edge-case gate, same-input review, exact-span/safety assertions, and V2's pinned provider-neutral runtime; reinterpreted QA capture as synthetic source cases plus ignored privacy-minimized review artifacts; retired the unused candidate-session QA export as a live-validation dependency because it carries raw case text and operational ids; and deferred credentialed execution, candidate-route/DB reconciliation, a second QA-only model profile, human reviewer mutation/UI, technical-reference retrieval, Coach Update synthesis, and serving promotion. Live execution requires the dedicated command, CLI confirmation, affirmative environment flag, exact provider, exact profile, and server-only key before evaluator assembly. Seven fixed cases cover thin, off-topic, sensitive, transferable, typed/voice fairness, and confidently wrong technical behavior. Artifacts capture only configuration identity, safe stage outcomes, deterministic appraisal summaries, validation facts, latency/tokens, and candidate-safe coaching; schema refinements verify configuration fingerprints, projection fingerprints, unique cases, and derived counts. Offline comparison makes no provider call, distinguishes repeatability from future profile A/B, preserves both candidate-safe projections, and leaves preference `not_reviewed`. The senior pass added exclusive artifact writes, manifest/fingerprint verification, derived summary integrity, future-profile comparison compatibility without weakening pinned execution, and safe unknown-failure capture. Verification passed 486 candidate tests, 118 evaluator-configuration tests, 89 Coach Update tests, 49 next-round tests, 13 focused live-harness tests, lint, typecheck, production build, the fail-closed no-confirmation CLI check, and `git diff --check`. No live credential was supplied, so no Gemini call or live artifact was produced.

121. Land bounded evaluator recovery and continue without coaching. Preserved V1's useful answer-before-analysis continuity; reinterpreted fallback feedback as explicit pending, recoverable, retryable, or unavailable candidate-safe states over immutable V2 evaluator runs; retired fabricated coaching, generic endless retry, and UI-only retry enforcement; and deferred live provider calls, technical-reference retrieval, coaching-content changes, production observability, and Coach Update provider synthesis. Accepted answers remain locked. A fresh claim offers status check without duplicate provider work; an accepted internal result repairs its candidate-safe session projection without another provider call or current provider availability; transient failures may claim a later generation only when persisted policy allows it; and configuration, safety, validation, malformed-completed-result, nonretryable, or exhausted-cap outcomes let the candidate continue or finish without coaching. The ownership-scoped claim query now blocks direct retries after nonretryable terminal results and caps candidate coaching at three generations per answer attempt in ten minutes while leaving QA comparison uncapped. Refresh and second-tab recovery derive only bounded capabilities, never provider detail. The senior pass additionally fixed runtime-configuration recovery drift and provider-independent accepted-result restoration. Verification passed 473 candidate tests, 105 evaluator-configuration tests, 89 Coach Update tests, 49 next-round tests, lint, typecheck, production build, the rollback-only evaluator-claims DB smoke, 83 focused recovery tests, and `git diff --check`. Live credentials/provider and browser fault validation were intentionally not performed.

120. Wire the conformed Google evaluator into the candidate answer-analysis route. Preserved V1's useful answer-before-analysis durability and V2's candidate/session/attempt ownership, durable evaluator claim, immutable configuration identity, provider-neutral runtime, run-id/fingerprint completion fence, and candidate-safe projection boundary; reinterpreted provider selection as an exact server provider/profile/key assembly; retired any direct production transport path that could bypass the evaluator claim; and deferred live credentials/model calls, technical references, bounded candidate failure UI, production observability, and Coach Update provider wiring. The Google bridge refuses work without the claimed evaluator-run id. Invalid configuration, failed ownership, fresh replay, and completed projection repair make zero provider calls. Accepted internal results complete before the session projection; terminal failures persist bounded safe metadata and return no provider detail. Explicit local fixture/fault modes remain nonproduction-only. Mocked route integration proves exact assembly, claim-before-provider ordering, success, replay, safe terminal failure, runtime-owned retry, and key nonexposure. Verification passed 454 candidate tests, 90 evaluator-configuration tests, 85 Coach Update tests, 49 next-round tests, lint, typecheck, production build, and `git diff --check`. No database schema or candidate UI changed; live provider, browser, and DB validation were intentionally not performed.

119. Implement the Google evaluator adapter conformance boundary. Preserved V1's exercised Google Gen AI SDK and pinned `gemini-2.5-flash` seam; reinterpreted its monolithic prompt/call as three strict stage adapters over the provider-neutral V2 runtime; retired candidate-data prompt concatenation, model-owned evaluation meaning, fallback success, raw prompt/output capture, and adapter-owned retries; and deferred route selection, credentials/live calls, technical references, candidate failure UI, and Coach Update provider wiring. The code-owned `google_gemini_2_5_flash_v1` profile records stage prompt/schema versions, temperature, output cap, candidate count, seed, numeric Gemini 2.5 thinking budget, and disabled thought return in the immutable configuration manifest. Renderers ignore caller policy values, keep candidate/JD/resume/reference data inside marked untrusted user JSON, and request a sanitized provider-supported subset of the strict application schemas. Each invocation makes one standard `models.generateContent` transport call with runtime abort/timeout; JSON, schema, finish-reason, safety, HTTP, auth, timeout, and unknown failures map to bounded safe classes without retaining provider detail. Mocked conformance proves all three stages, optional missing token metadata, application-strict output rejection, nonretryable safety rejection, environment/config fail-closed assembly, and runtime-owned invalid-schema retry. Verification passed 447 candidate tests, 80 evaluator-configuration tests including 19 Google conformance cases, 78 Coach Update tests, 49 next-round tests, lint, typecheck, production build, and `git diff --check`. No candidate-serving behavior or database schema changed, so browser and DB validation are not required.

118. Persist immutable evaluator configuration identity at claim time. Defined one typed resolved manifest spanning pipeline profile, service mode, adapter, prompt bundle, evaluator, stage provider/model/schema, and request-affecting generation settings, with deterministic stages represented truthfully. Its canonical SHA-256 fingerprint and full bounded manifest are persisted before provider work begins, participate in claim replay/conflict, remain immutable through terminal transitions, and are exposed through evaluator-run records and evidence-first QA capture. Migration 016 marks only evaluator rows created during earlier V2 rebuild slices as `pre_manifest_v2` with unknown settings rather than inventing reproducibility; every future insert must be resolved. V1-created app data is explicitly outside migration and runtime compatibility. Repository/schema negative tests cover inconsistent fingerprints, nonresolved future claims, and configuration mutation. Verification passed 447 candidate tests, 61 focused configuration tests, 78 Coach Update tests, 49 next-round tests, lint, typecheck, production build, the full disposable-DB readiness chain through migration 016, and `git diff --check`. No candidate UI changed, so browser validation is not required.

117. Ratify the production evaluator integration contract. Reviewed V1 provider coupling, the original refactor plan, the landed evidence-first runtime, failure matrix, persistence boundaries, and current Gemini seam. Selected the official Google Gen AI SDK through Gemini Developer API for the first serving profile, pinned `gemini-2.5-flash`, separated Vertex AI as a future explicit service profile rather than a fallback, assigned prompt/schema/retry ownership, required structured stage outputs and immutable safe configuration capture, preserved fail-closed technical correctness, and defined continue-without-coaching behavior plus QA promotion/release gates. Live calls, credentials, technical-reference retrieval, and candidate UI changes remain deferred to bounded implementation slices.

116. Prove the evaluator orchestration failure and recovery matrix. Preserved V1's answer-first durability, idempotent replay, and locked-answer recovery; reinterpreted retry as a new fenced evaluator generation for the same immutable answer; retired request-only failure simulation and compatibility writes that could race an active claim; and deferred live provider/model credentials, technical-reference integration, and final persistent-unavailable UI. An explicit local-only server-environment harness covers timeout, rate limit, provider 5xx/unavailable, misconfiguration, malformed extraction/feedback, fingerprint/span mismatch, unsafe inference, and verifier rejection through the real evaluator runtime. Route/repository tests prove one provider call under concurrency, pending-idempotency preservation, stale and late-result fencing, completed-run projection repair without another provider call, and generation-two recovery after terminal failure. Browser validation proved one durable answer survives failure, refresh, and a second tab; analysis-only retry then produced coaching without resubmission. DB reconciliation confirmed one answer attempt, two evaluator generations, one failed and one completed run, redacted accepted artifacts, and no hidden evaluator facts in session state. Verification passed 445 candidate tests, 78 Coach Update tests, 49 next-round tests, lint, typecheck, production build, migration 015 apply/smoke, and `git diff --check`.

115. Establish the provider-neutral evidence-first evaluator runtime. Reviewed V1's exact candidate/session/question context assembly, answer-before-analysis durability, retry surfaces, modality awareness, and QA metadata against the ratified V2 evaluator contract. Preserved those useful boundaries; reinterpreted V1's monolithic model judgment as extractor -> code appraisal -> conditional verifier -> feedback composer -> code validation; retired scores, model-owned next action, fallback success, raw prompt/output persistence, and hidden evaluator facts in session state; and deferred live provider/model choice, credentials, technical-reference ownership, development fault injection, media input, and QA UI. The runtime defines structured one-transport-attempt adapter ports, a structured verifier task, a hard 45-second aggregate budget, 12-second stage ceilings, two extractor/two composer attempts, one verifier attempt, safe-code failure classification, allowlisted re-extraction, strict accepted-run replay validation, and bounded token/latency metadata. The deterministic fixture now traverses this runtime. Accepted extraction, criteria, pattern gap, verification, hidden feedback plan, and stage metadata persist only in the fenced evaluator-run result; the session receives candidate feedback plus one interaction directive. Legacy snapshots remain readable but are projected down. Coach Update reads both accepted internal runs and bounded legacy results. Answer submit now shares the evaluator's 20,000-character ceiling. Verification passed 423 candidate tests, 72 Coach Update tests, 49 next-round tests, lint, typecheck, production build, and `git diff --check`. No migration or browser validation was required for this behavior-preserving runtime slice.

114. Fence durable evaluator-run claims and terminal retries. Reviewed V1's candidate-owned analysis route, answer-before-analysis persistence, provider validation, retry surfaces, and QA metadata against the ratified V2 evidence-first contract. Preserved exact answer/session ownership, immutable answer attempts, completed replay, candidate-controlled analysis recovery, and provider/version lineage; reinterpreted route idempotency as sequential evaluator generations under a durable lease; retired one-key-only run uniqueness and duplicate provider work on a fresh replay; and deferred extractor/verifier/composer execution, provider/model choice, live credentials, technical references, full fault injection, and UI refinement. Migration 015 adds positive generations, exact 60-second leases over the 45-second runtime ceiling, candidate-coaching requested/completed uniqueness, immutable generation/lease metadata, and backfill that preserves historical `updated_at`. The repository claims under an ownership-scoped advisory lock, terminalizes stale work, allocates the next generation with the same logical key when appropriate, replays matching requested/completed work, rejects mismatched active configuration, and uses the run id plus database clock as the completion fence. The route makes zero provider calls for a replayed fresh claim. Rollback-only DB smoke proves terminal retry, late completion rejection, one accepted result, immutable generations, migration rerun safety, and the full candidate readiness chain. Verification passed 415 candidate tests, 71 Coach Update tests, 49 next-round tests, lint, typecheck, production build, and `git diff --check`. No candidate UI changed, so browser validation is not required for this workpass.

113. Establish the production-shaped Coach Update synthesis runtime. Reviewed V1's session-summary generation/finalization, the original refactor persistence and telemetry guardrails, the accepted-evidence artifact lifecycle, and the cleared Phase D dashboard contract. Preserved best-effort post-completion generation, stable replay, and candidate-safe unavailable states; reinterpreted V1's whole-session score-driven Markdown summary as a strict structured language pass over accepted evaluator facts; retired provider-owned evidence identity, hidden-score inputs, stock fallback prose, raw answer/prompt/output exposure, and unbounded recent-session comparison reads; and deferred provider/model choice, prompt rendering, live credentials, and provider-specific SDK wiring. The provider-neutral request excludes current/prior raw answers, candidate/launch identity, ownership ids, raw evaluator output, hidden plans, and unanswered plan coverage. Code hydrates immutable question, answer, source, accepted-coaching, and comparison facts after exact schema/fingerprint/order/safety validation. One artifact claim owns one 12-second transport attempt; pending claims replay without concurrent provider work, terminal or abandoned claims can produce a later generation attempt, and a late provider result has no repository capability. Metadata-only telemetry and explicit nonproduction server-environment fault modes cover success, timeout, rate limiting, provider/config failure, malformed JSON/schema, fingerprint/question drift, and unsafe language. Session completion remains successful when synthesis fails, complete candidate-owned session history feeds comparison assembly, and dashboard pending/unavailable meaning is unchanged. Verification passed 409 candidate tests, 70 Coach Update tests, 49 next-round tests, lint, typecheck, production build, and `git diff --check`; no migration or DB smoke was required.

112. Audit and package the Phase D dashboard/follow-up-practice milestone. Verdict: `ready`. Integrated browser evidence covered new setup, initial practice, immutable in-session retry, Coach Update review and update replacement, add/remove/clear/clear-all/reorder queue behavior, queued-round launch and completion, one-question launch, and browser recovery. The senior pass fixed a latent 50-session cross-role inventory truncation by adding a complete candidate-owned dashboard history read, confined keyboard focus and inertness to the nested clear-all confirmation, and added immediate request claims so rapid fixed-set or builder activation cannot emit duplicate client requests. Contract review confirmed candidate plus opaque prep-context ownership, latest-attempt Coach Update truth, practiced-only debrief, original-plan coverage, shared cross-surface queue state, optimistic conflict recovery, atomic replay-safe builder launch, ready-landing continuity, exact source-session resolution, canonical root-question lineage, bounded null-profile compatibility, and candidate-safe URL pointers. Verification passed 408 candidate tests, 56 Coach Update tests, 49 next-round tests, lint, typecheck, production build, `git diff --check`, and rollback-only Coach Update, next-round draft, and prep-context practice-path DB smokes. Direct fixed-intent request idempotency, production provider orchestration, follow-up intent/session transaction hardening, browser-local Coach Update seen state, and later read projections remain explicit bounded follow-ups.

111. Unify Practice Next actions across immediate practice, fixed sets, and the editable builder. Reviewed V1's `Practice this now`, local queue toggle, next-round modal, and generic `/practice` launch against V2's candidate-owned draft and immutable intent boundaries. Preserved the two useful candidate meanings; reinterpreted them as `Practice this now` for a one-question ready intent, `Add to next round` for a durable draft mutation that stays in place, `Finish planned practice` for an immutable unanswered-plan set, `Customize round` for the editable builder, and `Start practice` for atomic draft snapshot; retired browser-local queue truth and the generic setup fallback; and deferred candidate-facing coach bundles until a trusted recommendation producer exists. Practice Next, Coach Update, and Coach Plan now resolve the same server-derived eligible choice and queued root, with one authoritative count and add/remove state. Fixed coach-bundle source handling is executable and tested without fabricating bundle content. Direct intent creation now loads each exact candidate-owned source session rather than relying on a latest-50 window. Rapid duplicate mutations are guarded immediately, stale versions replace local state and explain that newer changes were loaded, invalid choices suppress the queue claim, and every immediate/fixed/builder launch remains on `/candidate/practice/ready/[intentId]` with source and per-item provenance intact. Live profile-backed validation covered Coach Update add/remove with count synchronization, Coach Plan action parity, empty-draft restoration, desktop/mobile containment, and a clean console. Focused tests, all 406 candidate tests, 56 Coach Update tests, the expanded next-round suite, lint, typecheck, production build, and diff check pass.

110. Wire the durable next-round draft into a responsive practice builder. Reviewed V1's prep-context queue modal against the V2 evidence-first queue and launch contracts. Preserved truthful queued count, contextual question review, add/remove/clear, and one explicit launch; reinterpreted queue truth as a candidate-owned server draft keyed by opaque `role_profile_id`; retired title-keyed selection, volatile content copies, generic setup launch, silent stale-tab overwrite, and drag-only ordering; and deferred cross-surface Practice Next toggles and bundles to slice 111. The server derives every eligible choice from the canonical selected-context Coach Plan and accepted latest-answer coaching evidence, derives practice kind and provenance instead of trusting browser payloads, and returns an authoritative builder after each mutation. The responsive UI uses a right-edge anchored desktop panel and full-width mobile bottom sheet with focus trap, Escape/clickaway/close exits, accessible move controls, capacity handling, confirmed clear, conflict recovery, and truthful disabled/loading/error states. Atomic launch preserves the draft until a durable ready destination exists, recovers duplicate launch, and always stops at `/candidate/practice/ready/[intentId]`. Live persisted-data validation covered add, count, reorder, clear, desktop/mobile containment, exact selected-context identity, and restoration of the original empty draft; it also found and fixed a desktop left-edge anchoring defect. Focused tests, all 403 candidate tests, 56 Coach Update tests, lint, typecheck, production build, and diff check pass.

109. Shape the selected-context Coach Plan reference. Reviewed V1's fixed plan framing, category teaching sheets, answered-first question sequence, and score-driven preparedness/skills surfaces against the V2 evidence-first architecture. Preserved target role, initial interview stage, baseline question count, category teaching, practiced coverage, and deliberate reveal of upcoming wording; reinterpreted coverage through canonical baseline-root evidence; retired score/mastery claims, preparedness gauges, the premature Skills face, and duplicate transcript content; and deferred queue mutation, saved face preference, resume-driven question versioning, and production evaluator aggregation. Profile-backed dashboard reads now load the complete candidate-owned selected-context session history instead of trusting the latest-50 activity window. The reference fixes the baseline to the earliest original session, counts durable submitted answers from active or completed rounds, maps repeated follow-ups to one root question, ignores drafts, and rejects malformed lineage. The responsive, focus-managed Categories/Question Set dialog supports Escape, clickaway, explicit close, category teaching, and hidden-until-revealed missing-evidence questions. Browser validation on a live profile-backed context confirmed baseline/stage/coverage truth, desktop and 390px containment, and a clean console; it also found and fixed a mobile implicit-grid shrink that had collapsed the summary row. Focused tests, all 402 candidate tests, 55 Coach Update tests, 27 next-round tests, lint, typecheck, production build, and diff check pass.

108. Shape the artifact-backed Coach Update entry and debrief. Reviewed V1's full-card entry, clickaway modal, Embla question carousel, accessible previous/next controls, and practice exits against the V2 evidence-first artifact boundary. Preserved the useful debrief interaction; reinterpreted all claims through the exact newest candidate-owned artifact attempt; retired mutable score-shaped chips, completed-only fallback, unanswered-plan questions, the inline transcript, and obsolete detail CSS; and deferred durable read receipts, analytics, queue toggles, provider retry controls, and final answer annotation. The dashboard now projects awaiting-practice, pending, ready, and unavailable states without exposing operational errors. Ready artifacts receive a hydration-safe browser-local `New` treatment keyed only by opaque context/session and artifact values; opening performs no server mutation, shifts action priority to Practice Next when appropriate, and opens only practiced questions in a focus-managed carousel with keyboard, clickaway, close, and question-level focused-practice exits. Live persisted-data checks confirmed truthful unavailable and awaiting-practice states, mobile containment, and a clean browser console; the ready modal and browser-held `New` lifecycle are covered without mutating candidate history by focused interaction tests. All 396 candidate tests, 55 Coach Update tests, 27 next-round tests, lint, typecheck, production build, and diff check pass.

107. Replace the dashboard scaffold with the stable state-priority shell. Reviewed V1's identity badge, role switcher, Practice Next control, Coach Update entry, Coach Plan reference, and history treatment against the ratified evidence-first information architecture. Preserved persistent identity/context and one clear next action; reinterpreted the page as a responsive learning-loop workspace; retired the provisional metric strip, equal-weight three-card row, title-keyed presentation, duplicate latest-round transcript, and their unused CSS; and deferred durable queue UI, final Coach Update navigation, Coach Plan detail, history, and cross-device `New` state. The page now reads candidate profile identity, canonical opaque prep-context selection, active-round priority, one sparse artifact-backed Coach Update entry, Practice Next, Coach Plan, and one synthesized detail section. A completed selected-context plan without an executable follow-up remains buttonless rather than misrouting to generic setup. Browser verification confirmed desktop/mobile viewport containment, closed/open prep-menu behavior, sticky-header geometry, and no runtime warnings. Focused tests, all 390 candidate tests, lint, typecheck, production build, and diff check pass.

106. Replace silent manual prep-context reuse with an explicit candidate choice. Reviewed V1's same-role/JD create-or-resolve behavior and retired reuse once a profile has session activity while preserving candidate ownership, exact normalized matching, and repair-only reuse of empty profiles. Migration 014 adds a private positive practice-path ordinal so an explicit choice can create a new independent `role_profile_id` without inherited sessions, answers, plan coverage, queue, coaching, or evaluator evidence. `/candidate/setup/start` returns every candidate-owned active/paused exact match and its creation/activity, role/JD, original stage/count, completed-session/question, and active-round facts before any session mutation. The setup dialog offers `View in dashboard` or `Start a separate path`, preserves the draft on close/failure, and clears it only after an intentional destination succeeds. If profile creation succeeds but session creation fails, the explicit retry reuses that empty independent profile rather than accumulating abandoned paths. One-question and multi-question follow-up launch contracts were rechecked and ratified to always stop at the durable pre-session landing; direct resume remains limited to an already-started/consumed round. Resume revisions, candidate-approved question replacement/versioning, staged resume labels, and new-profile interview-stage lineage are documented but deferred. Focused route/UI/repository tests, the candidate suite, migration apply, rollback-only DB smoke, lint, typecheck, production build, and diff check pass.

105. Add the durable next-round draft and atomic queue-to-intent snapshot contract. Preserved V1's prep-context-scoped add/remove/clear/review behavior while retiring dashboard-local queue truth, role-title identity, volatile content copies, and generic setup launch. Migration 013 adds one candidate-owned versioned draft per opaque `role_profile_id`, normalized ordered item rows with stable session/question pointers and provenance, ownership FKs, deduplication, capacity, and optimistic mutation semantics. Add/remove/clear/reorder operations fail closed on stale versions or invalid evidence. The atomic PostgreSQL snapshot function locks and revalidates the exact draft, source question, latest answer/analysis relationship, and ordered payload before creating one immutable `practice_builder` intent, clearing item rows, and advancing the draft version; duplicate submissions recover the same ready intent or consumed session. Queue item assembly lineage is carried into follow-up setup, plan, and wording snapshots. Canonical root-question resolution now traverses repeated follow-up chains, preserves monotonic question-attempt numbering, and prevents two queued occurrences of the same root question from launching in one round. Direct one-question and fixed-bundle paths remain unchanged. Focused tests and rollback-only PostgreSQL smoke cover ownership, conflicts, duplicate replay, invalid-payload non-mutation, and session lineage. Final queue UI and staged resume-version consumption remain deferred to dashboard/builder design slices.

104. Implement the versioned post-session Coach Update artifact boundary. Preserved V1's stable latest-practice Coach Update node and question-first review while replacing legacy summary/fallback derivation with a candidate-owned, opaque-prep-context artifact over one immutable first-write completion snapshot, the latest answer attempt for every answered source item, and matching completed `candidate_coaching` evaluator runs whose validation disposition is accepted. Fixture analysis now persists its accepted evaluator run before updating the slot-keyed compatibility projection. Synthesis rejects missing, mismatched, superseded, cross-context, or stale evidence; excludes skipped/unanswered plan items; carries same-source-question comparison lineage without claiming repetition is improvement; and validates exact candidate-safe content before a requested artifact can complete. Source-session locking makes same-input claims replay-safe, expires abandoned requests after a bounded lease, and makes terminal failures eligible for a later generation attempt. Completed reads require accepted validation and reject undeclared or score-like fields. Completion remains successful when synthesis is unavailable, and replay can repair a missing unchanged artifact. The dashboard now consumes only an artifact matching the latest selected-context completed round and no longer falls back to legacy Coach Update content. Its provisional evidence widgets also count active-round question occurrences without treating retries as additional answered questions, while current coaching requires analysis for the latest submission. First accepted answers now promote durable sessions from `planned` to `in_progress`; migration 012 repairs historical answered sessions and an exact 0-completed/2-answered/2-coached interruption regression protects the discovered browser journey. Migrations 011-012, rollback-only DB smoke where applicable, local bootstrap/readiness wiring, domain/generation/detail/read-model/route tests, and the deterministic browser-validation path landed. Production synthesis/model wiring, final Coach Update UI, durable seen state, queue UI, and historical artifact backfill remain later work.

103. Make candidate-owned prep-context identity authoritative across dashboard and follow-up practice. Added migration 010 to carry nullable `role_profile_id` on durable practice intents under a composite candidate/profile ownership constraint and to backfill only traceable legacy intents. Dashboard reads now group profile-backed sessions by opaque identity, keep same-title contexts separate, reject invalid or unauthorized selectors through candidate-scoped resolution, and canonicalize to `/candidate/dashboard?prep=<roleProfileId>`. `targetRole` can select only historical null-profile contexts. Focused and plan-aware intents preserve one prep context, follow-up creation proves every source session is candidate-owned and context-consistent, attempt counts remain context-scoped, and ready/session/completion return links preserve the opaque identity. Bounded legacy rows remain readable; final host JobCollectionID/RequirementID resolution and archived-profile behavior remain later contracts.

102. Make opaque prep-context identity authoritative for newly created V2 setup work. Restored V1's useful concurrent create-or-resolve pattern through an injected V2 repository, while adding candidate ownership verification for explicitly requested profiles and a trust flag that prevents production launch identities from creating host-labeled contexts from browser role/JD text. Explicit dev/manual setup creates or reuses an active/paused profile from candidate id plus normalized role and normalized JD hash, keeping same-title/different-JD contexts distinct and storing only resume inclusion/mode metadata in the profile. Concurrent insert conflicts reread the winning profile. `/candidate/setup/start` now resolves the prep context before session persistence and writes non-null `role_profile_id`; unresolved or unowned profiles fail before session creation, durable identities no longer silently fall back, and the provisional browser bridge remains explicitly nonproduction-only. No dashboard composition or title-keyed read behavior changed.

101. Ratify the evidence-first candidate-dashboard information architecture. Reviewed V1, the current V2 scaffold, ADR-0008, the design-system wireframe direction, and the refactor persistence guardrails. Preserved Coach Plan, question-first review, role switching, flexible follow-up practice, and the feedback/feedforward loop; reinterpreted Coach Update as a versioned post-session synthesis distinct from immediate in-session feedback; and retired normalized role-title identity, unanswered items inside Coach Update, duplicate review surfaces, analytics-as-coaching, hidden-score/mastery claims, and dashboard-local queue truth. Established the stable-shell state priority, source/claim matrix, candidate-safe repeat-practice comparison, opaque prep-context invariant, durable editable queue, direct fast paths, and atomic queue-to-immutable-intent launch contract. ADR-0010 supersedes the conflicting parts of ADR-0008. No production dashboard composition changed in this contract slice.

100. Close and browser-validate the production candidate session arc. Initial-round evidence covers Q1-Q3 immutable feedback retries, latest-attempt coaching for all five questions, acknowledgement-stage skip on Q2, refresh recovery on answer and feedback screens, final-stage completion, selected-role dashboard return, and live DB reconciliation of eight correctly linked attempts with five latest candidate-safe analyses and no duplicate from partial-submit recovery. Follow-up evidence covers completing a four-question plan-aware queue round and a fresh one-question coach-feedback round, including return-to-dashboard/resume from an unsubmitted draft and staged feedback, same-session new-tab recovery, a final-question feedback retry, acknowledgement-stage skip-to-finish, and selected-role dashboard return. Live DB reconciliation confirms the four-question round retained its plan-aware source lineage and four initial attempts, while the one-question round retained coach-update source lineage, two immutable linked attempts, one latest analysis, and the final skip action. The analysis-unavailable UI and analysis-only retry boundary remain covered by automated failure injection because fixture analysis is deterministic and no production provider is wired; shared configuration was not broken merely to manufacture a browser failure. The senior milestone verdict is ready with production provider orchestration, full feedback-engagement event history, and follow-up intent/session transaction hardening retained as bounded later work.

99. Implement staged evidence-first feedback and immutable retry execution. Preserved V1's useful acknowledgement -> explore/skip -> focused coaching -> retry/continue/finish cadence while replacing legacy score-driven feedback truth with the ratified V2 candidate-safe projection. The local fixture now traverses the evidence-first case, exact-span extraction, deterministic appraisal/pattern-gap, feedback validation, and candidate-safe projection boundary. The UI renders one feedback stage at a time, persists every selected transition before execution, recovers the selected stage only for the exact analyzed attempt, and keeps continue/finish candidate-controlled. Retry is hidden for legacy snapshots without immutable identity; an authorized feedback retry reopens the preserved answer and appends a linked answer attempt under a slot lock. The server rejects forged, stale, mismatched, or concurrent retry transitions. Browser validation exposed and fixed the PostgreSQL timestamp adapter rejecting driver-returned `Date` objects after a successful immutable insert; replay now repairs the latest-answer projection without duplicating the accepted attempt. Analysis retry remains evaluator work against the same attempt. Production provider and evaluator-run route orchestration remain deferred.

98. Ratify the production evidence-first evaluator contract. Established one immutable answer attempt and exact question occurrence as the evaluation unit; a two-call extraction/composition architecture with deterministic schema, fingerprint, span, applicability, qualitative-band, pattern-gap, technical-reference, privacy, and candidate-safe feedback validation between calls; conditional verification; hard runtime budgets; metadata-only telemetry; and replay-safe evaluator-run requirements. Ratified internal qualitative bands as evaluator/QA facts rather than candidate-visible scores, fail-closed technical correctness without versioned references, and no raw prompt/unvalidated output retention by default.

97. Add immutable answer-attempt and evaluator-run lineage. Reviewed V1's overwrite-on-retry behavior and retained pre-submit editing as one discardable draft while reinterpreting a candidate retry after feedback as a future appended attempt. Added `candidate_answer_attempts` and `candidate_answer_evaluation_runs` with candidate/session ownership, exact question-occurrence lineage, per-occurrence attempt numbering, immutable answer rows, idempotency, provider/model/prompt/evaluator metadata, terminal lifecycle checks, and session-delete cascades. The repository serializes same-slot writes, rejects unrelated or out-of-order retry lineage, supports replay-safe evaluator start/completion/failure, and keeps provider retry or QA A/B runs attached to one fixed answer attempt. `/candidate/session/[sessionId]/answers` now appends or replays attempt one before updating the latest-answer compatibility projection, allowing a failed projection write to recover without duplicating history. QA case ids include normalized attempt identity while legacy rows retain compatible ids. Migration 009, rollback-only DB smoke, local bootstrap/readiness wiring, domain/repository/route tests, and candidate-suite coverage landed. Candidate-visible attempt UI, feedback-triggered append, and analysis-route evaluator-run creation remain deferred to their product slices.

96. Productize answer capture and mutation states. Reviewed V1's `CandidateActiveQuestionWorkspace`, `useSessionAnswerMutations`, and `FeedbackDrawer` against the shared-session and evidence-first refactor specs. Preserved one-question focus, explicit answer submit, visible coach-working state, candidate-controlled feedback progression, answer recovery, and mode affordance parity. Retired V1's generic full-screen loader, browser alert, whole-session optimistic rollback, and combined submit-plus-analysis failure semantics. Added an audience-neutral answer mutation contract for idle, dirty, saving, saved, submit, analysis, ready, and retryable failure states. Durable drafts now debounce and serialize writes, flush on blur/submit/page exit where supported, and retry in place without locking the response; browser-bridge drafts persist with their exact question. Accepted answers become read-only. Analysis failure truthfully preserves the saved-answer state and retries only the idempotent analysis route, including recovered pending-analysis answers. Failed answer or analysis work clears its pending idempotency record; abandoned pending work reopens after a two-minute recovery window instead of stranding the candidate. Voice and photo remain unavailable pending explicit privacy, permission, storage, transcription, and provider contracts.

95. Establish the real shared live-practice shell. Replaced the candidate preview/live scaffold with one audience-neutral, one-question workspace driven by `SessionRuntimeFacts`. Candidate-led practice now shows role and question progress, typed answer capture, candidate-safe coaching, exact-question/draft recovery, and a persistent Return to dashboard exit that leaves durable progress intact; invited adapters can instead return to the original invitation boundary. Legacy preview progress migrates to the same live question, and candidate UI no longer exposes preview navigation, Back to plan, or a resume-session detour. Preserved the 1.25-second landing transition as "Entering practice space" and, after browser validation, matched v0.5's stronger sequencing: fade in over landing, mount live practice beneath the overlay, then fade out. Added an optional shared audio lifecycle contract for Q1 landing prefetch, current/next question prefetch, user-gesture unlock, and browser-session play-once memory without copying V1's legacy TTS authorization route. Validation remediation also canonicalized role-scoped dashboard return links so Current focus stays stable, routed the green feedback action to focused-practice staging, changed Finish planned practice to create one plan-aware durable intent for all unanswered questions instead of opening generic setup, made both practice-intent JSONB parameters explicitly serialized so node-postgres cannot reinterpret the items array as a PostgreSQL array, made the focused-practice session-creation redirect relative so internal bind hosts and ports cannot leak into browser navigation, and made that explicit Start action create its follow-up session at live Q1 because focused-ready is already the round's landing surface. The destination session now owns the one-time routed entry transition so it can mount Q1 beneath the overlay and perform the real fade-out after navigation. Final candidate finish CTA presentation, production TTS transport, production media, and invited route wiring remain deferred.

94. Build the production-shaped pre-session landing experience. Replaced the candidate planned-session scaffold with a shared initial/follow-up landing composition grounded in durable session or practice-intent facts. Initial rounds show role, stage, question count, resume inclusion, pause/recovery, candidate-led practice disclosure, and one Start practice action; follow-up rounds add the selected question list and retain their durable POST mutation. Both pass through a brief preparing-practice transition that does not claim to generate an existing round or enter an interview room. Fixture preview moved behind development tools. Added an unwired shared invited variant that preserves V1's initials -> landing sequence, explicitly treats initials as a possible-misinvitation signal rather than identity proof, points return behavior to the original invitation, and separates recruiter answer visibility from candidate-only AI coaching. Refined the landing into a calm, grid-led readiness surface with the canonical TalentArbor logo header, a compact page heading and guidance, and a bento-style round widget. The role occupies a full `--candidate-primary-wash` title band; stage, question count, and resume render as equal-width two-row fact columns without separators; blue and neutral border segments follow the surface they touch; and a short top-down neutral/blue shadow defines the widget edge. The reassurance widget uses `--candidate-accent-wash` with accessible dark-neutral text, establishing a distinct supportive purpose rather than an accidental second blue. Native 24px card silhouettes and the 12px action radius remain intact. The primary action uses the system's semantic CTA elevation: a short dark-neutral shadow with a nearly imperceptible blue tint. Treat the broader masonry/bento language as an active visual direction to test across later candidate surfaces before promoting the custom widget, border, or reassurance treatments into shared design-system primitives. The landed typography pairs Atkinson Hyperlegible Next for body/UI legibility with IBM Plex Sans for display character; permanent typography specimen documentation remains deferred. Deferred: invite-token route wiring, initials persistence/match reporting, exact affirmative-consent requirements, dedicated optional pre/post confidence persistence, and shared-component promotion.

93. Re-scope the real candidate UI build runway before implementing attempt UI. Inspected V1 setup/session, shared feedback, summary, and dashboard behavior alongside the refactor pack. Preserve V1's pre-session entry, one-question live workspace, mode parity, pause/resume, staged feedback actions, and explicit completion transitions; reinterpret its candidate summary and dashboard against V2 destinations and evidence-first facts; retire preview-as-product, duplicate runtimes, and legacy score-driven truth; defer candidate-visible attempt counts, production media, and trend UI. Staged slices 94-98 as the next candidate-session productization commit milestone.

92. Consume follow-up attempt context in dashboard/session read models. `candidate-completed-round-read-model` reads `setupSnapshot.followUpPractice` and exposes follow-up session/question attempt context. `candidate_dashboard_v2_read_model` derives selected-context attempt rollups. Tests prove repeated follow-up practice remains linked to the source question and does not inflate baseline coverage.

91. Create live follow-up sessions from durable practice intents. `/candidate/practice/ready/[intentId]/start` resolves identity, loads a ready candidate-owned intent, creates a normal follow-up `candidate_practice_sessions` row, marks the intent consumed with the created session id, and redirects to `/candidate/session/[sessionId]`. Repeated practice is intentionally unlimited and carries session/question attempt lineage.

90. Create durable follow-up practice intents from dashboard actions. `candidate-practice-intent-creation` converts one or many resolved follow-up items into a ready `candidate_practice_intents` row. `POST /candidate/practice/ready/intents` supports future builders/queues, and the existing pointer bridge can create one-item durable intents when persistence is available.

89. Add durable follow-up practice intents for one or many selected questions. Added `candidate_practice_intents`, migration/script/smoke wiring, repository, fail-closed durable intent builder, and `/candidate/practice/ready/[intentId]`. The durable route validates candidate ownership and supports the same staging surface for one-question and multi-question follow-up practice.

88. Rehome follow-up practice staging away from generic setup. Added `/candidate/practice/ready` for follow-up staging, moved Coach Update question-level practice actions there, and restored `/candidate/setup` to generic new-prep-context setup only. Captured future practice builder, fast path, plan-aware queue, coach bundles, and foundational guidance ideas in the spec.

87. Validate follow-up practice intents against durable candidate-owned facts. The resolver upgrades Coach Update practice intent pointers only when the current candidate owns the source session, the source question key exists, selected target context matches when present, and the requested intent matches actual answer/coaching evidence.

86. Teach the app how to parse Coach Update practice intent pointers. Added a fail-closed parser that accepts only stable intent, source practice-session id, and question key values, and does not echo arbitrary URL content, answer text, coach text, JD text, resume text, or score-like values.

85. Shape the first focused-practice action boundary from Coach Update detail. Coached answers expose `practice_from_feedback`; the scaffold also exposed skipped/unanswered questions as `practice_missing_evidence`. Slice 101 supersedes that placement: missing coverage remains actionable through Coach Plan/Practice Next and is excluded from Coach Update. Action hrefs carry only stable source metadata and deliberately do not prefill setup or put sensitive content in URLs.

## Completed Phase Summary

- Slices 1-3: pre-reset repairs and temporary V2 route shells.
- Cleanroom reset decision: from slice 4 onward, the rebuild intentionally starts from minimal app code and brings old app behavior back only when a numbered slice requires it.
- Slices 4-16: canonical route scaffold, first tracked design-system primitives, public root rebuild, layout tokens, design-system import, and temporary-route redirects.
- Slices 17-29: `/candidate/setup` UI, typed setup payload, local draft preservation, host-launch contract scaffolding, dev/prod launch boundaries, launch context, launch-session schema, and setup-to-session server transition.
- Slices 30-40: planned-session shell, deterministic question planning, provider-free wording boundary, fixture wording, preview shell, setup reset boundary, browser progress, and first durable `candidate_practice_sessions` persistence boundary.
- Slices 41-50: durable setup-created session creation, durable session recovery, answer-draft shell/persistence, answer lifecycle stub, setup-start validation, local dev host-launch hardening, cross-tab recovery, and active progress persistence.
- Slices 51-73: live-question runtime boundary, typed answer submission, idempotency, analysis adapter, fixture provider, accepted analysis snapshots, coaching facts, feedback interaction contract, feedback action persistence, completion snapshots, completed-round read model, QA export contract, and live-session phase packaging.
- Slices 74-84: first dashboard read model and UI, feedback/feedforward split, dashboard recovery fixes, live completion routing, selected-context scoping/switching, active-round resume state, latest-round review, Coach Update detail contract, and first opened Coach Update surface.
- Slices 85-92: focused-practice action boundary, safe intent parsing, durable candidate-owned intent validation, follow-up staging, durable one-or-many intents, intent creation routes, live follow-up session creation, and attempt-context read consumption.
- Slices 93-99: V1/refactor-plan UI readiness synthesis, production pre-session landing, shared live practice, productized answer mutations, immutable attempt/evaluator lineage, ratified evidence-first evaluator contract, and staged candidate-safe feedback with linked retries.
- Slices 100-124: browser-validated candidate session arc, evidence-first dashboard IA, opaque prep-context propagation, versioned Coach Update artifacts, durable next-round drafts and atomic intent launch, explicit duplicate-path setup choice, stable dashboard shell, artifact-backed Coach Update debrief, Coach Plan reference, shared Practice Next actions, the cleared Phase D integration milestone, the production-shaped Coach Update synthesis runtime, durable evaluator-run fencing, the provider-neutral evidence-first evaluator runtime, the evaluator failure/recovery matrix, the ratified production integration contract, immutable evaluator configuration identity, the Google adapter conformance boundary, exact route-level Google runtime selection, bounded continue-without-coaching recovery, the explicit synthetic live/golden evaluator validation and offline comparison path, credentialed live-provider/browser/DB reconciliation, and serving-profile ratification.
- Slices 125-133: hardened one-time host-launch exchange, TA-only MSSQL launch-context lookup, setup-entry staging and journey propagation, privacy-safe staging acceptance probe, completed-round evaluator repair, first production Coach Update Google adapter, explicit credentialed Coach Update validation, configuration-aware immutable artifact claims, durable completion/replay/dashboard reconciliation, and the integrated migration/security/privacy/dependency milestone audit.

## Archive Pointers

- Full pre-compaction handoff snapshot: [handoff-pre-compaction-2026-07-12](./reference-archive/handoff-pre-compaction-2026-07-12.md).
- Historical V1/interim architecture and SQL references: [Reference Archive](./reference-archive/README.md).
- Current local setup and DB commands: [Local Dev Bootstrap](./09-dev/local-dev-bootstrap.md).

## Deprecation Check

For each future candidate workflow or dashboard refactor slice:

- Name the old surface or helper being superseded.
- Search call sites before removing or replacing behavior.
- Classify the old code as `keep as transition`, `remove now`, or `mark for retirement`.
- Keep tests aligned with that classification.
- Record transition/retirement status here when it affects future work.

## V1 Reference Rule

Before each meaningful candidate workflow slice:

- Inspect the matching V1 behavior from `feature/candidate-module` without switching the active rebuild worktree when possible.
- Record the V1 files checked and the behavior contract learned from them.
- State whether each relevant V1 behavior is `preserve`, `reinterpret`, `retire`, or `defer`.
- If V1, the original refactor pack, and the current V2 contract disagree, name the divergence before implementation so the user can steer.

V1 is a behavior reference, not a default source-code donor. The cleanroom V2 contracts remain the landing structure.

## Refresh Rule

At the end of each meaningful work session, rewrite this file so the next session can resume without reading the full doc set.
